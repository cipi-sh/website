window.CIPI_DOCS = [{"p":"getting-started","c":"Getting Started","id":"installation","h":"Installation","t":"Getting Started Installation Cipi installs a complete Laravel-ready production stack on Ubuntu 24.04 or 26.04 with a single command. The installer takes roughly 10 minutes and sets up Nginx, MariaDB, PHP, Valkey, Supervisor, Fail2ban, UFW, Certbot, Deployer, and the cipi CLI itself. During installation, the wizard asks for your SSH public key (accepted formats: ssh-rsa , ssh-ed25519 , ecdsa ) before any package is installed. Cipi creates a dedicated cipi Linux user for admin SSH access and applies SSH hardening: root login is disabled, cipi uses public-key only (group cipi-ssh ), app users (group cipi-apps ) can connect with password. Login attempts are limited to 3 with a 20-second grace period, and X11 forwarding is disabled. The installer also generates a random 40-character root password , stores it in /etc/cipi/server.json , and displays it in the installation summary. Pasted SSH keys are automatically sanitized — comments, carriage returns, and excess whitespace are stripped before validation. Standard installation bash Copy $ wget -O - https://cipi.sh/setup.sh | bash Non-interactive installation For automated setups, pass your SSH public key via the SSH_PUBKEY environment variable to skip the interactive prompt: bash Copy $ SSH_PUBKEY=\"ssh-ed25519 AAAA...\" wget -O - https://cipi.sh/setup.sh | bash If you don't have an SSH key yet, generate one with: ssh-keygen -t rsa -b 4096 AWS (root login disabled by default) bash Copy $ ssh ubuntu@your-server-ip $ sudo -s $ wget -O - https://cipi.sh/setup.sh | bash At the end of the installation you will see a summary screen with the SSH access details, the auto-generated root password, and the MariaDB root password. Save them immediately — they are shown only once. Credentials are stored in /etc/cipi/server.json , which — like all Cipi configuration files — is encrypted at rest using AES-256-CBC via the built-in Vault system. Credentials, SSH keys, .env files, and database dumps are protected both on disk and during trans"},{"p":"getting-started","c":"Getting Started","id":"requirements","h":"Requirements","t":"Requirements Ubuntu 24.04 or 26.04 LTS (fresh installs only; interim releases are not supported) Root access (or sudo -s on AWS) Ports 22, 80, and 443 open A clean server — do not install Cipi on a server with an existing web stack Cipi is tested and works on: DigitalOcean, AWS EC2, Vultr, Linode / Akamai, Hetzner, Google Cloud, OVH, Scaleway, and any KVM or bare-metal host running Ubuntu."},{"p":"getting-started","c":"Getting Started","id":"quickstart","h":"Quick Start","t":"Quick Start 1. Create your first app The interactive wizard asks for a username, primary domain, Git repository URL (SSH format) and branch when applicable, and PHP version. Laravel apps always require a repository; for custom apps you can leave the repository empty to host the site via SFTP only — see custom apps . bash Copy $ cipi app create Or pass all flags directly to skip interactive mode: bash Copy $ cipi app create \\ --user=myapp \\ --domain=myapp.com \\ --repository=git@github.com:you/myapp.git \\ --branch=main \\ --php=8.5 At the end, Cipi prints a credentials summary — save it, shown only once — including the server's public IP address (for DNS configuration), the SSH deploy key, database credentials, a ready-to-use mariadb+ssh:// connection URL for GUI clients (TablePlus, DBeaver, Sequel Pro), webhook URL, and webhook token. 2. Add the deploy key to your Git provider If you have configured a GitHub or GitLab token via cipi git , this step is automatic — Cipi adds the deploy key and creates the webhook for you. See Git auto-setup for details. Otherwise, copy the ssh-ed25519 ... key shown after app creation and add it as a Deploy Key in your repository: GitHub: Repository → Settings → Deploy keys → Add deploy key GitLab: Repository → Settings → Repository → Deploy keys 3. Prepare your Laravel project Cipi uses the database driver for cache, sessions, and queues. Run these once inside your Laravel project, commit, and push the generated migrations: bash Copy $ php artisan cache:table $ php artisan session:table $ php artisan queue:table $ php artisan migrate Cipi automatically runs artisan migrate --force on every deploy. The cache, session, and queue tables will be created on first deploy if you commit the migrations. 4. Deploy bash Copy $ cipi deploy myapp Deployer clones your repo into a new releases/N/ directory, runs composer install --no-dev , links .env and storage/ , runs migrations, runs artisan optimize , creates storage:link , swaps the current symli"},{"p":"getting-started","c":"Getting Started","id":"tech-stack","h":"Tech Stack","t":"Tech Stack Cipi brings a complete, production-ready stack to your Ubuntu server. Here is everything that gets installed and configured: Component Role Ubuntu Base OS (24.04 or 26.04 LTS) Nginx Web server, reverse proxy, SSL termination PHP-FPM PHP runtime (8.3–8.5; ondrej/php, packages.sury.org, or Ubuntu main per release) MariaDB Relational database (drop-in MySQL replacement) Valkey In-memory store for cache, sessions, queues, broadcast (BSD-licensed Redis fork) Supervisor Process manager for Laravel queue workers Deployer Zero-downtime deployment tool Certbot Let's Encrypt SSL certificates UFW Firewall (ports 22, 80, 443) Fail2ban Progressive brute-force protection with recidive jail unattended-upgrades Automatic security patches Composer PHP dependency manager cipi CLI Server management and orchestration"},{"p":"getting-started","c":"Getting Started","id":"app-structure","h":"App Structure","t":"App Structure When you run cipi app create , Cipi creates a fully isolated environment for the app. Here is everything that gets set up: /home/myapp/ ← isolated Linux user (chmod 750) ├── .ssh/ │ ├── id_ed25519 ← deploy key (private) │ └── id_ed25519.pub ← deploy key (public — add to Git) ├── .deployer/ │ └── deploy.php ← Deployer config (auto-generated) ├── current -> releases/3/ ← symlink to active release ├── releases/ │ ├── 1/ │ ├── 2/ │ └── 3/ ← latest release (last 5 kept) ├── shared/ │ ├── .env ← auto-compiled by Cipi │ └── storage/ ← persistent storage └── logs/ ├── nginx-access.log ├── nginx-error.log ├── php-fpm-error.log ├── worker-default.log └── deploy.log In addition to the home directory, Cipi creates these system files: PHP-FPM pool: /etc/php/8.5/fpm/pool.d/ myapp .conf (user=myapp) Nginx vhost: /etc/nginx/sites-available/ myapp Supervisor: /etc/supervisor/conf.d/ myapp .conf Crontab: * * * * * php artisan schedule:run MariaDB: database ' myapp ', user ' myapp '@'localhost' The .env is auto-compiled with all credentials — database name, password, webhook token, and APP_KEY . You never have to touch it manually, but you can always edit it with cipi app env myapp ."},{"p":"agent","c":"Cipi Agent","id":"agent-install","h":"Installation","t":"Cipi Agent Installation cipi-agent is a lightweight Laravel package that connects your application to Cipi. It provides a webhook endpoint for automatic deploys on git push , a health check endpoint, an MCP server for AI assistants, a database anonymizer for GDPR-safe data exports, and a set of useful Artisan commands. bash Copy $ composer require cipi/agent The service provider auto-discovers — no configuration or config/app.php change is needed. Cipi already injected the required .env variables during cipi app create , so the package works out of the box. After installing the package, commit and push. Cipi will pick it up on the next deploy automatically. Artisan commands Command Description php artisan cipi:status Show Cipi config values and connectivity status php artisan cipi:deploy-key Print the SSH deploy key for this app php artisan cipi:mcp Show the MCP endpoint URL and setup snippets for Cursor, VS Code, and Claude Desktop php artisan cipi:generate-token {type} Generate a secure token. Type can be mcp , health , or anonymize php artisan cipi:service {type} --enable|--disable Toggle a service on or off. Updates .env in place. Type can be mcp or anonymize php artisan cipi:init-anonymize Scaffold the anonymization config at /home/{app_user}/.db/anonymization.json"},{"p":"agent","c":"Cipi Agent","id":"agent-webhook","h":"Webhook — Automatic Deploys","t":"Webhook — Automatic Deploys The agent exposes a POST endpoint at /cipi/webhook . When your Git provider sends a push event, the agent verifies the signature and writes a .deploy-trigger flag file. A cron job running every minute as the app user detects this file, removes it, and runs Deployer in the background. This design means the webhook response is instant (no HTTP timeout waiting for deployment to complete) and Deployer runs with the correct user permissions — no sudo required. Configure your Git provider Provider Webhook URL Secret field GitHub https://yourdomain.com/cipi/webhook Secret GitLab https://yourdomain.com/cipi/webhook Secret token The token to use is stored in .env as CIPI_WEBHOOK_TOKEN . You can also retrieve it any time with: bash Copy $ cipi deploy myapp --webhook Branch filtering By default every push triggers a deploy. To restrict deploys to a specific branch, add this to your .env : env CIPI_DEPLOY_BRANCH=main Pushes to any other branch will receive a skipped response and no deploy will be triggered."},{"p":"agent","c":"Cipi Agent","id":"agent-health","h":"Health Check","t":"Health Check The agent also exposes a GET endpoint at /cipi/health that returns a JSON payload with the status of the app, database, cache, queue, and the currently deployed Git commit hash. Useful for external monitoring services such as UptimeRobot. Protected by the CIPI_HEALTH_TOKEN Bearer token — generate one with php artisan cipi:generate-token health . bash Copy $ curl -H \"Authorization: Bearer YOUR_CIPI_HEALTH_TOKEN\" \\ https://yourdomain.com/cipi/health json { \"status\" : \"healthy\" , \"app_user\" : \"myapp\" , \"php\" : \"8.5\" , \"laravel\" : \"12.0.0\" , \"commit\" : \"a1b2c3d\" , \"checks\" : { \"database\" : { \"ok\" : true }, \"cache\" : { \"ok\" : true }, \"queue\" : { \"ok\" : true , \"pending_jobs\" : 0 } } } The commit field shows the short Git hash of the currently deployed release, useful for verifying which version is running in production."},{"p":"agent","c":"Cipi Agent","id":"agent-mcp","h":"MCP Server","t":"MCP Server cipi-agent includes a built-in MCP server (Model Context Protocol) that exposes your application to AI assistants such as Cursor , VS Code (with GitHub Copilot), and Claude Desktop . The endpoint implements MCP 2024-11-05 over HTTP using JSON-RPC 2.0 and is protected by the CIPI_MCP_TOKEN Bearer token. The MCP endpoint is available at POST /cipi/mcp and is disabled by default. To enable it: bash Copy $ php artisan cipi:service mcp --enable $ php artisan cipi:generate-token mcp Available tools The MCP server exposes six tools that an AI assistant can invoke by name: Tool Description health App, database, cache, and queue status — same data as the /cipi/health endpoint app_info Full application configuration: app user, PHP version, Laravel version, environment, queue/cache/session drivers, deploy branch, and all Cipi URLs deploy Trigger a new zero-downtime deployment — writes the .deploy-trigger file; Deployer picks it up within 1 minute logs Read the last N lines (default 50, max 500) from application logs. Supports type ( laravel , nginx , php , worker , deploy ), level for Laravel severity filtering (e.g. error ), and search for keyword filtering. Laravel daily rotation ( laravel-YYYY-MM-DD.log ) is auto-detected. db_query Execute SQL queries against the application database — equivalent to cipi app tinker . Supports SELECT, SHOW, DESCRIBE, EXPLAIN (read) and INSERT, UPDATE, DELETE (write). Results formatted as ASCII table, capped at 100 rows. Destructive DDL (DROP TABLE/DATABASE, TRUNCATE, GRANT/REVOKE, file I/O) is blocked. artisan Run any Artisan command (e.g. migrate:status , queue:size , cache:clear ). Long-running and interactive commands like serve , queue:work , and tinker are blocked Setup instructions Run the cipi:mcp Artisan command to get the endpoint URL and ready-to-paste configuration snippets for your AI client: bash Copy $ php artisan cipi:mcp The command prints the available tools and the JSON configuration for Cursor, VS Code, and Clau"},{"p":"agent","c":"Cipi Agent","id":"agent-anonymizer","h":"Database Anonymizer","t":"Database Anonymizer cipi-agent includes a built-in database anonymizer that creates sanitized copies of your production database — ideal for sharing with developers, QA teams, or staging environments without exposing real user data. It supports both MySQL and PostgreSQL and uses Faker -based transformations configured via a JSON file. How it works A POST /cipi/db request queues an AnonymizeDatabaseJob in the background The job runs php artisan cipi:anonymize , which dumps the database, applies Faker-based transformations according to anonymization.json , and produces a sanitized SQL file When complete, an email notification is sent with a signed download URL GET /cipi/db/{token} serves the anonymized dump via a signed URL that expires after 15 minutes A utility endpoint POST /cipi/db/user is also available for user ID lookup, useful when building anonymization rules. Setup Scaffold the anonymization configuration file: bash Copy $ php artisan cipi:init-anonymize This creates /home/{app_user}/.db/anonymization.json with a starter template. The file is stored outside the project repository for security — it never gets committed. Configuration Edit /home/{app_user}/.db/anonymization.json to define which tables and columns to anonymize: json { \"tables\" : { \"users\" : { \"name\" : \"name\" , \"email\" : \"safeEmail\" , \"phone\" : \"phoneNumber\" , \"address\" : \"address\" }, \"orders\" : { \"shipping_address\" : \"address\" , \"notes\" : \"sentence\" } } } Each key under tables is a database table name. Each nested key is a column name, and the value is a Faker formatter (e.g. name , safeEmail , phoneNumber , address , sentence ). Endpoints Method Endpoint Description POST /cipi/db Queue an anonymization job. Returns immediately; sends email when done. POST /cipi/db/user User ID lookup utility GET /cipi/db/{token} Download the anonymized dump (signed URL, expires in 15 minutes) All endpoints are protected by the CIPI_ANONYMIZER_TOKEN Bearer token. Enable the anonymizer The anonymizer is disabled"},{"p":"agent","c":"Cipi Agent","id":"agent-env","h":"ENV Variables","t":"ENV Variables These variables are automatically injected by Cipi into the app's .env during cipi app create . You normally do not need to set them manually. Variable Description Default CIPI_WEBHOOK_TOKEN HMAC secret / token for webhook validation auto-generated CIPI_DEPLOY_BRANCH Branch that triggers a deploy (empty = all branches) empty CIPI_APP_USER Linux user owning this app auto-set CIPI_MCP Enable or disable the MCP server endpoint at /cipi/mcp false CIPI_MCP_TOKEN Bearer token for MCP endpoint authentication none CIPI_HEALTH_TOKEN Bearer token for the /cipi/health endpoint none CIPI_ANONYMIZER Enable or disable the database anonymizer endpoints at /cipi/db false CIPI_ANONYMIZER_TOKEN Bearer token for the anonymizer endpoints none Use php artisan cipi:generate-token {type} to generate tokens for mcp , health , or anonymize . Use php artisan cipi:service {type} --enable|--disable to toggle services — the command updates your .env in place."},{"p":"apps","c":"Apps","id":"cli-app-create","h":"cipi app create","t":"Apps cipi app create cipi app create supports two app types: Laravel (default) and Custom ( --custom ). Laravel apps get a fully isolated environment: Linux user, PHP-FPM pool, Nginx vhost, MariaDB database, Supervisor worker, crontab entry, Deployer zero-downtime releases, SSH deploy key, and auto-compiled .env . Custom apps are simpler — see custom apps for details. Laravel app (default) bash Copy $ cipi app create Non-interactive (flags) bash Copy $ cipi app create \\ --user=myapp \\ --domain=myapp.com \\ --repository=git@github.com:you/myapp.git \\ --branch=main \\ --php=8.5 --user Linux username for the app. Must be unique, lowercase, alphanumeric. --domain Primary domain (e.g. myapp.com ). Used for Nginx vhost and APP_URL . --repository SSH Git URL (e.g. git@github.com:you/repo.git ). Must be SSH, not HTTPS. --branch Branch to deploy. Defaults to main . --php PHP version for this app (e.g. 8.5 ). Defaults to PHP 8.5. Must be an installed version — only 8.3 , 8.4 and 8.5 are accepted since v4.5.4 (Deployer 8 requires PHP &ge; 8.3). If a GitHub or GitLab token is configured, Cipi automatically adds the SSH deploy key and creates the webhook on the repository — no manual steps needed. See Git auto-setup for setup instructions and fallback options when auto-configuration is not available."},{"p":"apps","c":"Apps","id":"cli-app-custom","h":"cipi app create --custom","t":"cipi app create --custom Creates a custom app with classic deploy (no zero-downtime): code is deployed into htdocs — no current / shared symlinks. Ideal for static sites, SPAs (Vue, React, Svelte), WordPress, other CMS, or any non-Laravel PHP project or framework. During creation you only choose the document root (default / , or e.g. www , dist , public ). Nginx is pre-configured with index index.html index.php , try_files $uri $uri/ /index.php?$args , and error_page 404 /404.html — no prompts for try_files or entry point. Git optional (SFTP-only) The Git repository is optional for custom apps. Laravel apps still require a repository. If you skip the repository for a custom app, Cipi creates /home/<app>/htdocs with a placeholder index.html and does not configure a deploy key or webhook — you upload files with SFTP (or SCP/rsync) to ~/htdocs as the app user. The onboarding output explains this “no repo — SFTP only” workflow. If you do provide a repository, behavior is unchanged: use cipi deploy <app> to pull code into htdocs . What’s included and what you add Custom apps have no database , no .env , no cron, and no queue workers. When a repository is configured, a deploy key and (with Git auto-setup) webhook are shown; for SFTP-only apps, those are omitted. The post-creation summary lists SSH access and next steps accordingly. If your custom app needs a database (e.g. WordPress, Drupal), create one with cipi db create --name=<app> after deployment. See cipi db for backup, restore, and password management. With Git — non-interactive example: bash Copy $ cipi app create --custom --user=mysite --domain=mysite.com \\ --repository=git@github.com:you/mysite.git --docroot=dist SFTP-only — omit --repository and --branch : bash Copy $ cipi app create --custom --user=mysite --domain=mysite.com --docroot=dist With a repository, use cipi deploy <app> to deploy; code is cloned into /home/<app>/htdocs ."},{"p":"apps","c":"Apps","id":"cli-app-manage","h":"app list / app show / app edit / app delete","t":"app list / app show / app edit / app delete Command Description cipi app list List all apps with domain, PHP version, and status cipi app show <app> Full details: domain, PHP, deploy key, workers, webhook. For custom apps: type \"Custom\", docroot; webhook (and deploy key) omitted when SFTP-only without a repository. cipi app edit <app> --php=8.5 Hot-swap PHP version. Updates FPM pool, Nginx socket, Supervisor, crontab, Deployer config, and .env — zero downtime cipi app edit <app> --branch=develop Change the deploy branch cipi app edit <app> --domain=new.example.com Rename the primary domain (since v4.6.2 ). Validates format and uniqueness, moves the old primary to aliases, regenerates the Nginx vhost, updates APP_URL , refreshes Git webhooks when auto-configured, and re-issues Let's Encrypt when a certificate already existed cipi app env <app> Open the app's .env file in nano as the app user. Exits with error for custom apps (no .env). cipi app reset-password <app> Regenerate the app's Linux user SSH password. The new password is displayed on screen — save it immediately cipi app reset-db-password <app> Regenerate the app's MariaDB password and automatically update the DB_PASSWORD value in the app's .env file. Exits with error for custom apps (no database). cipi app delete <app> Permanently remove the app, user, database (if Laravel), Nginx vhost, FPM pool, and Supervisor workers. For custom apps, skips database drop (none was created). Asks for confirmation."},{"p":"apps","c":"Apps","id":"cli-basicauth","h":"cipi basicauth","t":"cipi basicauth Available since v4.5.2 . Protect any app — Laravel or custom — behind an Nginx username/password prompt. Useful for staging sites, internal tools, or apps that aren't ready for public traffic yet. Command Description cipi basicauth enable <app> [--user=NAME] [--password=PASS] Turn on HTTP basic auth. Credentials are generated when omitted and shown once on screen — save them immediately cipi basicauth disable <app> Remove the prompt and clear stored credentials cipi basicauth status <app> Show whether basic auth is enabled and the configured user Credentials are hashed with openssl passwd -apr1 (no apache2-utils needed) and stored in /etc/nginx/cipi-basicauth/<app>.htpasswd ; the enabled state lives in apps.json . The auth_basic directives are injected per location block, so protection survives vhost regeneration (alias changes, PHP edits) and is cloned into the :443 block by certbot — HTTPS is covered too . ACME challenges stay public, so certificate issuance and renewal are never blocked. Basic auth is removed automatically on cipi app delete . This is distinct from cipi auth , which manages the Composer auth.json for private package repositories."},{"p":"apps","c":"Apps","id":"cli-app-env","h":"Managing ENV variables","t":"Managing ENV variables Every Laravel app has a single .env file living at /home/<app>/shared/.env . Custom apps have no .env . It is created and pre-populated by Cipi during app create with the database credentials, APP_KEY , APP_URL , cache/session/queue settings, and the webhook token. The shared/ directory is symlinked into every release, so the same .env is always active regardless of which release is current. Edit interactively via CLI The safest way to change ENV values is through Cipi itself — it opens the file in nano as the app user, with the correct permissions: bash Copy $ cipi app env myapp Save with Ctrl+O then exit with Ctrl+X . Changes take effect immediately for new requests — no restart needed for most values. If you change queue connection or cache driver, restart the workers: bash Copy $ cipi worker restart myapp Edit directly via SSH You can also edit the file directly over SSH as root or as the app user: bash Copy # as root $ nano /home/myapp/shared/.env # or switch to the app user first $ su - myapp $ nano ~/shared/.env Key ENV variables set by Cipi Variable Description Set by APP_KEY Laravel encryption key — generated once at app creation Cipi APP_URL Updated automatically by cipi ssl install Cipi DB_CONNECTION Always mysql (MariaDB is drop-in compatible) Cipi DB_DATABASE / DB_USERNAME / DB_PASSWORD Auto-generated credentials for the app's isolated database Cipi CACHE_STORE database — uses the app's MariaDB Cipi SESSION_DRIVER database Cipi QUEUE_CONNECTION database Cipi CIPI_WEBHOOK_TOKEN HMAC secret for cipi-agent webhook validation Cipi CIPI_APP_USER Linux username owning this app Cipi CIPI_MCP Enable or disable the built-in MCP server at /cipi/mcp User ( true by default) Do not change DB credentials manually. If you need to regenerate the database password use cipi db password myapp — it updates both MariaDB and the .env atomically. Editing them by hand risks leaving the two out of sync. Adding your own variables Add any custom variable at"},{"p":"apps","c":"Apps","id":"cli-app-logs","h":"cipi app logs","t":"cipi app logs Tail application logs in real-time. Logs are rotated daily and kept for 14 days. By default, all logs are shown including Laravel daily logs ( laravel-YYYY-MM-DD.log ) from shared/storage/logs/ . bash Copy $ cipi app logs myapp # all logs (incl. Laravel daily logs) $ cipi app logs myapp --type=nginx # Nginx access + error $ cipi app logs myapp --type=php # PHP-FPM errors $ cipi app logs myapp --type=worker # queue worker output $ cipi app logs myapp --type=deploy # deploy history $ cipi app logs myapp --type=laravel # Laravel application logs --type=nginx Nginx access and error logs --type=php PHP-FPM error log --type=worker Supervisor / queue worker output --type=deploy Deployer output — full deploy history with timestamps --type=laravel Laravel application logs from shared/storage/logs/ (daily rotating laravel-YYYY-MM-DD.log )"},{"p":"apps","c":"Apps","id":"cli-app-artisan","h":"app artisan & app tinker","t":"app artisan & app tinker Run Artisan commands and Tinker as the app user with the correct PHP version and open_basedir context — exactly as they would run during a deploy. bash Copy $ cipi app artisan myapp migrate:status $ cipi app artisan myapp queue:retry all $ cipi app artisan myapp db:seed --class=ProductionSeeder $ cipi app artisan myapp cache:clear $ cipi app tinker myapp"},{"p":"apps","c":"Apps","id":"cli-app-ssh","h":"SSH as the app user","t":"SSH as the app user Each app runs under its own isolated Linux user. Sometimes you need to work directly inside that user's environment — inspect files, run one-off scripts, or debug something that only reproduces as the correct user. Direct SSH as app user (recommended) App users can SSH directly to the server with the password generated at app creation: bash Copy # connect as the app user (password auth) $ ssh myapp@your-server-ip # you are directly inside the app user's shell myapp@server:~$ pwd /home/myapp myapp@server:~$ cd ~/current myapp@server:~$ ls The password is shown when the app is created (or use cipi app reset-password myapp to regenerate it). This works for SFTP clients, IDE remote sessions, and terminal access. Via cipi (admin path) If you are already connected as cipi , you can switch directly to any app user: bash Copy $ ssh cipi@your-server-ip cipi@server:~$ sudo su - myapp myapp@server:~$ pwd /home/myapp Reset the app user password If you need to regenerate an app user's password (e.g. for direct SSH or SFTP), use: bash Copy $ cipi app reset-password myapp The new password is displayed on screen — save it immediately. Useful commands once logged in as the app user bash Copy # navigate to the active release myapp@server:~$ cd ~/current # run artisan directly with the correct PHP version myapp@server:~$ /usr/bin/php8.5 ~/current/artisan tinker # inspect the shared .env myapp@server:~$ cat ~/shared/.env # tail all logs myapp@server:~$ tail -f ~/logs/*.log # check active releases (ll is a built-in alias for ls -al) myapp@server:~$ ll ~/releases/ Every app user's .bashrc defines a handy ll='ls -al' alias (since v4.5.5 ) for quicker directory listings over SSH, alongside the deploy and composer shortcuts. Apps created before 4.5.5 receive the ll alias automatically on the next cipi self-update via the 4.5.5 migration . The app user's open_basedir restriction limits PHP to /home/myapp . This is enforced at the PHP-FPM level, not at the shell level — yo"},{"p":"deploy","c":"Deploy & CI/CD","id":"cli-deploy","h":"cipi deploy","t":"Deploy & CI/CD cipi deploy Cipi uses Deployer for all deployments. Every deploy is atomic: a new release directory is prepared fully before the current symlink is swapped, so traffic is never interrupted. Since v4.5.4 Cipi bundles Deployer 8 , which requires PHP &ge; 8.3 . cipi deploy and cipi deploy --rollback abort with a clear upgrade message before invoking Deployer when an app is still pinned to an older PHP version — switch it with cipi app edit <app> --php=8.3 (or higher) first. Deploy pipeline Deployer and Composer run with the app's configured PHP version (e.g. /usr/bin/php8.5 ), not the system default. This applies to cipi deploy , cipi deploy --rollback , crontab deploy triggers, cipi sync import deploys, and the deploy / composer aliases in the app user's .bashrc . Stop queue workers ( cipi worker stop ) Clone repo into releases/N/ Run composer install --no-dev (with app's PHP) Link shared/.env and shared/storage/ Run artisan migrate --force Run artisan optimize Run artisan storage:link Swap current symlink atomically Restart queue workers Prune old releases (keep last 5) bash Copy $ cipi deploy myapp # deploy latest commit $ cipi deploy myapp --rollback # instant rollback to previous release $ cipi deploy myapp --releases # list all releases with timestamps $ cipi deploy myapp --key # show the SSH deploy key $ cipi deploy myapp --webhook # show webhook URL and token $ cipi deploy myapp --unlock # remove a stuck deploy lock $ cipi deploy myapp --trust-host=git.mycompany.com # trust a custom Git server fingerprint $ cipi deploy myapp --trust-host=git.mycompany.com:2222 # trust on non-standard port (also writes ~/.ssh/config) If a deploy is interrupted (e.g. by a network error), Deployer may leave a lock file behind. Use cipi deploy myapp --unlock to remove it before re-deploying."},{"p":"deploy","c":"Deploy & CI/CD","id":"cli-auth","h":"auth.json","t":"auth.json Manage the auth.json file for an app. This file lives at /home/<app>/shared/auth.json and is automatically symlinked into every release by Deployer — exactly like .env . Use it to store structured credential data (e.g. API keys, feature flags, or any JSON payload) that your Laravel app can read at runtime. bash Copy $ cipi auth create myapp # create auth.json with initial { \"users\": [] } structure $ cipi auth edit myapp # open in $EDITOR (fallback: nano), validate JSON on close $ cipi auth show myapp # print contents formatted with jq $ cipi auth delete myapp # delete file (asks for confirmation) Command details Command Description cipi auth create <app> Creates shared/auth.json with the initial structure {\"users\":[]} , sets permissions to 640 (owner app:app ), and adds auth.json to shared_files in the app's Deployer config so it is symlinked on every deploy. cipi auth edit <app> Opens shared/auth.json in $EDITOR (falls back to nano ). After the editor closes, validates the JSON with jq and warns if the file is malformed. cipi auth show <app> Prints the contents of shared/auth.json formatted with jq . cipi auth delete <app> Asks for confirmation, then deletes shared/auth.json and removes the auth.json entry from shared_files in the app's Deployer config. Deployer integration cipi auth create automatically appends auth.json to the shared_files list in /home/<app>/.deployer/deploy.php , and cipi auth delete removes it. This means the file is treated exactly like .env : it persists across releases and is never overwritten by a deploy. Every cipi auth operation is logged via log_action for auditability. The AUTH section is also listed in the output of cipi help ."},{"p":"deploy","c":"Deploy & CI/CD","id":"git-providers","h":"Git providers","t":"Git providers Cipi is ready to work with GitHub and GitLab but it supports any other Git provider that supports SSH deploy keys — no vendor lock-in. For self-hosted or custom Git servers, you need to trust the server's host fingerprint before Deployer can clone over SSH. Use the --trust-host flag to add the fingerprint to the app user's ~/.ssh/known_hosts automatically: bash Copy # show the deploy key and add it to your Git provider $ cipi deploy myapp --key # trust a custom Git server fingerprint (standard port) $ cipi deploy myapp --trust-host=git.mycompany.com # trust a custom Git server on a non-standard port # (also writes ~/.ssh/config automatically) $ cipi deploy myapp --trust-host=git.mycompany.com:2222 When a non-standard port is specified, Cipi also writes the Host / Port entry to the app user's ~/.ssh/config so that Deployer can reach the server without any extra configuration."},{"p":"deploy","c":"Deploy & CI/CD","id":"git-auto-setup","h":"Git auto-setup","t":"Git auto-setup If you save a GitHub or GitLab Personal Access Token, Cipi automatically adds the SSH deploy key and creates the webhook on the repository every time you run cipi app create . No manual steps required. Save a token bash Copy # GitHub (fine-grained or classic PAT) $ cipi git github-token ghp_xxxxxxxxxxxxxxxxxxxx # GitLab (gitlab.com) $ cipi git gitlab-token glpat-xxxxxxxxxxxxxxxxxxxx # GitLab (self-hosted — set the URL before or after the token) $ cipi git gitlab-url https://gitlab.example.com $ cipi git gitlab-token glpat-xxxxxxxxxxxxxxxxxxxx GitHub token permissions Fine-grained tokens (recommended) need Administration and Webhooks set to Read and write on the target repositories. Classic tokens need the repo scope. GitLab token permissions The api scope is the minimum required — GitLab does not offer a more granular scope that covers both deploy keys and webhooks. Automatic lifecycle Event What Cipi does automatically app create Adds deploy key + creates webhook on the repository via API. The summary shows \"auto-configured ✓\" instead of manual instructions. app edit --repository=... Removes deploy key + webhook from the old repository, then adds them to the new one. app delete Removes deploy key + webhook from the repository before deleting the app. cipi git commands Command Description cipi git status Show provider connection status and per-app integration details (deploy key ID, webhook ID) cipi git github-token <token> Save a GitHub Personal Access Token cipi git gitlab-token <token> Save a GitLab Personal Access Token cipi git gitlab-url <url> Set the base URL for a self-hosted GitLab instance cipi git remove-github Remove the stored GitHub token cipi git remove-gitlab Remove the stored GitLab token and URL Manual setup (fallback) Auto-setup is skipped when no token is configured, when the API call fails (wrong permissions, repository not found, rate limit), or when the repository is hosted on a provider other than GitHub or GitLab (e.g. Gitea, "},{"p":"deploy","c":"Deploy & CI/CD","id":"cli-deploy-script","h":"Customising the deploy script","t":"Customising the deploy script The deploy configuration for each app is stored at: /home/ myapp /.deployer/deploy.php This file is auto-generated by Cipi during app create and updated automatically when you change the PHP version or deploy branch via cipi app edit . You can edit it to customise the deploy pipeline, but you should understand the implications before doing so. Default deploy pipeline The auto-generated deploy.php runs these tasks in order: php Copy deploy:prepare // create releases/N/ directory deploy:vendors // composer install --no-dev deploy:shared // link shared/.env and shared/storage/ artisan:migrate // php artisan migrate --force artisan:optimize // php artisan optimize artisan:storage:link // php artisan storage:link deploy:symlink // swap current → releases/N/ atomically cipi:restart-workers // supervisorctl restart myapp-* deploy:cleanup // keep last 5 releases, delete older Adding custom tasks You can add tasks before or after any step. Common examples: php Copy // Run artisan db:seed after migrations after('artisan:migrate', 'artisan:db:seed'); // Clear view cache after symlink swap after('deploy:symlink', 'artisan:view:clear'); // Custom task — send a Slack notification task('notify:slack', function () { run('curl -X POST https://hooks.slack.com/... -d \\'{\"text\":\"Deployed!\"}\\''); }); after('deploy:symlink', 'notify:slack'); Running additional artisan commands php Copy // Seed only in specific environments task('artisan:db:seed', function () { run('{{bin/php}} {{release_path}}/artisan db:seed --force'); }); Cipi may overwrite deploy.php when you run cipi app edit myapp --php=X or cipi app edit myapp --branch=X . Back up your customisations or keep them in a section clearly separated from the Cipi-managed blocks. A safe pattern is to put all custom tasks at the bottom of the file after the default task definition. Disabling a default step To skip a task — for example if you handle migrations manually — comment it out or remove it from the dep"},{"p":"deploy","c":"Deploy & CI/CD","id":"ci-pipelines","h":"CI/CD Pipelines — GitHub & GitLab","t":"CI/CD Pipelines — GitHub & GitLab Cipi supports two integration patterns with CI/CD pipelines. Choose the one that fits your workflow. Option A — Webhook (recommended) The cleanest approach: install cipi-agent in your Laravel project, configure one webhook in your Git provider, and every push to the target branch triggers a deploy automatically. The pipeline does not need SSH access to your server. bash Copy # 1. Install the agent in your Laravel project $ composer require cipi/agent # 2. Get your webhook URL and token $ cipi deploy myapp --webhook Then add the webhook in your Git provider (URL + token) and you are done. See the Cipi Agent — Webhook section for the full setup. Option B — SSH deploy from the pipeline If you want explicit control inside your pipeline (e.g. deploy only after tests pass, or only from a specific environment), you can SSH into the server and run cipi deploy directly from the CI job. Generate a dedicated ed25519 SSH key pair for the CI runner. Add the public key to /root/.ssh/authorized_keys on the server and store the private key as a CI secret. Never reuse deploy keys or personal keys. Generate a dedicated CI key bash Copy # on your local machine $ ssh-keygen -t ed25519 -C \"ci-deploy\" -f ~/.ssh/ci_deploy -N \"\" # copy the public key to the server $ ssh-copy-id -i ~/.ssh/ci_deploy.pub cipi@your-server-ip # copy the private key content → add it as a CI secret $ cat ~/.ssh/ci_deploy GitHub Actions Add the private key as a repository secret named SERVER_SSH_KEY and the server IP as SERVER_HOST . yaml Copy # .github/workflows/deploy.yml name: Deploy on: push: branches: [main] jobs: test: runs-on: ubuntu-latest steps: - uses: actions/checkout@v4 - name: Run tests run: php artisan test deploy: runs-on: ubuntu-latest needs: test # only deploy if tests pass steps: - name: Deploy via Cipi uses: appleboy/ssh-action@v1 with: host: ${{ secrets.SERVER_HOST }} username: cipi key: ${{ secrets.SERVER_SSH_KEY }} script: sudo cipi deploy myapp For rollback "},{"p":"deploy","c":"Deploy & CI/CD","id":"deploy-notifications","h":"Deploy notifications","t":"Deploy notifications Add notification steps to any pipeline to keep the team informed on deploy success, failure, and automatic rollbacks. Both examples below work with GitHub Actions and GitLab CI using only standard HTTP calls — no extra platform dependencies. Slack Add a final step that posts to a Slack webhook regardless of deploy outcome. Use if: always() in GitHub Actions so the notification fires on both success and failure. Create an Incoming Webhook in your Slack workspace and store the URL as SLACK_WEBHOOK_URL in your CI secrets. yaml Copy # GitHub Actions — deploy + Slack notification jobs: deploy: runs-on: ubuntu-latest steps: - name: Deploy id: deploy uses: appleboy/ssh-action@v1 with: host: ${{ secrets.SERVER_HOST }} username: root key: ${{ secrets.SERVER_SSH_KEY }} script: cipi deploy myapp - name: Notify Slack — success if: success() uses: slackapi/slack-github-action@v2 with: webhook: ${{ secrets.SLACK_WEBHOOK_URL }} webhook-type: incoming-webhook payload: | { \"text\": \":white_check_mark: *myapp* deployed successfully\", \"attachments\": [{ \"color\": \"good\", \"fields\": [ { \"title\": \"Branch\", \"value\": \"${{ github.ref_name }}\", \"short\": true }, { \"title\": \"By\", \"value\": \"${{ github.actor }}\", \"short\": true }, { \"title\": \"Commit\", \"value\": \"${{ github.sha }}\", \"short\": false } ] }] } - name: Notify Slack — failure if: failure() uses: slackapi/slack-github-action@v2 with: webhook: ${{ secrets.SLACK_WEBHOOK_URL }} webhook-type: incoming-webhook payload: | { \"text\": \":x: *myapp* deploy FAILED — rolling back\", \"attachments\": [{ \"color\": \"danger\", \"fields\": [ { \"title\": \"Branch\", \"value\": \"${{ github.ref_name }}\", \"short\": true }, { \"title\": \"By\", \"value\": \"${{ github.actor }}\", \"short\": true }, { \"title\": \"Run\", \"value\": \"${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\", \"short\": false } ] }] } - name: Rollback on failure if: failure() uses: appleboy/ssh-action@v1 with: host: ${{ secrets.SERVER_HOST }} username: root key: ${{ "},{"p":"deploy","c":"Deploy & CI/CD","id":"safe-deploy","h":"Safe deploy — backup before release","t":"Safe deploy — backup before release A production-grade deploy pipeline should always create a restore point before the new code goes live. Cipi provides two complementary backup commands that map to two different safety levels: bash # local DB snapshot — fast, on-disk, instant rollback $ cipi db backup myapp # → /var/log/cipi/backups/myapp_20260303_143012.sql.gz # S3 backup — DB dump + shared/ folder uploaded to your bucket $ cipi backup run myapp # → s3://your-bucket/cipi/myapp/2026-03-03_143015/db.sql.gz # → s3://your-bucket/cipi/myapp/2026-03-03_143015/shared.tar.gz Used together in a pipeline, they give you both a fast local restore point and an off-server copy of the database and all uploaded files. The deploy only starts if both backups succeed. Pre-requisite: run cipi backup configure once on the server to link your S3 credentials before cipi backup run can be used. cipi db backup works without any configuration — it is always available. What each command does internally cipi db backup <app> calls mysqldump --single-transaction --routines --triggers and gzips the output to /var/log/cipi/backups/<app>_<timestamp>.sql.gz . The file stays on the server and is never deleted automatically — add a cleanup step or a cron if disk space matters. cipi backup run <app> does two things: dumps the database with mariadb-dump --single-transaction into a temp dir, and archives the entire /home/<app>/shared/ folder (which contains .env , storage/ , and any user-uploaded files). Both archives are then uploaded to S3 under the path cipi/<app>/<timestamp>/ . The temp files are deleted after a successful upload. GitHub Actions — safe deploy workflow yaml Copy # .github/workflows/deploy.yml name: Deploy on: push: branches: [main] jobs: test: runs-on: ubuntu-latest steps: - uses: actions/checkout@v4 - run: php artisan test backup: runs-on: ubuntu-latest needs: test steps: - name: Local DB backup uses: appleboy/ssh-action@v1 with: host: ${{ secrets.SERVER_HOST }} username: root key:"},{"p":"deploy","c":"Deploy & CI/CD","id":"preview-environments","h":"Preview environments (per-branch deploy)","t":"Preview environments (per-branch deploy) Every branch in your repository can automatically get its own live URL — a fully deployed Laravel app with its own database, workers, and HTTPS. This pattern is sometimes called \"review apps\" or \"ephemeral environments\". The URL format uses three slugs separated by hyphens, so each environment is human-readable and globally unique: example URLs https://develop-acmeco-3a1f9c2e.preview.domain.ltd https://release-1-2-3-acmeco-3a1f9c2e.preview.domain.ltd https://main-acmeco-3a1f9c2e.preview.domain.ltd How the identifiers are generated Three values are derived at pipeline runtime: bash Copy # branch name → lowercase, non-alphanum → hyphens, trim edges BRANCH_SLUG=$(echo \"$BRANCH\" | tr '[:upper:]' '[:lower:]' \\ | sed 's/[^a-z0-9]/-/g; s/--*/-/g; s/^-//; s/-$//') # repo/project name → same treatment PROJECT_SLUG=$(echo \"$PROJECT\" | tr '[:upper:]' '[:lower:]' \\ | sed 's/[^a-z0-9]/-/g') # deterministic MD5 hash — same branch always gets the same environment HASH=$(echo -n \"${BRANCH_SLUG}${PROJECT_SLUG}\" | md5sum | cut -c1-8) # Cipi app username: must be lowercase alphanumeric, 3–32 chars, no hyphens # hex chars (0–9, a–f) are valid; prefix \"pr\" ensures it starts with a letter APP_NAME=\"pr${HASH}\" # e.g. pr3a1f9c2e # human-readable domain with wildcard base DOMAIN=\"${BRANCH_SLUG}-${PROJECT_SLUG}-${HASH}.${DEPLOY_WILDCARD_DOMAIN}\" Pre-requisites (one-time server setup) 1. DNS wildcard — add an A record *.preview.domain.ltd → <server-ip> in your DNS provider. All subdomains resolve automatically; no per-branch DNS changes needed. 2. Wildcard SSL certificate — obtain a wildcard cert via DNS-01 challenge once and install it on the server. See the Wildcard domains section for instructions. The cert path used by the pipeline examples below is /etc/letsencrypt/live/preview.domain.ltd/ . 3. Repository access — the pipeline examples use an HTTPS URL with a personal access token (PAT) embedded, so no per-app SSH deploy key setup is needed. The t"},{"p":"infrastructure","c":"Infrastructure","id":"cli-php","h":"cipi php","t":"Infrastructure cipi php PHP 8.5 is pre-installed during setup. Additional versions can be added at any time; since v4.5.12 Cipi probes APT sources per Ubuntu codename — typically the ondrej/php PPA on 24.04, packages.sury.org on 26.04 when Launchpad has no suite, or Ubuntu main as a last resort (co-installable 8.3–8.5 where the chosen repo supports it). Since v4.5.4 Cipi bundles Deployer 8 , which requires PHP &ge; 8.3 . cipi php install , cipi php switch , cipi app create and cipi app edit therefore accept only 8.3, 8.4 and 8.5 . Legacy versions (7.4–8.2) remain detectable and removable, so you can still clean up a pre-4.5.4 server with cipi php remove <old-version> . bash Copy $ cipi php list # list installed versions, status, and system default $ cipi php install 8.4 # install an additional PHP version (8.3–8.5) $ cipi php switch 8.4 # set system default (root/cipi, API pool) $ cipi php remove 8.1 # remove a version, incl. legacy (blocks if default or apps use it) System default vs app-specific PHP The system default is the PHP version used by the root and cipi users, the Cipi API FPM pool, and the API queue worker. Use cipi php switch <ver> to change it. The command migrates the API pool, restarts the API worker, and sends an email notification if SMTP is configured. Each app has its own PHP version (set at app create or via cipi app edit --php= ). Deployer, Composer, crontab deploy triggers, and cipi sync import always run with the app's configured PHP — never the system default. To switch an existing app to a different version: bash Copy $ cipi app edit myapp --php=8.5 This hot-swaps the PHP version with zero downtime: updates the FPM pool, Nginx socket, Supervisor workers, crontab, Deployer config, and .env in one atomic operation."},{"p":"infrastructure","c":"Infrastructure","id":"cli-db","h":"cipi db","t":"cipi db Cipi creates a dedicated MariaDB database for each Laravel app automatically during app create . Custom apps do not get a database — use cipi db create --name=<app> if you need one (e.g. for WordPress or another CMS). After database setup, a ready-to-use mariadb+ssh:// connection URL is displayed, combining SSH credentials, server IP, and database information in a single copyable string for GUI clients like TablePlus, DBeaver, or Sequel Pro. You can also manage additional standalone databases. bash Copy $ cipi db create # interactive $ cipi db create --name=analytics # non-interactive $ cipi db list # list all databases with sizes $ cipi db backup myapp # dump to /var/log/cipi/backups/ $ cipi db restore myapp backup.sql.gz $ cipi db password myapp # regenerate database password $ cipi db delete analytics"},{"p":"infrastructure","c":"Infrastructure","id":"cli-aliases","h":"cipi alias","t":"cipi alias Add multiple domains or subdomains to any app. After adding aliases, run cipi ssl install to provision or renew the certificate with SAN coverage for all domains. bash Copy $ cipi alias add myapp www.myapp.com $ cipi alias add myapp myapp.it $ cipi alias list myapp $ cipi alias remove myapp myapp.it"},{"p":"infrastructure","c":"Infrastructure","id":"cli-domains","h":"cipi domains","t":"cipi domains Available since v4.5.5 . A top-level command that lists every domain and alias across all apps in a single table — the global counterpart to the per-app cipi alias list <app> . Ideal for auditing the whole domain-to-app mapping or spotting a domain that is still missing a certificate. bash Copy $ cipi domains # list all domains and aliases across every app Each row shows the following columns: Column Description DOMAIN The domain or alias name. Rows are sorted alphabetically by domain. APP The app that owns the domain. KIND primary or alias . TYPE Laravel or Custom . PHP The app's PHP version. DOCROOT public for Laravel apps, /<docroot> for custom apps. BRANCH The Git branch deployed for the app. LAST DEPLOY Human-relative age ( just now , 10m ago , 2h ago , 3d ago , 2w ago , 2mo ago , 2y ago ) — derived from the mtime of the app's current symlink, which Deployer atomically re-points on every successful deploy. Shows - when the app was never deployed. SSL Per-name certificate status ( ✓ / &#10007; ), detected from /etc/letsencrypt/live/<domain> . REPOSITORY The Git repository, or (SFTP only) for custom apps with no repo. A footer summarises the totals — number of domains, apps, and certificates — making it easy to audit the whole mapping at a glance."},{"p":"infrastructure","c":"Infrastructure","id":"cli-ssl","h":"cipi ssl","t":"cipi ssl Certbot manages Let's Encrypt certificates. Certificates auto-renew via a weekly cron. cipi ssl status shows expiry dates with color-coded warnings: green (>30 days), yellow (14–30 days), red (<14 days). bash Copy $ cipi ssl install myapp # provision / renew — includes all aliases (SAN) $ cipi ssl renew # force renewal of all certificates $ cipi ssl status # show all certs with expiry dates After adding domain aliases with cipi alias add , always run cipi ssl install again to provision a new SAN certificate covering all domains."},{"p":"infrastructure","c":"Infrastructure","id":"cli-backup","h":"cipi backup","t":"cipi backup Back up databases and storage to Amazon S3 or any S3-compatible provider (Hetzner Object Storage, DigitalOcean Spaces, Backblaze B2, MinIO, etc.). Setup bash Copy $ cipi backup configure # → AWS Access Key ID # → AWS Secret Access Key # → Bucket name # → Region # → Endpoint URL (leave empty for AWS; required for other providers) S3-compatible endpoints Provider Endpoint URL AWS S3 leave empty Hetzner https://<datacenter>.your-objectstorage.com DigitalOcean Spaces https://<region>.digitaloceanspaces.com Backblaze B2 https://s3.<region>.backblazeb2.com MinIO https://your-minio-host Running backups bash Copy $ cipi backup configure # configure S3 credentials $ cipi backup run # backup all apps $ cipi backup run myapp # backup a single app $ cipi backup list # list all backups $ cipi backup list myapp # list backups for one app $ cipi backup prune myapp --weeks=4 # delete backups older than 4 weeks Each backup uploads to s3://your-bucket/cipi/appname/YYYY-MM-DD_HHMMSS/ and contains: db.sql.gz — compressed database dump shared.tar.gz — the entire shared/ directory ( .env + storage/ ) Scheduling automatic backups bash Copy # Add to root crontab (crontab -e) 0 2 * * * /usr/local/bin/cipi backup run >> /var/log/cipi/backup.log 2>&1 Pruning old backups from S3 Backups accumulate over time. Use cipi backup prune to delete backup folders older than N weeks from S3. Run it as a cron job alongside the backup itself. bash Copy $ cipi backup prune myapp --weeks=4 # delete backups older than 4 weeks $ cipi backup prune myapp --weeks=2 # keep only the last 2 weeks Add both commands to the root crontab to run automatically: bash Copy # root crontab — backup at 02:00, prune at 03:00 (keep 4 weeks) 0 2 * * * /usr/local/bin/cipi backup run myapp >> /var/log/cipi/backup.log 2>&1 0 3 * * * /usr/local/bin/cipi backup prune myapp --weeks=4 >> /var/log/cipi/backup-prune.log 2>&1 cipi backup prune reads S3 credentials from /etc/cipi/backup.conf , written by cipi backup configure ."},{"p":"infrastructure","c":"Infrastructure","id":"cli-app-cron","h":"User crontab","t":"User crontab Cipi automatically adds a crontab entry for the Laravel scheduler when an app is created: bash Copy # installed automatically by cipi app create * * * * * /usr/bin/php8.5 /home/myapp/current/artisan schedule:run >> /dev/null 2>&1 This entry runs as the myapp Linux user every minute, using the PHP version selected for the app. It is updated automatically when you change PHP version via cipi app edit myapp --php=X . Viewing the current crontab bash Copy # as root — view the app user's crontab $ crontab -u myapp -l # or after switching to the app user $ su - myapp myapp@server:~$ crontab -l Adding custom cron jobs You can add extra cron jobs to the app user's crontab. Switch to the app user first to ensure jobs run with the correct user context and file permissions: bash Copy $ su - myapp myapp@server:~$ crontab -e Example entries you might add: bash # existing Laravel scheduler (do not remove) * * * * * /usr/bin/php8.5 /home/myapp/current/artisan schedule:run >> /dev/null 2>&1 # nightly database backup at 2 AM 0 2 * * * /usr/local/bin/cipi db backup myapp >> /home/myapp/logs/backup.log 2>&1 # custom script every 15 minutes */15 * * * * /home/myapp/current/scripts/sync.sh >> /home/myapp/logs/sync.log 2>&1 Do not remove the Laravel scheduler entry. Cipi does not re-add it automatically if deleted — you would need to run cipi app edit myapp --php=<current-version> to restore it. Always keep the schedule:run line as the first entry so it is easy to identify. Cron jobs run as the app user. They respect the same filesystem restrictions as the app itself. If a cron job needs to write files, make sure the target path is inside /home/myapp/ . Jobs that require root access should be added to the root crontab instead, with crontab -e as root. Checking if cron is working bash Copy # check system cron log $ grep CRON /var/log/syslog | grep myapp | tail -20 # check Laravel scheduler execution $ cipi app artisan myapp schedule:list"},{"p":"infrastructure","c":"Infrastructure","id":"cli-workers","h":"cipi worker","t":"cipi worker Every app gets a default Supervisor worker for the default queue. You can add additional queues with custom process counts and timeouts. bash Copy $ cipi worker add myapp --queue=emails --processes=3 $ cipi worker add myapp --queue=exports --processes=1 --timeout=7200 $ cipi worker list myapp $ cipi worker edit myapp --queue=default --processes=3 $ cipi worker remove myapp emails $ cipi worker restart myapp # restart all workers for the app $ cipi worker stop myapp # stop all workers for the app (used during deploys) Flag Description --queue=<name> Queue name to consume (e.g. default , emails , exports ) --processes=<n> Number of parallel worker processes --timeout=<seconds> Job timeout in seconds. Default is 60. Workers are stopped before the symlink swap and restarted after every deploy, preventing Supervisor from picking up stale artisan paths. Supervisor is configured with autorestart=unexpected so workers only restart on unexpected exits, not on graceful stops."},{"p":"infrastructure","c":"Infrastructure","id":"cli-firewall","h":"cipi firewall","t":"cipi firewall Cipi installs UFW with ports 22, 80, and 443 open by default. Use the firewall commands to manage additional rules without touching UFW directly. bash Copy $ cipi firewall allow 3306 # open a port $ cipi firewall allow 3306 --from=10.0.0.5 # allow from specific IP $ cipi firewall allow 3306 --from=10.0.0.0/24 # allow from subnet $ cipi firewall deny 8080 # block a port $ cipi firewall list # show all rules"},{"p":"infrastructure","c":"Infrastructure","id":"cli-ban","h":"cipi ban","t":"cipi ban Inspect and manage Fail2ban bans directly from the CLI. Cipi configures Fail2ban with progressive banning: a 24-hour base ban that doubles on each repeat offence up to a 7-day cap, with max retries reduced to 3. A dedicated recidive jail bans repeat offenders for 7 days after 3 bans within 24 hours. bash Copy $ cipi ban list # list all banned IPs, grouped by jail $ cipi ban unban 203.0.113.42 # unban a specific IP from all jails cipi ban list Lists every IP currently banned by Fail2ban, grouped by jail (e.g. sshd , recidive ). Useful for a quick security check or before running an unban. cipi ban unban <IP> Removes the given IP from all Fail2ban jails at once. Handy when a legitimate user or CI runner gets locked out by mistake. Existing installations are upgraded automatically by the 4.3.0 migration script when you run cipi self-update . No manual configuration is needed."},{"p":"infrastructure","c":"Infrastructure","id":"cli-services","h":"cipi service","t":"cipi service Check and control the system services that power Cipi directly from the CLI. Nginx uses a graceful reload (zero downtime) instead of a full restart. bash Copy $ cipi service list # status of all services $ cipi service list nginx # status of a specific service $ cipi service restart # restart all services $ cipi service restart nginx # graceful reload (zero downtime) $ cipi service restart php # restart all PHP-FPM versions $ cipi service start fail2ban $ cipi service stop supervisor # asks for confirmation Supported service names: nginx , mariadb , valkey-server , supervisor , fail2ban , php<ver>-fpm (e.g. php8.5-fpm ). The keyword php targets all installed PHP-FPM versions at once. For the cache backend, redis-server , redis , and valkey are accepted as aliases of valkey-server . Valkey — the BSD-licensed, Redis-compatible fork — is included in the default stack and replaces redis-server since Cipi 4.5.6. It is installed with a password, bound to localhost only, and its credentials (user, password) are saved in /etc/cipi/server.json and shown at the end of installation. valkey-server is added to the unattended-upgrades blacklist — Cipi manages it, so it is not auto-upgraded automatically. See the Valkey section for details and the automatic Redis → Valkey migration."},{"p":"infrastructure","c":"Infrastructure","id":"cli-ssh","h":"cipi ssh — SSH Key Management","t":"cipi ssh — SSH Key Management Manage the authorized SSH keys for the cipi user — the admin SSH entry point. The cipi user (group cipi-ssh ) uses public-key only; root login is disabled. App users (group cipi-apps ) connect with password — see SSH as the app user . Commands bash Copy $ cipi ssh list # list all authorized keys with fingerprint, comment, and current-session marker $ cipi ssh add [key] # add a new SSH public key (validates format, prevents duplicates) $ cipi ssh remove [n] # remove a key by number $ cipi ssh rename [n] [name] # change the display name / comment of a key Safety mechanisms cipi ssh remove includes two safeguards to prevent lockout: Current-session protection — you cannot remove the key used by your active SSH session. Last-key protection — you cannot remove the last remaining authorized key. Key comments SSH keys are stored with their original comments intact, making it easy to identify who each key belongs to. Use cipi ssh rename to change the display name of any key: bash Copy # list keys to find the number $ cipi ssh list # rename key #2 $ cipi ssh rename 2 \"john-macbook\" Email notifications When SMTP is configured, Cipi sends an email alert every time a key is added, removed, or renamed. The notification includes the server hostname, IP address, key fingerprint, key comment, timestamp, and remaining key count. Rename notifications also include the old and new key name."},{"p":"infrastructure","c":"Infrastructure","id":"cli-cipi","h":"cipi — Server & Self-Update","t":"cipi — Server & Self-Update Top-level commands for server status and Cipi self-management. bash Copy $ cipi status # CPU, RAM, disk, services, PHP versions, apps $ cipi version # show installed Cipi version $ cipi self-update # update Cipi to the latest version $ cipi self-update --check # check for updates without installing Password & credential reset Cipi provides commands to regenerate server-level passwords. New passwords are stored in /etc/cipi/server.json (encrypted via Vault) and displayed on screen. Save them immediately — they are shown only once. bash Copy $ cipi reset root-password # regenerate the root Linux user SSH password $ cipi reset db-password # regenerate the MariaDB root password $ cipi reset valkey-password # regenerate the Valkey password and restart the service cipi reset valkey-password (alias cipi reset redis-password ) restarts the Valkey service. Connected clients will be temporarily disconnected. If your apps use Valkey for cache or sessions, expect a brief interruption."},{"p":"cli-client","c":"CLI Client","id":"cli-install","h":"Installation","t":"CLI Client Installation cipi-cli is a standalone Go binary that talks to the Cipi REST API from your local machine. No SSH required — manage apps, databases, SSL certificates, and deployments from any terminal. Binaries are available for Linux and macOS (amd64 and arm64). The Cipi server must have the API package installed and configured before using cipi-cli . See cipi api for setup instructions. Download binary Download the latest release for your platform from the Releases page , then: bash Copy chmod +x cipi-cli-* sudo mv cipi-cli-* /usr/local/bin/cipi-cli Build from source bash Copy git clone https://github.com/cipi-sh/cli.git cd cli make build sudo make install Verify installation bash Copy $ cipi-cli version cipi-cli v1.0.1 (linux/amd64)"},{"p":"cli-client","c":"CLI Client","id":"cli-configure","h":"Configuration","t":"CLI Client Configuration Before using cipi-cli , connect it to your Cipi API server. Each profile maps to one server — use separate profiles for production, staging, or any other environment. You need the API endpoint URL and a Sanctum token created with cipi api token create on your server. Credentials are stored per profile in ~/.cipi/config.json with permissions 0600 . If you omit --profile , the profile name defaults to default . Interactive setup bash Copy $ cipi-cli configure --profile prod Cipi API endpoint: https://api.example.com API token: 1|yourtoken... Profile \"prod\" saved to ~/.cipi/config.json $ cipi-cli configure --profile staging Cipi API endpoint: https://staging-api.example.com API token: 1|stagingtoken... Profile \"staging\" saved to ~/.cipi/config.json Non-interactive setup bash Copy $ cipi-cli configure --profile prod \\ --endpoint https://api.example.com --token \"1|yourtoken...\" Show configuration bash Copy $ cipi-cli profiles show prod Profile: prod Endpoint: https://api.example.com Token: 1|a8Kz...4f2a $ cipi-cli profiles show # Shows all configured profiles"},{"p":"cli-client","c":"CLI Client","id":"cli-profiles","h":"profiles","t":"CLI Client profiles Manage multiple server connections from a single cipi-cli installation. Prefix any command with a profile name to target that server, or set a default profile to omit the prefix. Targeting a server bash Copy $ cipi-cli prod apps list $ cipi-cli staging apps show myapp $ cipi-cli prod deploy myapp $ cipi-cli prod ssl install myapp With a default profile set, you can run commands without the prefix: bash Copy $ cipi-cli profiles default prod Default profile set to \"prod\" $ cipi-cli apps list # Uses the prod profile Command Description cipi-cli profiles List configured profiles cipi-cli profiles list List configured profiles cipi-cli profiles show [profile] Show one profile, or all if omitted cipi-cli profiles default <profile> Set the default profile cipi-cli profiles delete <profile> [-y] Delete a profile The same profile subcommands are also available under cipi-cli configure list , configure show , configure default , and configure delete ."},{"p":"cli-client","c":"CLI Client","id":"cli-apps","h":"apps","t":"CLI Client apps Manage applications on your Cipi server. Command Description cipi-cli apps list List all applications cipi-cli apps show <name> Show application details cipi-cli apps create [flags] Create a new application cipi-cli apps edit <name> [flags] Edit an application cipi-cli apps delete <name> [-y] Delete an application cipi-cli apps suspend <name> Take an app offline (HTTP 503 maintenance page) without deleting it cipi-cli apps unsuspend <name> Restore a suspended app to normal service Create flags --user Application name (used as Linux user and database name) --domain Primary domain for the application --php PHP version (e.g. 8.5 ) --repository Git repository URL --branch Git branch to deploy from --custom Create a custom (non-Laravel) app with htdocs deploy --docroot Document root relative to htdocs (custom apps only) Edit flags --php Change PHP version --repository Update Git repository URL --branch Change deploy branch --domain Rename the primary domain (requires Cipi 4.6.2+ and API 1.9.0+ ) Examples bash Copy # List all apps $ cipi-cli apps list # Create a Laravel app $ cipi-cli apps create --user=myapp --domain=myapp.com \\ --php=8.5 --repository=git@github.com:acme/myapp.git --branch=main # Create a custom (non-Laravel) app $ cipi-cli apps create --user=landing --domain=landing.acme.com \\ --custom --docroot=dist # Edit an existing app $ cipi-cli apps edit myapp --php=8.4 # Suspend staging for maintenance $ cipi-cli apps suspend staging # Delete an app (skip confirmation) $ cipi-cli apps delete myapp -y"},{"p":"cli-client","c":"CLI Client","id":"cli-domains","h":"domains","t":"CLI Client domains List every primary domain and alias across all apps on the server in a single table — the remote counterpart to cipi domains on the server. Useful for auditing DNS coverage or spotting domains missing SSL before a certificate renewal. Command Description cipi-cli domains List every domain and alias across all apps bash Copy $ cipi-cli domains DOMAIN APP KIND TYPE PHP SSL api.myapp.com myapp alias Laravel 8.5 ✓ myapp.com myapp primary Laravel 8.5 ✓ www.myapp.com myapp alias Laravel 8.5 ✓ 3 domains · 1 app · 3 certificates The global domain map is built from GET /api/apps on the server. It requires Cipi 4.5.5+ on the server (for the underlying cipi domains data) but no minimum API package version."},{"p":"cli-client","c":"CLI Client","id":"cli-deploy","h":"deploy","t":"CLI Client deploy Trigger deployments, rollback to the previous release, or unlock a stuck deployment. Command Description cipi-cli deploy <app> Trigger a zero-downtime deployment cipi-cli deploy rollback <app> [-y] Rollback to previous release cipi-cli deploy unlock <app> Unlock a stuck deployment bash Copy $ cipi-cli deploy myapp Deploying myapp... Polling job #42... completed Deployed to release #14. Zero downtime. $ cipi-cli deploy rollback myapp Rolled back to release #13."},{"p":"cli-client","c":"CLI Client","id":"cli-ssl","h":"ssl","t":"CLI Client ssl Install Let's Encrypt certificates for your applications. Command Description cipi-cli ssl install <app> Install Let's Encrypt certificate (covers primary domain and all aliases) bash Copy $ cipi-cli ssl install myapp Certificate provisioned for myapp.com"},{"p":"cli-client","c":"CLI Client","id":"cli-aliases","h":"aliases","t":"CLI Client aliases Manage domain aliases for an application. Command Description cipi-cli aliases list <app> List all aliases for an app cipi-cli aliases add <app> <domain> Add a domain alias cipi-cli aliases remove <app> <domain> [-y] Remove a domain alias bash Copy $ cipi-cli aliases add myapp www.myapp.com Alias www.myapp.com added to myapp. $ cipi-cli aliases list myapp www.myapp.com api.myapp.com # Re-run ssl install to include new aliases in the certificate $ cipi-cli ssl install myapp"},{"p":"cli-client","c":"CLI Client","id":"cli-db","h":"db","t":"CLI Client db Manage MariaDB databases. Command Description cipi-cli db list List all databases cipi-cli db create <name> Create a database cipi-cli db delete <name> [-y] Delete a database cipi-cli db backup <name> Create a database backup cipi-cli db restore <name> [-y] Restore database from backup cipi-cli db password <name> [-y] Regenerate database password bash Copy $ cipi-cli db list NAME SIZE myapp 24.5 MB blog 8.2 MB $ cipi-cli db backup myapp Backup created: myapp_20260402_143022.sql.gz $ cipi-cli db restore myapp -y Database restored successfully."},{"p":"cli-client","c":"CLI Client","id":"cli-jobs","h":"jobs","t":"CLI Client jobs Write operations (create, edit, delete, deploy, SSL, etc.) are asynchronous on the Cipi API. The CLI automatically polls for job completion and displays a spinner while waiting. If you prefer to handle polling manually, use the jobs commands. Command Description cipi-cli jobs show <id> Show job status cipi-cli jobs wait <id> Wait for a job to complete (blocking) bash Copy $ cipi-cli jobs show 42 Job #42: deploy myapp — completed $ cipi-cli jobs wait 43 Waiting for job #43... completed"},{"p":"cli-client","c":"CLI Client","id":"cli-update","h":"update","t":"CLI Client update Update cipi-cli to the latest release from GitHub. The command downloads the matching binary for your platform, verifies its SHA-256 checksum, and replaces the running binary in place. Command Description cipi-cli update Update to the latest release cipi-cli update --force Reinstall even if already up to date bash Copy $ cipi-cli update Downloading cipi-cli v1.0.1... Checksum verified. Updated to v1.0.1. # If installed in a system path, use sudo $ sudo cipi-cli update"},{"p":"cli-client","c":"CLI Client","id":"cli-global-flags","h":"Global Flags","t":"CLI Client Global Flags These flags are available on all commands. --json Output in JSON format — useful for scripting and CI/CD pipelines --no-color Disable colored output --help Show help for any command bash Copy # JSON output for scripting $ cipi-cli apps list --json [{\"name\":\"myapp\",\"domain\":\"myapp.com\",\"php\":\"8.5\"}, ...] # Pipe to jq for filtering $ cipi-cli apps list --json | jq '.[].name' \"myapp\" \"blog\" # Help for a specific command $ cipi-cli apps create --help Releases Releases are automated via GitHub Actions. Pre-built binaries for Linux (amd64/arm64) and macOS (amd64/arm64) are published with SHA-256 checksums on the GitHub Releases page . Source code cipi-cli is open source, MIT licensed, and written in Go. Contributions welcome on GitHub ."},{"p":"cli-client","c":"CLI Client","id":"cli-requirements","h":"Requirements","t":"CLI Client Requirements The Cipi server must have the API package installed and configured before using cipi-cli : bash Copy $ cipi api <domain> $ cipi api ssl $ cipi api token create New app PHP versions must be 8.3 , 8.4 , or 8.5 (Cipi 4.5.4+). Some CLI features depend on specific server and API versions: Feature Minimum Cipi Minimum API Suspend / unsuspend ( apps suspend ) 4.5.8 1.8.1 Rename primary domain ( apps edit --domain ) 4.6.2 1.9.0 Global domain map ( domains ) 4.5.5 — (built from /api/apps )"},{"p":"advanced","c":"Advanced","id":"cipi-api","h":"cipi api","t":"Advanced cipi api Cipi can optionally enable a REST API layer on the server via cipi api <domain> . The API exposes operations on apps, aliases, databases, and SSL with Bearer token authentication and granular permissions. The package requires PHP 8.2+ and Laravel 12+ . Commands bash Copy $ cipi api <domain> # configure API at root (e.g. api.myhosting.com) $ cipi api ssl # install Let's Encrypt certificate for API domain $ cipi api token list # list tokens $ cipi api token create # create a new token (choose abilities) $ cipi api token revoke <id> # revoke a token $ cipi api status # Laravel version, queue worker status, pending jobs $ cipi api update # soft update: composer update on Laravel and API packages $ cipi api upgrade # full rebuild with rollback at /opt/cipi/api.old Token creation and granular permissions Authentication uses Sanctum . Each token can have one or more abilities that limit allowed operations: apps-view — read apps apps-create — create apps apps-edit — edit apps (PHP, repository, branch, primary domain since API 1.9.0+ / Cipi 4.6.2+ ) apps-suspend — suspend and unsuspend apps apps-basicauth — enable, disable, and inspect HTTP Basic Auth on apps (API 1.10.0+ ) apps-delete — delete apps deploy-manage — deploy, rollback, unlock ssl-manage — install and manage SSL certificates aliases-view — read aliases aliases-create — add aliases aliases-delete — remove aliases dbs-view — list databases dbs-create — create databases dbs-delete — delete databases dbs-manage — backup, restore, regenerate password mcp-access — access the MCP server REST endpoints All endpoints require the Authorization: Bearer <token> header. Write operations (create, edit, delete, deploy, rollback, unlock, SSL, alias, database) are asynchronous: they return 202 Accepted with a job_id to poll via GET /api/jobs/{id} . POST /api/apps accepts optional custom (boolean) and docroot (string) parameters for creating custom apps with classic deployment. The repository field is required f"},{"p":"advanced","c":"Advanced","id":"whmcs","h":"WHMCS Module","t":"WHMCS Module An official WHMCS provisioning module is available at github.com/cipi-sh/whmcs . It bridges the WHMCS provisioning lifecycle to the Cipi REST API — automating app creation, deletion, SSL certificates, deployments, and configuration changes for your hosting customers. No Composer dependencies; the module is a self-contained drop-in. Requirements WHMCS 8.x (provisioning module type &ldquo;Server&rdquo;) Cipi server with API enabled: cipi api <domain> and cipi api ssl Sanctum Bearer token with the required abilities: Ability Required for apps-view Test Connection, App Info apps-create Create Account apps-edit Change Package apps-suspend Suspend / Unsuspend apps-delete Terminate Account deploy-manage Deploy, Rollback, Unlock ssl-manage Install SSL, Auto-SSL Installation Copy modules/servers/cipi/ into your WHMCS root: your-whmcs/ └── modules/ └── servers/ └── cipi/ ├── cipi.php └── lib/ └── CipiApiClient.php In WHMCS Admin &rarr; System Settings &rarr; Servers &rarr; Add New Server : Type : Cipi (Laravel hosting) Hostname : API base URL (e.g. https://api.example.com , no trailing slash) Password : Bearer token from cipi api token create Secure : Yes (recommended — enables TLS verification) Create a hosting product linked to this server and configure Module Settings : Setting Description Default PHP Version 8.2 / 8.3 / 8.4 / 8.5 8.5 App Type laravel or custom laravel Git Repository (SSH) Required for Laravel; optional for custom — Git Branch Branch to deploy main Auto SSL Install Let's Encrypt after creation No App types App type Cipi equivalent Stack Git / Deploy laravel (default) cipi app create Isolated Linux user, PHP-FPM pool, Nginx vhost, MariaDB, Supervisor workers, Deployer releases SSH repository URL required ; branch from settings custom cipi app create --custom htdocs/ directory, Nginx + PHP — ideal for static sites, SPAs, WordPress, or generic PHP apps Optional : leave Git Repository empty for SFTP-only hosting, or set a repo for Git-based deploy"},{"p":"advanced","c":"Advanced","id":"cipi-sync","h":"cipi sync","t":"cipi sync Transfer, replicate, and back up entire Laravel applications between Cipi servers — including configuration, database dumps, storage files, SSH keys, workers, and crontabs. Every archive is encrypted with AES-256-CBC and protected by a user-defined passphrase, so credentials and sensitive data are safe at rest and during transfer. Commands overview bash Copy $ cipi sync export [app ...] [--with-db] [--with-storage] [--output=<path>] [--passphrase=<secret>] $ cipi sync import <archive.tar.gz.enc> [app ...] [--update] [--deploy] [--yes] [--passphrase=<secret>] $ cipi sync push [app ...] [--host=IP] [--port=22] [--with-db] [--with-storage] [--import] [--passphrase=<secret>] $ cipi sync list <archive.tar.gz.enc> [--passphrase=<secret>] $ cipi sync pubkey # display the server's sync public key for inter-server trust $ cipi sync trust # add a remote server's public key to cipi's authorized_keys Archive encryption All sync archives are encrypted by default with AES-256-CBC. During export you are prompted for a passphrase (minimum 8 characters) that protects the archive. The same passphrase is required to import or inspect it. This protects SSH keys, .env files, database dumps, and credentials at rest and during transfer. bash Copy # Interactive mode (default) — prompted for passphrase $ cipi sync export --with-db # Enter passphrase to encrypt the archive: ******** # Confirm passphrase: ******** # Non-interactive mode — for cron jobs and scripts $ cipi sync export --with-db --passphrase=\"MyStr0ngP@ss\" For automated setups, store the passphrase in a secure file and reference it in your scripts: echo \"MyStr0ngP@ss\" > /etc/cipi/.sync_passphrase && chmod 400 /etc/cipi/.sync_passphrase . Then use --passphrase=\"$(cat /etc/cipi/.sync_passphrase)\" in cron jobs. Export Packs app configs into an encrypted .tar.gz.enc archive. Optionally includes database dumps and storage files. bash Copy # Export all apps (config only) $ cipi sync export # Export three specific apps with d"},{"p":"advanced","c":"Advanced","id":"cipi-vault","h":"Vault & Encryption","t":"Vault & Encryption Cipi encrypts all configuration files at rest using AES-256-CBC . The Vault system provides transparent encryption and decryption so that sensitive data — database passwords, API tokens, SSH keys, .env contents — is never stored in plaintext on disk. Architecture The system is built on two layers: Vault — transparent encryption of JSON configuration files on disk ( server.json , apps.json , databases.json , backup.json , smtp.json , api.json ) Sync encryption — passphrase-based encryption of export archives for secure transfer between servers How Vault works A master key is generated during installation with openssl rand -base64 32 and stored at /etc/cipi/.vault_key ( chmod 400 , root-only). Every JSON config file is encrypted on disk with openssl enc -aes-256-cbc -salt -pbkdf2 . Files keep the .json extension — the content is simply an encrypted blob instead of readable JSON. The vault_read function auto-detects whether a file is plaintext or encrypted (backward compatibility), so existing servers migrate seamlessly during update. Vault functions bash Copy # Core functions in lib/vault.sh vault_init # Generate .vault_key if not present vault_read <file> # Decrypt and output JSON to stdout (auto-detect plain/encrypted) vault_write <file> # Read JSON from stdin, encrypt and write to disk vault_seal <file> # Encrypt an existing plaintext file in-place vault_get <file> <jq_query> # Shortcut: vault_read | jq Public projection Cipi generates an apps-public.json file containing only non-sensitive fields (domain, aliases, PHP version, branch, repository, user, creation timestamp). The cipi-api group reads this plaintext projection instead of the encrypted file, keeping the vault key restricted to root. Sync archive encryption When you run cipi sync export , configs are decrypted from the vault into a staging area, then the entire archive is encrypted with your passphrase. On import, the archive is decrypted with the passphrase, and configs are re-encrypt"},{"p":"advanced","c":"Advanced","id":"cipi-smtp","h":"Email Notifications","t":"Email Notifications Cipi can send email alerts when backup errors, deploy failures, system cron job failures, or security-relevant authentication events occur. SMTP configuration is stored encrypted in /etc/cipi/smtp.json and included in sync exports. Commands bash Copy $ cipi smtp configure # interactive setup (Gmail, SendGrid, Mailgun, custom) $ cipi smtp status # display current notification settings $ cipi smtp test # send a verification email $ cipi smtp enable # enable notifications $ cipi smtp disable # disable without losing settings $ cipi smtp delete # remove SMTP configuration entirely Automatic alerts Once configured, Cipi sends email notifications on: Backup errors (S3 upload failures, dump errors) Deploy failures (Deployer errors, rollback triggers) System cron job failures (via the cipi-cron-notify wrapper) App lifecycle events — notifies when an app is created, edited, or deleted, including server hostname, app name, domain, and PHP version Sudo and su elevation — notifies when any user successfully elevates via sudo or su , including who ran it, target user (for su ), SSH key, client IP, and TTY Privileged SSH login — notifies when root or any sudoer logs in via SSH, including source IP, SSH key fingerprint, and key comment SSH key changes — notifies when an SSH key is added to, removed from, or renamed on the cipi user, including hostname, IP, fingerprint, key comment, timestamp, and remaining key count. Rename alerts also include the old and new key name. Every email notification includes a footer with the client IP ( SSH_CLIENT ) and the SSH key name used to authenticate, when applicable. The key name is resolved via SSH_USER_AUTH with an auth.log fallback when needed. Security auth notifications Cipi integrates PAM-based authentication notifications via pam_exec.so with ExposeAuthInfo enabled. When SMTP is configured, the system automatically sends email alerts on these security-relevant events: Sudo and su elevation — triggered when any user su"},{"p":"advanced","c":"Advanced","id":"cipi-log-retention","h":"Log Retention (GDPR)","t":"Log Retention (GDPR) Cipi enforces automatic log rotation policies designed to meet GDPR and general data protection requirements. Logs are rotated and deleted automatically — no manual cleanup needed. Category Logs Retention Application Laravel, PHP-FPM, workers, deploy, system 12 months Security Fail2ban, UFW firewall, authentication, Cipi events ( events.log ) 12 months HTTP / Navigation Nginx access and error logs 90 days HTTP/navigation logs (nginx access logs) contain IP addresses, which are personal data under GDPR. The 90-day retention ensures compliance with the data minimization principle while preserving enough history for debugging and security analysis. Application and security logs are retained for 12 months to support audit trails and incident investigation."},{"p":"advanced","c":"Advanced","id":"valkey","h":"Valkey","t":"Valkey Valkey is the in-memory data store that Cipi installs as part of the default stack. Since v4.5.6 Cipi provisions Valkey instead of redis-server . It excels at caching, session storage, message queues, real-time broadcasting, and rate limiting. Why Valkey instead of Redis Valkey is the truly open-source, BSD-licensed fork of Redis, stewarded by the Linux Foundation . It was created in 2024 after Redis Inc. relicensed Redis away from the permissive BSD license to the source-available SSPL / RSALv2 — a change that no longer met the open-source definition. Backed by AWS, Google Cloud, Oracle, and a large community, Valkey continues the same battle-tested codebase under a license that stays free forever . That makes it a perfect fit for Cipi's MIT, no-vendor-lock-in philosophy, and it ships natively in Ubuntu 24.04's Universe repository (packages valkey-server + valkey-tools ) — no third-party PPA to trust. Just as importantly, Valkey is a drop-in replacement : it speaks the exact same RESP protocol on the same port ( 127.0.0.1:6379 ), honours the same requirepass / bind directives, and reads the same RDB/AOF data format. Your apps need zero changes — the phpredis extension and your existing REDIS_* .env values keep working exactly as before. How Cipi implements it Install — setup.sh installs and configures Valkey ( /etc/valkey/valkey.conf , service valkey-server ), bound to localhost only and protected with a password. Service management — cipi service … manages valkey-server (the names redis-server , redis , and valkey are still accepted as aliases). It is added to the unattended-upgrades blacklist, so Cipi manages it instead of an automatic upgrade. Credentials — stored under valkey_user / valkey_password in /etc/cipi/server.json (the legacy redis_* keys are still read as a fallback). Host: 127.0.0.1, Port: 6379. Password reset — cipi reset valkey-password regenerates the password and restarts the service ( cipi reset redis-password stays as an alias). Migratin"},{"p":"advanced","c":"Advanced","id":"self-update","h":"Self-Update","t":"Self-Update Cipi can update itself from GitHub without affecting any app, database, or configuration. bash Copy $ cipi self-update --check # check for a new version $ cipi self-update # update to latest Update process Downloads the latest version from GitHub Backs up the current installation to /opt/cipi.bak.YYYYMMDDHHMMSS/ Replaces CLI and lib scripts Runs any pending migration scripts in order (e.g. new Nginx directives, new packages) Updates the version file Migration scripts live in lib/migrations/ and are named by version (e.g. 4.1.0.sh ). When updating from v4.0.0 to v4.2.0, Cipi automatically runs 4.1.0.sh and 4.2.0.sh in order. Your apps, databases, and configurations are never touched. For example, the 4.5.5 migration retro-fits existing apps with the new ll='ls -al' shell alias: it appends the alias to each app's ~/.bashrc once (only when missing, preserving ownership), so apps created before 4.5.5 get it on the next cipi self-update ."},{"p":"advanced","c":"Advanced","id":"wildcard-domains","h":"Wildcard domains","t":"Wildcard domains Cipi does not support wildcard domains ( *.myapp.com ) natively. The block is twofold and architectural — not a configuration detail. Why wildcards are not supported 1 — Domain validation rejects * Every domain passed to cipi alias add (and cipi app create ) is validated against a strict regex that requires the string to start with [a-zA-Z0-9] . The asterisk fails immediately, before nginx or certbot are ever touched. 2 — Certbot uses HTTP-01 challenge, which cannot issue wildcard certs cipi ssl install calls certbot --nginx , which relies on the HTTP-01 (or TLS-ALPN-01) challenge — placing a verification file on disk and serving it over port 80. Let's Encrypt only issues wildcard certificates via the DNS-01 challenge , which requires programmatic access to your DNS provider's API. Cipi does not integrate with any DNS provider, so even if the validation were bypassed, certbot would refuse to issue the wildcard cert. Recommended alternative — Multi-SAN certificate If your subdomains are fixed and enumerable (e.g. api , admin , www , staging ), the correct approach is to add each one as an explicit alias and let Cipi issue a single SAN certificate covering all of them: bash Copy $ cipi alias add myapp api.myapp.com $ cipi alias add myapp admin.myapp.com $ cipi alias add myapp www.myapp.com $ cipi ssl install myapp # single cert, SAN covers all domains Certbot's --expand flag (used internally by Cipi) adds the new SANs to the existing certificate without issuing a new one. The SAN list has no meaningful limit for typical use. Manual wildcard certificate (outside Cipi) If you need dynamic subdomains (e.g. <tenant>.saas.com ), you can obtain a wildcard certificate manually using a DNS plugin for certbot and place it on the server. Cipi will not manage, renew, or track it — you own the lifecycle entirely. bash Copy # example with the Cloudflare DNS plugin $ pip install certbot-dns-cloudflare $ certbot certonly --dns-cloudflare \\ --dns-cloudflare-credentia"},{"p":"advanced","c":"Advanced","id":"nginx-config","h":"Edit Nginx config","t":"Edit Nginx config To customise the Nginx vhost for an app, edit the site config directly. After changes, test and reload Nginx. bash Copy $ sudo nano /etc/nginx/sites-available/<app> $ sudo nginx -t && sudo systemctl reload nginx"},{"p":"advanced","c":"Advanced","id":"uninstall","h":"Uninstall Cipi","t":"Uninstall Cipi Cipi does not provide a built-in uninstall command. If you need to completely remove Cipi from a server, follow the steps below in order . This procedure removes every component that Cipi installs — users, services, packages, configs, and data. This is a destructive and irreversible operation. All apps, databases, SSL certificates, and server configurations managed by Cipi will be permanently deleted. Back up everything you need before proceeding. After uninstalling, the recommended approach is to reprovision the server from scratch. 1 — Stop and remove all apps For each app managed by Cipi, remove its system user, home directory, database, nginx vhost, PHP-FPM pool, and supervisor config. bash Copy # List all app users (members of cipi-apps group) $ grep cipi-apps /etc/group # For EACH app user, remove everything $ supervisorctl stop <app_user>:* $ rm -f /etc/supervisor/conf.d/<app_user>.conf $ rm -f /etc/nginx/sites-enabled/<app_user> $ rm -f /etc/nginx/sites-available/<app_user> $ rm -f /etc/php/*/fpm/pool.d/<app_user>.conf $ rm -f /etc/sudoers.d/cipi-<app_user> $ mysql -e \"DROP DATABASE IF EXISTS <app_user>; DROP USER IF EXISTS '<app_user>'@'localhost'; DROP USER IF EXISTS '<app_user>'@'127.0.0.1';\" $ userdel -r <app_user> 2 — Remove the Cipi user and groups bash Copy $ userdel -r cipi $ groupdel cipi-ssh 2>/dev/null $ groupdel cipi-apps 2>/dev/null 3 — Remove Cipi binaries, libraries, and data bash Copy $ rm -f /usr/local/bin/cipi $ rm -f /usr/local/bin/cipi-worker $ rm -f /usr/local/bin/cipi-cron-notify $ rm -f /usr/local/bin/cipi-auth-notify $ rm -rf /opt/cipi $ rm -rf /etc/cipi $ rm -rf /var/log/cipi 4 — Remove Cipi API (if installed) bash Copy $ systemctl stop cipi-queue 2>/dev/null $ systemctl disable cipi-queue 2>/dev/null $ rm -f /etc/systemd/system/cipi-queue.service $ systemctl daemon-reload 5 — Remove Cipi cron jobs bash Copy # Edit root crontab and remove all Cipi entries $ crontab -e # Remove lines referencing: cipi self-update, certb"},{"p":"about","c":"About Cipi","id":"history","h":"History","t":"About Cipi History Cipi was created by Andrea Pollastri , a software developer since 2005 specialising in Laravel, system administration, and cybersecurity — with experience at companies like Docebo and Musement. He built Cipi out of a practical need: a fast, scriptable way to provision and deploy Laravel applications on his own VPS, without handing control to a SaaS panel. What started as a private tool became an open-source project that grew to over 1,000 GitHub stars and hundreds of active deployments worldwide. Here is a brief account of how it evolved across six years and four major versions. v0.1 June 2019 The idea A collection of shell scripts to automate the tedious parts of setting up a Laravel server on a fresh Ubuntu VPS — Nginx, PHP, MariaDB, Supervisor. No web UI, no package. Just bash. v1.x June 2019 First release — Laravel panel The shell scripts were wrapped in a Laravel web application acting as a server control panel. Users could create apps, manage deployments, and configure Nginx through a browser UI hosted on the same server. The project was published on GitHub and quickly attracted interest from the Laravel community. v2.x May 2020 Feature growth A year of rapid iteration added SMTP configuration, local database backups, PHP-FPM permission fixes, server service management, and root password reset. The v2 series reached 2.4.9 across dozens of patch releases, establishing Cipi as a stable option for small-team Laravel hosting. v3.0 March 2021 The big leap — API, PHP 8, real-time UI Built on Laravel 8 , v3 introduced a fully documented REST API (Swagger / OA), PHP 8 support, real-time CPU/RAM charts, a Cronjob editor, Supervisor management, a GitHub repository manager, Node 15, Composer 2, and JWT authentication. Cipi could now manage the same server it ran on. The project reached 1k GitHub stars. v3.1 December 2021 The last web UI — and a question PHP 8.1 became the default version. Node was upgraded to v16, Certbot was refreshed, and domain alia"},{"p":"about","c":"About Cipi","id":"security-model","h":"Security Model","t":"Security Model SSH hardening During installation, Cipi creates groups cipi-ssh and cipi-apps and applies the following SSH architecture: User Access Method root blocked PermitRootLogin no cipi key-only group cipi-ssh , PasswordAuthentication no globally app users user + password group cipi-apps , Match Group cipi-apps → PasswordAuthentication yes sshd_config uses AllowGroups cipi-ssh cipi-apps instead of AllowUsers . App users can connect with ssh myapp@server-ip and the password generated at app creation. Admin access is via public-key as cipi . SSH keys are managed with cipi ssh list / add / remove — see SSH Key Management . SSH key management & notifications Authorized keys for the cipi user can be managed via the CLI with built-in safety: format validation, duplicate prevention, current-session protection, and last-key protection to avoid lockout. When SMTP is configured, every key addition, removal, or rename triggers an email alert with hostname, IP, fingerprint, timestamp, and remaining key count. Rename alerts also include the old and new key name. Privilege escalation prevention Application users are restricted from using su to escalate privileges to root or the cipi account. This is enforced via pam_wheel.so group=sudo , ensuring that only members of the sudo group can switch users. Sudoers hardening The www-data user (used by Nginx and PHP-FPM) has its sudoers access restricted to an explicit command whitelist instead of a wildcard pattern. Only the specific /usr/local/bin/cipi subcommands required by the API are allowed. This prevents www-data from running arbitrary commands even if the web application is compromised. API command whitelist The Cipi API validates every CLI command against an internal whitelist before executing it via sudo . Commands not on the whitelist are rejected, preventing command injection through the API layer. User isolation Each app runs under its own Linux user with chmod 750 on the home directory. No app can read another app's "},{"p":"about","c":"About Cipi","id":"why-mariadb","h":"Why MariaDB?","t":"Why MariaDB? Cipi uses MariaDB instead of MySQL for several reasons: Drop-in replacement — Laravel doesn't notice the difference. Same PDO driver, same SQL syntax, same migration files. Better performance — more advanced query optimizer and native thread pool in the community edition. Clean licensing — pure GPL, no Oracle involvement. Native on Ubuntu — installs from the default Ubuntu repositories without external PPAs. Auto-tuned — Cipi configures innodb_buffer_pool_size based on your server's RAM (256M for 1 GB RAM, up to 4 GB for 16 GB+ servers)."},{"p":"about","c":"About Cipi","id":"why-valkey","h":"Why Valkey?","t":"Why Valkey? Cipi uses Valkey instead of Redis for several reasons: Drop-in replacement — same RESP protocol on port 6379 , same phpredis extension, same REDIS_* .env keys. Laravel doesn't notice the difference. Truly open source — BSD-licensed fork stewarded by the Linux Foundation, created after Redis Inc. moved to the source-available SSPL / RSALv2 in 2024. Native on Ubuntu — ships in Ubuntu 24.04's Universe repository ( valkey-server + valkey-tools ) without third-party PPAs. No vendor lock-in — aligns with Cipi's MIT, self-hosted philosophy: free forever, backed by AWS, Google Cloud, Oracle, and a large community. Auto-migrated — existing servers switch from Redis to Valkey automatically on cipi self-update , reusing the same password and dataset with zero app changes."},{"p":"about","c":"About Cipi","id":"why-laravel","h":"Why Laravel?","t":"Why Laravel? When v4 was designed, one of the clearest decisions was to drop generic PHP and WordPress support entirely and go all-in on a single framework. That framework was Laravel — and the reasoning goes well beyond personal preference. Speed of development Laravel gives developers an extraordinary head start. Authentication, queues, scheduled tasks, file storage, email, notifications, API resources, database migrations — all of it is built in, consistent, and documented. A team can go from an empty project to a production-ready application in a fraction of the time it would take with a lower-level stack. For Cipi, this means that every deployment assumption — directory layout, .env handling, queue workers, the scheduler — maps cleanly to a known structure. There is no guesswork. A thriving open-source community Laravel has one of the most active and welcoming communities in the PHP world. Packages are published, maintained, and discussed daily on GitHub, Laracasts, Discord, and X. When you hire a Laravel developer, you get someone who already knows the conventions, the tooling, and the ecosystem. When you publish a Laravel package, it reaches tens of thousands of developers overnight. That network effect is genuinely valuable — and it is one of the reasons Cipi's companion package, cipi-agent , works so well: drop it into a Laravel app and it just fits. Security by default Laravel takes security seriously out of the box. CSRF protection, SQL injection prevention through the query builder, XSS mitigation via Blade's auto-escaping, rate limiting, signed URLs, encrypted cookies — these are not add-ons you wire together, they are the defaults. The framework's release cycle includes prompt security patches, and the team publishes clear upgrade guides. For production applications that handle real users and real data, that reliability matters enormously. Longevity and support Every major Laravel version receives bug fixes for 18 months and security fixes for two year"},{"p":"about","c":"About Cipi","id":"why-cipi","h":"Why \"ci-pi\"?","t":"Why \"ci-pi\"? Cipi is the Italian reading of the letters C and P — which stand for Control Panel . It is a quiet nod to the project's Italian roots and to the fact that, underneath all the CLI commands, Cipi is doing exactly what a control panel does: managing Nginx, PHP-FPM, MariaDB, Supervisor, Certbot, and UFW on your behalf — just without the browser tab. The name also carries a deliberate lightness. Server management tools tend to take themselves very seriously. Cipi does not. It is a tool built by a developer for developers, with the goal of getting out of the way as quickly as possible. The mascot The Cipi mascot is a penguin — an intentional reference to Tux , the official Linux mascot created by Larry Ewing in 1996. Where Tux is polished and iconic, the Cipi penguin is drawn in a minimal, hand-sketched line style: same species, different attitude. Think of it as Tux's quieter sibling who prefers the terminal over the spotlight. The illustration is rendered as a pure SVG using the project's accent color, so it adapts naturally to light and dark mode and scales to any size without losing quality."},{"p":"about","c":"About Cipi","id":"vs-alternatives","h":"Cipi vs alternatives","t":"Cipi vs alternatives There are many tools for provisioning and managing web servers. This page explains where Cipi sits in that landscape and why you might — or might not — choose it over each category. The comparison is focused on seven axes: AI integration (dual MCP server — per-app and global) , native Git deploy (GitHub & GitLab) , REST API , CLI-first automation , encryption at rest (Vault) , GDPR compliance , and total cost of ownership . Looking for a specific head-to-head? See the dedicated comparison pages: Laravel Forge alternative , Laravel Cloud alternative , Ploi alternative , RunCloud alternative , ServerPilot alternative , CloudPanel alternative , Vito Deploy alternative , and cPanel alternative , and Plesk alternative — or browse them all on the alternatives overview . Cipi's core proposition: free forever, MIT open source, Laravel-first (with custom app support), CLI-first, AI-ready with dual MCP server, native GitHub & GitLab integration, full REST API, AES-256-CBC encryption at rest, GDPR-compliant log retention, self-contained on your VPS . It does one thing and does it well — it does not try to be a generic hosting panel, a SaaS platform, or a multi-stack tool. In addition to Laravel, Cipi supports custom apps (static sites, SPAs, WordPress, alternative PHP frameworks, and other non-Laravel projects). Cipi provides automatic deployment integration with GitHub and GitLab, a complete REST API for managing hosts and applications programmatically, a dual MCP server architecture for AI-driven management, built-in Vault encryption for all configuration files and sync archives, and GDPR-compliant automatic log rotation. No credentials, SSH keys, or sensitive data are ever stored in plaintext — and no infrastructure data ever leaves your VPS. Tool AI ready (MCP) CLI / automation Free / OSS No SaaS dep. Cipi Laravel Cloud partial usage-based Laravel Forge partial $12–19/mo Ploi partial €8–30/mo moss.sh limited free / $9–49/mo RunCloud $12–18/mo CloudPane"}];
