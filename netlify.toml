# Netlify — https://cipi.sh
# Edge i18n/legacy redirects live in netlify/edge-functions/i18n.js

[build]
  publish = "."
  # No build step — static site

[[edge_functions]]
  function = "i18n"
  path = "/*"
