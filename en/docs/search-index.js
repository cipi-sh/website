window.CIPI_DOCS = [{"p":"about","c":"About Cipi","id":"history","h":"The History of Cipi","t":"About Cipi The History of Cipi About Cipi The History of Cipi Cipi was created by Andrea Pollastri , a software developer since 2005 specialising in Laravel, system administration, and cybersecurity — with experience at companies like Docebo and Musement. He built Cipi out of a practical need: a fast, scriptable way to provision and deploy Laravel applications on his own VPS, without handing control to a SaaS panel. What started as a private tool became an open-source project that grew to over 1,000 GitHub stars and hundreds of active deployments worldwide. Here is a brief account of how it evolved across six years and five major versions. v0.1 June 2019 The idea A collection of shell scripts to automate the tedious parts of setting up a Laravel server on a fresh Ubuntu VPS — Nginx, PHP, MariaDB, Supervisor. No web UI, no package. Just bash. v1.x June 2019 First release — Laravel panel The shell scripts were wrapped in a Laravel web application acting as a server control panel. Users could create apps, manage deployments, and configure Nginx through a browser UI hosted on the same server. The project was published on GitHub and quickly attracted interest from the Laravel community. v2.x May 2020 Feature growth A year of rapid iteration added SMTP configuration, local database backups, PHP-FPM permission fixes, server service management, and root password reset. The v2 series reached 2.4.9 across dozens of patch releases, establishing Cipi as a stable option for small-team Laravel hosting. v3.0 March 2021 The big leap — API, PHP 8, real-time UI Built on Laravel 8 , v3 introduced a fully documented REST API (Swagger / OA), PHP 8 support, real-time CPU/RAM charts, a Cronjob editor, Supervisor management, a GitHub repository manager, Node 15, Composer 2, and JWT authentication. Cipi could now manage the same server it ran on. The project reached 1k GitHub stars. v3.1 December 2021 The last web UI — and a question PHP 8.1 became the default version. Node was upgraded to v16, Certbot was refreshed, and domain alias handling was fixed. The v3.1.x series was the most polished release of the web-UI era and many teams kept it running in production for years. It was also the version that prompted a harder question: was a browser-based control panel still the right interface? Modern development workflows had moved toward SSH, CI/CD pipelines, GitOps, and — increasingly — AI agents that could orchestrate infrastructure through shell commands. A web UI required authentication, a running Laravel process, and a database just to issue a deploy. A CLI needed none of that. The answer shaped everything that came next. v4.x March 2026 The rewrite — CLI-first, Laravel-exclusive After years of maintaining a full Laravel web application as the control plane, v4 made the boldest decision yet: drop the web UI entirely . Cipi became a pure CLI tool operated over SSH. The scope also narrowed from generic PHP to Laravel exclusively , allowing every part of the stack to be optimised for one framework. MySQL was replaced by MariaDB 11.4 , git pull was replaced by Deployer zero-downtime releases , shared deploy keys became per-app ed25519 keys, and S3 automated backups and native webhook support for GitHub and GitLab were added from day one. v4 also introduced a complete REST API for programmatic management of hosts and applications, native integration with GitHub and GitLab git providers, a groundbreaking dual MCP server architecture — one per-app and one global — enabling full AI-driven infrastructure management directly from any MCP-compatible IDE or AI agent, encrypted server sync for migrating apps between VPS instances, and a built-in database anonymizer for GDPR-safe data exports. The full v4 changelog is available on GitHub Releases . Notable late-v4: 4.8.1 — cipi self-update no longer stuck on Migration 4.7.16 ( sed delimiter clash with || true ); upgrades from versions below 4.7.16 (e.g. 4.7.14 → 4.8.x) complete again. 4.8.0 — multi-engine database"},{"p":"about","c":"About Cipi","id":"security-model","h":"Security Model","t":"About Cipi Security Model Security Model SSH hardening During installation, Cipi creates groups cipi-ssh and cipi-apps and applies the following SSH architecture: User Access Method root blocked PermitRootLogin no cipi key-only group cipi-ssh , PasswordAuthentication no globally app users user + password group cipi-apps , Match Group cipi-apps → PasswordAuthentication yes sshd_config uses AllowGroups cipi-ssh cipi-apps instead of AllowUsers . App users can connect with ssh myapp@server-ip and the password generated at app creation. Admin access is via public-key as cipi . SSH keys are managed with cipi ssh list / add / remove — see SSH Key Management . SSH key management & notifications Authorized keys for the cipi user can be managed via the CLI with built-in safety: format validation, duplicate prevention, current-session protection, and last-key protection to avoid lockout. When SMTP is configured, every key addition, removal, or rename triggers an email alert with hostname, IP, fingerprint, timestamp, and remaining key count. Rename alerts also include the old and new key name. Privilege escalation prevention Application users are restricted from using su to escalate privileges to root or the cipi account. This is enforced via pam_wheel.so group=sudo , ensuring that only members of the sudo group can switch users. Sudoers hardening The www-data user (used by Nginx and PHP-FPM) has its sudoers access restricted to an explicit command whitelist instead of a wildcard pattern. Only the specific /usr/local/bin/cipi subcommands required by the API are allowed. This prevents www-data from running arbitrary commands even if the web application is compromised. API command whitelist The Cipi API validates every CLI command against an internal whitelist before executing it via sudo . Commands not on the whitelist are rejected, preventing command injection through the API layer. User isolation Each app runs under its own Linux user with chmod 750 on the home directory. No app can read another app's files. PHP-FPM runs each app's pool as the app user with its own Unix socket. PHP open_basedir open_basedir is configured per FPM pool to restrict PHP to the app's home directory. Even if an app is compromised, PHP cannot access the filesystem outside its own home. Database isolation Each app has its own database and user (MariaDB by default; optional PostgreSQL since v4.8.0) with privileges restricted to that database only. A compromised app cannot read or write to another app's database. Per-app SSH deploy keys Each app gets its own ed25519 SSH key pair. A compromised deploy key only affects one repository — not all apps on the server. Webhook security GitHub webhooks are verified using HMAC-SHA256 signatures. GitLab webhooks use token comparison. The webhook handler in cipi-agent returns 200 immediately and writes a flag file — Deployer runs separately as the app user with no elevated privileges. Configuration encryption (Vault) All Cipi configuration files — server.json , apps.json , databases.json , backup.json , smtp.json , api.json — are encrypted at rest using AES-256-CBC via the built-in Vault system. A per-server master key ( /etc/cipi/.vault_key , chmod 400 ) ensures that even if an attacker gains read access to the filesystem, configuration files containing database passwords, API tokens, and credentials are unreadable without root privileges. See Vault & Encryption for the full architecture. Encrypted sync archives When transferring applications between servers via cipi sync , the entire archive — including .env files, SSH keys, database dumps, and configuration — is encrypted with AES-256-CBC using a user-provided passphrase. Archives use the .tar.gz.enc extension and cannot be read without the passphrase. This protects sensitive data both at rest (on disk) and in transit (during rsync/scp transfer). See Sync for details. GDPR-compliant log retention Cipi enforces automatic log rotation: application and security logs are retai"},{"p":"about","c":"About Cipi","id":"why-not-local","h":"Why not local?","t":"About Cipi Why not local? Why not local? Cipi is a production server provisioner . It is designed to turn a fresh Ubuntu VPS into a live, internet-facing Laravel host — not to run on your laptop, inside a local Docker container, or behind a home NAT. This is explicit in the requirements and is not a bug or missing feature. Cipi needs a server that is: Reachable from the public internet — with a routable public IPv4 address, not hidden behind NAT (home router, corporate firewall, or NAT-only cloud tiers) Running on real hardware or full KVM — not on shared-kernel virtualization (OpenVZ, LXC) or desktop container runtimes that emulate a Linux VM on your machine Accessible on ports 22, 80, and 443 — Let's Encrypt HTTP validation, Nginx vhosts, and SSH deploy workflows all assume inbound traffic can reach the host Not compatible with local dockerization Cipi is not compatible with local dockerized environments. Tools like Docker Desktop, OrbStack, Colima, and similar runtimes give you a Linux VM behind NAT on your Mac or Windows machine — no public IP, no inbound routing from the internet, and often a shared kernel under the hood. The Cipi installer may finish, but the result is not a production server: SSL will not work — Certbot cannot complete Let's Encrypt validation when the host is not reachable on port 80 from the internet Domains will not resolve — there is no public endpoint for DNS to point to Deploy and webhook flows break — Git providers, CI runners, and external services cannot reach a NAT-ted local VM Do not install Cipi inside Docker, OrbStack, WSL, or a local VM expecting a working production stack. Use a real VPS — even a €4/month instance is enough for development and staging. Industry-standard constraint No production server provisioner supports NAT-ted local environments. Commercial panels and open-source tools alike require a public, internet-accessible host — this is how SSL, domain routing, and remote deploy pipelines work. Cipi follows the same rule. The difference is what Cipi does not ask for: no proprietary agent daemon, no dedicated outbound port to an external control panel, and no mandatory cloud account. Cipi runs entirely on your server — you SSH in and use the CLI. The public-internet requirement is about network topology, not about tying you to a SaaS vendor. What to use locally instead For local Laravel development, use Laravel Sail , Laravel Herd , or a plain composer install with php artisan serve . Provision a cheap VPS with Cipi when you need a real staging or production environment."},{"p":"about","c":"About Cipi","id":"why-mariadb","h":"Why MariaDB?","t":"About Cipi Why MariaDB? Why MariaDB? MariaDB is the default database engine on every Cipi server (port 3306 ). Since v4.8.0 you can also install optional PostgreSQL ( cipi db install pgsql , port 5432 ) and pick an engine per app or database — see cipi db and Why PostgreSQL? . MariaDB remains the right default for most Laravel apps: familiar MySQL-compatible SQL, strong out-of-the-box performance, and a license model that stays simple for self-hosted production. Performance that matters in production MariaDB is built for high-concurrency web workloads — exactly the pattern Laravel apps generate (many short queries, connection churn from PHP-FPM, mixed reads and writes). Advanced query optimizer — better join planning and cost-based decisions than classic MySQL for typical application queries, which often means fewer full scans and lower latency under load. Native thread pool — available in the community edition, so the server handles many concurrent connections more efficiently than a one-thread-per-connection model when PHP-FPM spikes. InnoDB tuned for your VPS — Cipi sets innodb_buffer_pool_size from server RAM (256M on 1 GB hosts, up to 4 GB on 16 GB+ machines), so hot data stays in memory without starving Nginx or PHP. Lean default for Laravel — most apps need a fast relational store, not exotic extensions. MariaDB delivers that with low operational overhead on a single Ubuntu box. Fewer licensing headaches Choosing MariaDB avoids the ambiguity that surrounds Oracle MySQL in commercial and redistribution scenarios — especially when you ship products, host client apps, or run infra for a company. Pure GPL community server — MariaDB Server is GPL; there is no dual-license enterprise fork of the core engine that you must navigate just to stay compliant. No Oracle MySQL CLA / dual-license maze — you are not depending on Oracle's MySQL product line, trademark policy, or commercial editions for features that “should” be free. Predictable for SaaS and agencies — self-hosting MariaDB on your VPS keeps database licensing simple: you run open-source software on hardware you control, without MySQL Enterprise feature gates or surprise license reviews. Aligned with Cipi's MIT philosophy — open stack, no vendor lock-in, no paid database SKU required for production performance features like the thread pool. MySQL compatibility without the baggage Drop-in for Laravel — use the mysql driver, the same DB_* env keys, and the same migration files. Eloquent, queues, and most packages just work. Familiar tooling — TablePlus, DBeaver, Sequel Ace, and mariadb / mysql CLI clients connect the same way; Cipi still exposes a ready mariadb+ssh:// URL after install. Native on Ubuntu — installs from MariaDB.org / Ubuntu packaging without inventing a custom database stack; Cipi wires users, backups, and vault credentials on top. Battle-tested for the LAMP/LEMP world — decades of MySQL-compatible applications, docs, and hiring familiarity — with an engine that stays community-first."},{"p":"about","c":"About Cipi","id":"why-postgresql","h":"Why PostgreSQL?","t":"About Cipi Why PostgreSQL? Why PostgreSQL? Since v4.8.0 , PostgreSQL is a first-class optional engine on Cipi (port 5432 ). Install it with cipi db install pgsql , then create apps or databases with --engine=pgsql — see cipi db . Keep MariaDB as the default for classic Laravel MySQL workloads, or choose Postgres when you need richer SQL, stronger concurrency guarantees, or a stack that grows toward analytics and AI. Functionality beyond “just tables” Rich SQL and data types — JSON/JSONB, arrays, ranges, UUID, full-text search, and expressive constraints (CHECK, exclusion, deferred FKs) that map cleanly to complex domain models. Powerful indexing — B-tree, GIN, GiST, BRIN, partial and expression indexes — so you can index JSON paths, search documents, or speed up time-series-style queries without a second datastore. Extensions ecosystem — pgcrypto , uuid-ossp , pg_trgm , PostGIS, and many others turn Postgres into a platform, not only a SQL file. First-class Laravel support — native pgsql driver, migrations, Eloquent, and a huge ecosystem of packages that assume Postgres features when you need them. Scalability and concurrency MVCC done right — readers do not block writers; long-running reports and busy write traffic coexist more gracefully than on lock-heavy setups. Reliable under mixed load — strong consistency, transactional DDL in many cases, and a planner that holds up as schemas and query shapes get more sophisticated. Room to grow on one VPS — then beyond — excellent vertical scale for a single-node Cipi server; when you outgrow the box, the same engine is what managed Postgres, replicas, and logical replication ecosystems are built around. Operational clarity — mature monitoring views ( pg_stat_* ), explain plans, and tooling that senior teams already know how to tune. Compatibility and portability Industry standard — Postgres is the default answer for many SaaS backends, analytics pipelines, and cloud “managed database” offerings. Skills and runbooks transfer. Standards-leaning SQL — closer to ANSI SQL than MySQL dialects, which reduces surprise when you share queries across tools, ORMs, and warehouses. Works with the wider data stack — BI tools, ORMs, ETL, and CDC systems almost always speak Postgres; your Cipi-hosted DB is not a dead end. Side-by-side with MariaDB on Cipi — both engines can run on the same server (3306 + 5432). Pick per app: WordPress-style or classic Laravel MySQL on MariaDB; product, multi-tenant, or JSON-heavy apps on Postgres. Ready for AI and modern data workloads Vectors and embeddings — with extensions such as pgvector , you can store embeddings next to relational data for semantic search, RAG, and recommendation features without bolting on a separate vector database on day one. JSONB as a flexible document layer — keep structured columns for invariants and JSONB for evolving AI metadata, model outputs, or tool payloads — queryable and indexable. Full-text + similarity building blocks — combine FTS, trigram similarity, and vectors for hybrid retrieval (keyword + semantic) inside one transactional system. Analytics-friendly — window functions, CTEs, materialized views, and robust aggregation make Postgres a solid home for product metrics and feature stores before you need a warehouse. When to choose PostgreSQL on Cipi You need advanced constraints, JSONB, or Postgres-specific extensions You care about concurrent writers, complex reporting, or stricter SQL semantics You are building AI features (embeddings, hybrid search) or expect to Your team or upstream packages already standardize on pgsql MariaDB stays the zero-friction default. PostgreSQL is there when the product needs a more capable relational engine — still managed through the same cipi db workflow for create, list, backup, restore, and password rotation."},{"p":"about","c":"About Cipi","id":"why-valkey","h":"Why Valkey?","t":"About Cipi Why Valkey? Why Valkey? Cipi uses Valkey instead of Redis for several reasons: Drop-in replacement — same RESP protocol on port 6379 , same phpredis extension, same REDIS_* .env keys. Laravel doesn't notice the difference. Truly open source — BSD-licensed fork stewarded by the Linux Foundation, created after Redis Inc. moved to the source-available SSPL / RSALv2 in 2024. Native on Ubuntu — ships in Ubuntu 24.04's Universe repository ( valkey-server + valkey-tools ) without third-party PPAs. No vendor lock-in — aligns with Cipi's MIT, self-hosted philosophy: free forever, backed by AWS, Google Cloud, Oracle, and a large community. Auto-migrated — existing servers switch from Redis to Valkey automatically on cipi self-update , reusing the same password and dataset with zero app changes."},{"p":"about","c":"About Cipi","id":"why-laravel","h":"Why Laravel?","t":"About Cipi Why Laravel? Why Laravel? When v4 was designed, one of the clearest decisions was to drop generic PHP and WordPress support entirely and go all-in on a single framework. That framework was Laravel — and the reasoning goes well beyond personal preference. Speed of development Laravel gives developers an extraordinary head start. Authentication, queues, scheduled tasks, file storage, email, notifications, API resources, database migrations — all of it is built in, consistent, and documented. A team can go from an empty project to a production-ready application in a fraction of the time it would take with a lower-level stack. For Cipi, this means that every deployment assumption — directory layout, .env handling, queue workers, the scheduler — maps cleanly to a known structure. There is no guesswork. A thriving open-source community Laravel has one of the most active and welcoming communities in the PHP world. Packages are published, maintained, and discussed daily on GitHub, Laracasts, Discord, and X. When you hire a Laravel developer, you get someone who already knows the conventions, the tooling, and the ecosystem. When you publish a Laravel package, it reaches tens of thousands of developers overnight. That network effect is genuinely valuable — and it is one of the reasons Cipi's companion package, cipi-agent , works so well: drop it into a Laravel app and it just fits. Security by default Laravel takes security seriously out of the box. CSRF protection, SQL injection prevention through the query builder, XSS mitigation via Blade's auto-escaping, rate limiting, signed URLs, encrypted cookies — these are not add-ons you wire together, they are the defaults. The framework's release cycle includes prompt security patches, and the team publishes clear upgrade guides. For production applications that handle real users and real data, that reliability matters enormously. Longevity and support Every major Laravel version receives bug fixes for 18 months and security fixes for two years after release. LTS versions extend that further. Taylor Otwell and the core team have maintained a consistent, principled release cadence since 2011 — a track record that is rare and genuinely reassuring for long-term projects. When you deploy on Cipi, you are not betting on a framework that might be abandoned next year. The Laravel ecosystem Laravel does not stand alone. Around it has grown an ecosystem of first-party and community tools that are among the best in the industry: Livewire — full-stack reactive components without leaving PHP Filament — a stunning admin panel and form builder built on Livewire Inertia.js — the bridge between Laravel and modern front-end frameworks (Vue, React, Svelte) Laravel Horizon — a beautiful dashboard for monitoring Valkey/Redis queues; Cipi enables it with cipi worker horizon enable Laravel Reverb — first-party WebSockets; Cipi wires Supervisor, Nginx /app proxy, and REVERB_* with cipi app reverb enable Laravel Telescope — an elegant debug assistant for local and staging environments Laravel Octane — supercharges application performance with FrankenPHP (and optionally Swoole / RoadRunner). Cipi runs Octane natively: cipi app create --octane or cipi app convert --to=octane Pest — a delightful PHP testing framework with a Laravel-native plugin Laracasts — the best screencasts in PHP, period These tools do not just exist — they are actively developed, widely adopted, and genuinely fun to use. The Laravel world has a rare quality: it manages to be both opinionated enough to feel cohesive and flexible enough to stay out of your way when you need it to. Ship or die Taylor Otwell , the founder of Laravel, has a mantra that resonates far beyond the framework: \"we must ship.\" It is a simple statement, but it encapsulates an entire philosophy — that code only has value when it reaches users, that perfectionism is the enemy of progress, and that the best developers are the ones who find ways to move forward even w"},{"p":"about","c":"About Cipi","id":"why-cipi","h":"Why \"ci-pi\"?","t":"About Cipi Why \"ci-pi\"? Why \"ci-pi\"? Cipi is the Italian reading of the letters C and P — which stand for Control Panel . It is a quiet nod to the project's Italian roots and to the fact that, underneath all the CLI commands, Cipi is doing exactly what a control panel does: managing Nginx, PHP-FPM, MariaDB, Supervisor, Certbot, and UFW on your behalf — just without the browser tab. The name also carries a deliberate lightness. Server management tools tend to take themselves very seriously. Cipi does not. It is a tool built by a developer for developers, with the goal of getting out of the way as quickly as possible. The mascot The Cipi mascot is a penguin — an intentional reference to Tux , the official Linux mascot created by Larry Ewing in 1996. Where Tux is polished and iconic, the Cipi penguin is drawn in a minimal, hand-sketched line style: same species, different attitude. Think of it as Tux's quieter sibling who prefers the terminal over the spotlight. The illustration is rendered as a pure SVG using the project's accent color, so it adapts naturally to light and dark mode and scales to any size without losing quality."},{"p":"about","c":"About Cipi","id":"vs-alternatives","h":"Cipi vs alternatives","t":"About Cipi Cipi vs alternatives Cipi vs alternatives There are many tools for provisioning and managing web servers. This page explains where Cipi sits in that landscape and why you might — or might not — choose it over each category. The comparison is focused on eight axes: AI integration (dual MCP server — per-app and global) , Laravel Octane (FrankenPHP) + Horizon + Reverb , native Git deploy (GitHub & GitLab) , REST API , CLI-first automation , encryption at rest (Vault) , GDPR compliance , and total cost of ownership . Looking for a specific head-to-head? See the dedicated comparison pages: Laravel Forge alternative , Laravel Cloud alternative , Ploi alternative , Cleavr alternative , moss.sh alternative , Kamal alternative , RunCloud alternative , ServerPilot alternative , CloudPanel alternative , Coolify alternative , Dokku alternative , Easypanel alternative , Vito Deploy alternative , cPanel alternative , DirectAdmin alternative , Plesk alternative , plus in-depth sections on HestiaCP & VestaCP , aaPanel , and GridPane, xCloud & ServerAvatar — or browse them all on the alternatives overview . Cipi's core proposition: free forever, MIT open source, Laravel-first (with custom app support), CLI-first, AI-ready with dual MCP server, native GitHub & GitLab integration, full REST API, AES-256-CBC encryption at rest, GDPR-compliant log retention, self-contained on your VPS . It does one thing and does it well — it does not try to be a generic hosting panel, a SaaS platform, or a multi-stack tool. In addition to Laravel, Cipi supports custom apps (static sites, SPAs, WordPress, alternative PHP frameworks, and other non-Laravel projects). Cipi provides automatic deployment integration with GitHub and GitLab, a complete REST API for managing hosts and applications programmatically, a dual MCP server architecture for AI-driven management, built-in Vault encryption for all configuration files and sync archives, and GDPR-compliant automatic log rotation. No credentials, SSH keys, or sensitive data are ever stored in plaintext — and no infrastructure data ever leaves your VPS. Tool AI ready (MCP) CLI / automation Free / OSS No SaaS dep. Cipi Laravel Cloud partial usage-based Laravel Forge partial $12–19/mo Ploi partial €8–30/mo moss.sh limited free / $9–49/mo Cleavr partial ~$8+/mo Kamal Dokku Easypanel partial RunCloud $12–18/mo CloudPanel partial Coolify partial ServerPilot $12–49/mo HestiaCP / VestaCP cPanel / Plesk $20+/mo DirectAdmin $2–29/mo aaPanel Vito Deploy GridPane / xCloud / ServerAvatar Cipi vs Laravel Cloud Laravel Cloud is the Laravel team's fully-managed deployment platform — a Platform-as-a-Service that runs your application on dedicated AWS infrastructure with one-click autoscaling, managed databases (MySQL / serverless Postgres), Valkey cache, object storage, an edge network with DDoS protection, automatic SSL, and scale-to-zero compute. You connect a Git provider, pick a repo, and deploy in under 60 seconds — with no servers to manage. It is the opposite end of the spectrum from Cipi: where Cipi gives you full ownership of a VPS you control, Cloud abstracts the server away entirely in exchange for a usage-based bill. Where Cipi wins: Cost & predictability — Cloud is usage-based: you pay for compute, databases, cache, bandwidth, and storage as you consume them (after the $5 monthly credit). Costs scale with traffic and can be hard to predict. Cipi is free for every server you own, for life — your only cost is the VPS itself, at a fixed, predictable price. You own the server — Cloud runs on infrastructure managed entirely by Laravel on AWS; you never get root, SSH, or control over the underlying machine. Cipi runs on your own VPS at any provider (Hetzner, OVH, DigitalOcean, AWS, bare metal) with full root access and complete control over the stack. No SaaS dependency — Cloud is a hosted platform: if you stop paying, your apps stop running, and management depends on a reachable cloud.laravel.com. Cipi lives entirel"},{"p":"advanced","c":"Advanced","id":"cipi-api","h":"cipi api","t":"Advanced cipi api Advanced cipi api Cipi can optionally enable a REST API layer on the server via cipi api <domain> . It is powered by the Laravel package cipi/api (current release 1.20 ), which exposes: REST API — apps (including Octane create, .env , auth.json , Artisan, whitelisted app run , deploy-config), aliases, www redirects, deploy, SSL, multi-engine databases, app logs, server status ( /api/* ) MCP server — 50+ tools at /mcp (Streamable HTTP) Server cockpit — PHP install/switch, SSH keys, services, SMTP, healthchecks, IP whitelist (API 1.15.0+ / Cipi 5.0.6+ ) Swagger UI — interactive reference at /docs Requires PHP 8.2+ and Laravel 12+ on the panel host. This is server-level automation — distinct from the per-app Cipi Agent package ( cipi/agent on each Laravel app). The cipi/api package On a normal Cipi server you never install the package manually — cipi api <domain> provisions Laravel at /opt/cipi/api , Nginx, SSL, SQLite job queue, and cipi-queue.service . For reference or custom setups: bash Copy Panel .env uses CIPI_APPS_JSON=/etc/cipi/apps.json (or apps-public.json projection for non-sensitive fields). Token abilities are defined in config/cipi.php — list them with php artisan cipi:token-abilities (same list as cipi api token create since Cipi 4.6.3 ). Source and changelog: github.com/cipi-sh/api (MIT). Client wrapper: cipi-cli . Commands bash Copy Panel API troubleshooting After cipi self-update , root-owned files under /opt/cipi/api or /opt/cipi/gui can prevent PHP-FPM ( www-data ) from writing logs or the SQLite job database — the browser shows a bare HTTP 500 on /docs or /mcp . Cipi normally repairs ownership automatically during self-update (migration 5.0.13+ reclaims API/GUI ownership); if problems persist: bash Copy cipi api status prints the installed Laravel and cipi/api package versions, whether cipi-queue.service is active, pending async jobs in the panel SQLite database, and PHP-FPM pool stats for the API vhost (including slow requests when configured). cipi api update soft-updates the panel API package from Packagist (since 5.0.15 ; migration drops stale VCS repo entries). cipi api upgrade performs a full rebuild with rollback at /opt/cipi/api.old . Since 5.0.14–5.0.17 , self-update uses timed GitHub tarballs and Packagist dist installs instead of blocking Composer VCS clones for API and GUI packages. Since 5.0.18 , GUI upgrade/update avoids Composer symlinks that break PHP-FPM open_basedir (HTTP 500); run cipi gui fix-permissions or cipi self-update to repair existing panels. Since v4.7.18 (migrations 4.7.15–4.7.18 ), Panel API failures on Ubuntu 25.10+ / 26.04 are fixed end-to-end: sudo-rs rejects cipi db restore * * wildcards (the whole sudoers file was ignored — I'm afraid I can't do that ), so the whitelist uses trailing * only; sourcing common.sh no longer aborts read-only commands when /etc/cipi is remounted read-only; API open_basedir includes /usr/local/bin/ for log helpers; and cipi db list shows empty databases and surfaces vault/MariaDB errors. Run cipi self-update to apply. Token creation and granular permissions Authentication uses Sanctum . Each token can have one or more abilities that limit allowed operations: apps-view — read apps apps-create — create apps apps-edit — edit apps (PHP, repository, branch, primary domain since API 1.9.0+ / Cipi 4.6.2+ ) apps-suspend — suspend and unsuspend apps apps-basicauth — enable, disable, and inspect HTTP Basic Auth on apps (API 1.10.0+ ) apps-env — list/merge app .env keys (API 1.14.0+ / Cipi 5.0.3+ ) apps-auth — manage shared Composer auth.json (API 1.14.0+ ; distinct from apps-basicauth ) apps-artisan — run Artisan as an async job (API 1.14.0+ ) apps-run — whitelisted non-interactive app run (API 1.14.0+ ) apps-deploy-config — structured Deployer recipe options (API 1.14.0+ ) php-view — list installed PHP versions (API 1.15.0+ ) php-manage — install/remove PHP, set system default (API 1.15.0+ / 1.17.0+ for PUT /api/php/default ) ssh-view — l"},{"p":"advanced","c":"Advanced","id":"whmcs","h":"WHMCS Module","t":"Advanced WHMCS Module WHMCS Module An official WHMCS provisioning module is available at github.com/cipi-sh/whmcs . It bridges the WHMCS provisioning lifecycle to the Cipi REST API — automating app creation, deletion, SSL certificates, deployments, and configuration changes for your hosting customers. No Composer dependencies; the module is a self-contained drop-in. Requirements WHMCS 8.x (provisioning module type “Server”) Cipi server with API enabled: cipi api <domain> and cipi api ssl Sanctum Bearer token with the required abilities: Ability Required for apps-view Test Connection, App Info apps-create Create Account apps-edit Change Package apps-suspend Suspend / Unsuspend apps-delete Terminate Account deploy-manage Deploy, Rollback, Unlock ssl-manage Install SSL, Auto-SSL Installation Copy modules/servers/cipi/ into your WHMCS root: In WHMCS Admin → System Settings → Servers → Add New Server : Type : Cipi (Laravel hosting) Hostname : API base URL (e.g. https://api.example.com , no trailing slash) Password : Bearer token from cipi api token create Secure : Yes (recommended — enables TLS verification) Create a hosting product linked to this server and configure Module Settings : Setting Description Default PHP Version 8.2 / 8.3 / 8.4 / 8.5 8.5 App Type laravel or custom laravel Git Repository (SSH) Required for Laravel; optional for custom — Git Branch Branch to deploy main Auto SSL Install Let’s Encrypt after creation No App types App type Cipi equivalent Stack Git / Deploy laravel (default) cipi app create Isolated Linux user, PHP-FPM pool, Nginx vhost, MariaDB, Supervisor workers, Deployer releases SSH repository URL required ; branch from settings custom cipi app create --custom htdocs/ directory, Nginx + PHP — ideal for static sites, SPAs, WordPress, or generic PHP apps Optional : leave Git Repository empty for SFTP-only hosting, or set a repo for Git-based deploy Provisioning lifecycle WHMCS action API call Behaviour Test Connection GET /api/apps Validates token and API reachability Create Account POST /api/apps Provisions a Cipi app (Laravel or custom); waits for async jobs; optionally installs SSL Suspend POST /api/apps/{name}/suspend Takes the app offline (HTTP 503 maintenance page) without deleting it; waits for async jobs Unsuspend POST /api/apps/{name}/unsuspend Restores the app’s normal Nginx vhost; waits for async jobs Terminate Account DELETE /api/apps/{name} Removes the app; waits for async jobs Change Package PUT /api/apps/{name} Updates PHP version, Git repository, or branch Suspend / Unsuspend require Cipi 4.5.8+ (suspend/unsuspend endpoints), the API package 1.8.1+ , and a token with the apps-suspend ability. Suspending swaps the app’s vhost for a generic HTTP 503 maintenance page; unsuspending restores it. Admin buttons From the WHMCS admin service view, operators can trigger one-click actions: Button API call Description Install SSL POST /api/apps/{name}/ssl Install a Let’s Encrypt certificate Deploy POST /api/apps/{name}/deploy Trigger a zero-downtime deployment Rollback Deploy POST /api/apps/{name}/deploy/rollback Revert to the previous release Unlock Deploy POST /api/apps/{name}/deploy/unlock Unlock a stuck deployment App Info GET /api/apps/{name} Fetch current app details into the Module Log Auto-SSL on creation Enable Auto SSL in the product module settings to automatically install a Let’s Encrypt certificate right after provisioning. If SSL installation fails, the app is still created successfully and a warning is logged. Full API client The bundled CipiApiClient covers the entire Cipi REST API surface. Even if a feature is not wired to a WHMCS hook, you can use the client in custom hooks or addons: Area Methods Apps listApps, getApp, createApp, editApp, suspendApp, unsuspendApp, deleteApp Deploy deployApp, rollbackDeploy, unlockDeploy SSL installSsl Aliases listAliases, addAlias, removeAlias Databases listDatabases, createDatabase, deleteDatabase, backupDatabase, restoreDatabase, resetDatabasePas"},{"p":"advanced","c":"Advanced","id":"cipi-sync","h":"cipi sync","t":"Advanced cipi sync cipi sync Transfer, replicate, and back up entire Laravel applications between Cipi servers — including configuration, database dumps, storage files, SSH keys, workers, and crontabs. Every archive is encrypted with AES-256-CBC and protected by a user-defined passphrase, so credentials and sensitive data are safe at rest and during transfer. Commands overview bash Copy Archive encryption All sync archives are encrypted by default with AES-256-CBC. During export you are prompted for a passphrase (minimum 8 characters) that protects the archive. The same passphrase is required to import or inspect it. This protects SSH keys, .env files, database dumps, and credentials at rest and during transfer. bash Copy For automated setups, store the passphrase in a secure file and reference it in your scripts: echo \"MyStr0ngP@ss\" > /etc/cipi/.sync_passphrase && chmod 400 /etc/cipi/.sync_passphrase . Then use --passphrase=\"$(cat /etc/cipi/.sync_passphrase)\" in cron jobs. Export Packs app configs into an encrypted .tar.gz.enc archive. Optionally includes database dumps and storage files. bash Copy What goes into the archive File Description Included env The app's .env from /home/<app>/shared/.env Always auth.json Composer auth credentials (if exists) Always deploy.php Deployer config Always ssh/* Deploy key, known_hosts, authorized_keys, SSH config Always supervisor.conf Queue workers config Always crontab App's crontab (scheduler + deploy trigger) Always db.sql.gz Gzipped MariaDB dump (schema + data + routines) --with-db storage.tar.gz Archive of /home/<app>/shared/storage/ --with-storage Plus global configs: apps.json (filtered to selected apps), databases.json , backup.json , api.json . Import Restores apps from an archive onto the current server. bash Copy What import does for a NEW app When an app does not exist on the target server, import creates it from scratch — equivalent to cipi app create with all configs pre-filled from the archive: Linux user — Creates a new user with a random password Directories — Creates /home/<app>/shared/ , logs/ , .ssh/ , .deployer/ SSH deploy key — Restores from archive (same key works with GitHub/GitLab without reconfiguration) MariaDB database — Creates database + user with a new random password Database data — Imports the dump if --with-db was used during export .env — Copies from archive, then overwrites DB_PASSWORD , DB_USERNAME , DB_DATABASE , DB_HOST with the new server's values. Everything else ( APP_KEY , MAIL_* , REDIS_* , custom vars) stays as-is PHP-FPM pool, Nginx vhost, Supervisor workers, Crontab, Deployer — Fully configured from archive data At the end of import, Cipi prints the new SSH and DB passwords. Save them — they are shown only once. Safety checks before import The import runs pre-flight checks before touching anything: App already exists — blocked unless --update is passed Domain conflict — blocked if another app already uses the same domain Missing PHP version — warning (the app is skipped; install the version first with cipi php install ) Update mode ( --update ) The key feature for repeated sync (e.g. failover replication). Without --update , import refuses to touch apps that already exist. With --update , it updates existing apps and creates new ones. bash Copy What update does for an existing app .env sync — The archive .env replaces the local one, but DB_PASSWORD , DB_USERNAME , DB_DATABASE , and DB_HOST are preserved from the local server . Everything else ( APP_KEY , MAIL_* , REDIS_* , custom vars) comes from the source. Database data — If the archive has a dump, drops all tables (with SET FOREIGN_KEY_CHECKS=0 ) and reimports. Uses local root credentials. Storage — If the archive has storage, extracts over the existing directory (new files added, existing overwritten). PHP version migration — If the source uses a different PHP version, the update migrates FPM pool, supervisor, crontab, deployer, and .env automatically. Nginx vhost, Supervisor workers, Dep"},{"p":"advanced","c":"Advanced","id":"cipi-vault","h":"Vault & Encryption","t":"Advanced Vault & Encryption Vault & Encryption Cipi encrypts all configuration files at rest using AES-256-CBC . The Vault system provides transparent encryption and decryption so that sensitive data — database passwords, API tokens, SSH keys, .env contents — is never stored in plaintext on disk. Architecture The system is built on two layers: Vault — transparent encryption of JSON configuration files on disk ( server.json , apps.json , databases.json , backup.json , smtp.json , api.json ) Sync encryption — passphrase-based encryption of export archives for secure transfer between servers How Vault works A master key is generated during installation with openssl rand -base64 32 and stored at /etc/cipi/.vault_key ( chmod 400 , root-only). Every JSON config file is encrypted on disk with openssl enc -aes-256-cbc -salt -pbkdf2 . Files keep the .json extension — the content is simply an encrypted blob instead of readable JSON. The vault_read function auto-detects whether a file is plaintext or encrypted (backward compatibility), so existing servers migrate seamlessly during update. Vault functions bash Copy Public projection Cipi generates an apps-public.json file containing only non-sensitive fields (domain, aliases, PHP version, branch, repository, user, creation timestamp, plus suspended and basic_auth flags). The cipi-api group reads this plaintext projection instead of the encrypted file, keeping the vault key restricted to root. Sync archive encryption When you run cipi sync export , configs are decrypted from the vault into a staging area, then the entire archive is encrypted with your passphrase. On import, the archive is decrypted with the passphrase, and configs are re-encrypted with the destination server's vault key . If the vault key is lost, configuration files become irrecoverable. The key is protected by chmod 400 and included in server backups. Consider exporting it manually for additional safety."},{"p":"advanced","c":"Advanced","id":"cipi-smtp","h":"Email Notifications","t":"Advanced Email Notifications Email Notifications Cipi can send email alerts when backup errors, deploy failures, system cron job failures, or security-relevant authentication events occur. SMTP configuration is stored encrypted in /etc/cipi/smtp.json and included in sync exports. Commands bash Copy Granular notification triggers Since v4.6.3 , you can control which events send email when SMTP is configured. All triggers are on by default ; events are always logged to /var/log/cipi/events.log regardless. bash Copy Config: /etc/cipi/notifications.json . Run cipi notifications list on the server for the live on/off state. Trigger IDs for cipi notifications enable|disable <trigger> : v5.1.0 adds five: backup_stale (a profile has not succeeded within twice its own interval), ini_set (a PHP setting changed), yml_apply and yml_fail (a project's cipi.yml was applied, or was invalid), and self_update (Cipi updated itself — so an unattended nightly update no longer lands without a word). Trigger ID Category Event app_create Apps App created app_edit Apps App modified app_delete Apps App deleted app_suspend Apps App suspended app_unsuspend Apps App unsuspended app_ssh_password_reset Apps App SSH password reset app_db_password_reset Apps App DB password reset alias_add Domains Alias added alias_remove Domains Alias removed www_add Domains WWW alias added www_force_to_root Domains WWW force to-root www_force_from_root Domains WWW force from-root www_clear Domains WWW redirect cleared auth_create Auth Composer auth.json created auth_edit Auth Composer auth.json edited auth_delete Auth Composer auth.json deleted basicauth_enable Basic auth HTTP basic auth enabled basicauth_disable Basic auth HTTP basic auth disabled deploy_success Deploy Deploy succeeded deploy_fail Deploy Deploy failed deploy_rollback Deploy Deploy rollback deploy_snapshot_fail Deploy Pre-deploy DB snapshot failed health_fail Health HTTP healthcheck failed (periodic, after 3 failures) deploy_health_fail Health Post-deploy healthcheck failed ssl_install SSL SSL certificate installed ssl_force SSL HTTP → HTTPS redirect forced ssl_renew SSL SSL certificates renewed php_install PHP PHP version installed php_switch PHP System PHP switched php_remove PHP PHP version removed php_upgrade PHP PHP packages upgraded db_create Database Database created db_delete Database Database deleted worker_add Workers Worker added worker_remove Workers Worker removed ssh_key_add SSH keys SSH key added ssh_key_rename SSH keys SSH key renamed ssh_key_remove SSH keys SSH key removed ssh_login Security SSH login (cipi/root/sudo users) sudo Security Sudo elevation su Security su to root by cipi backup_fail Backup Backup failed backup_stale Backup Backup overdue (no successful run in its window) (5.1.0) cron_fail Cron Cron job failed ini_set PHP PHP setting changed (5.1.0) yml_apply cipi.yml cipi.yml applied (5.1.0) yml_fail cipi.yml cipi.yml invalid or could not be applied (5.1.0) self_update Updates Cipi updated itself (5.1.0) reset_root_password Reset Root SSH password reset reset_db_password Reset MariaDB root password reset reset_valkey_password Reset Valkey password reset api_configure API Panel API configured api_update API Panel API updated api_upgrade API Panel API upgraded api_ssl API Panel API SSL installed git_configure Git Git provider token configured sync_export Sync Apps exported sync_import Sync Apps imported sync_push Sync Apps pushed to remote service_restart Services Service restarted service_start Services Service started service_stop Services Service stopped Automatic alerts Once configured, Cipi sends email notifications on: Backup errors (S3 upload failures, dump errors, a corrupt archive) and, since v5.1.0 , backups that are simply overdue ( backup_stale ) Deploy results — since v5.1.0 both success and failure, from the CLI and the Git webhook alike, with the branch, release number, commit, author, duration and the post-deploy healthcheck verdict in the body A release that faile"},{"p":"advanced","c":"Advanced","id":"cipi-log-retention","h":"Log Retention (GDPR)","t":"Advanced Log Retention (GDPR) Log Retention (GDPR) Cipi enforces automatic log rotation policies designed to meet GDPR and general data protection requirements. Logs are rotated and deleted automatically — no manual cleanup needed. Category Logs Retention Application Laravel, PHP-FPM, workers, deploy, system 12 months Security Fail2ban, UFW firewall, authentication, Cipi events ( events.log ) 12 months HTTP / Navigation Nginx access and error logs 90 days HTTP/navigation logs (nginx access logs) contain IP addresses, which are personal data under GDPR. The 90-day retention ensures compliance with the data minimization principle while preserving enough history for debugging and security analysis. Application and security logs are retained for 12 months to support audit trails and incident investigation."},{"p":"advanced","c":"Advanced","id":"valkey","h":"Valkey","t":"Advanced Valkey Valkey Valkey is the in-memory data store that Cipi installs as part of the default stack. Since v4.5.6 Cipi provisions Valkey instead of redis-server . It excels at caching, session storage, message queues, real-time broadcasting, and rate limiting. Why Valkey instead of Redis Valkey is the truly open-source, BSD-licensed fork of Redis, stewarded by the Linux Foundation . It was created in 2024 after Redis Inc. relicensed Redis away from the permissive BSD license to the source-available SSPL / RSALv2 — a change that no longer met the open-source definition. Backed by AWS, Google Cloud, Oracle, and a large community, Valkey continues the same battle-tested codebase under a license that stays free forever . That makes it a perfect fit for Cipi's MIT, no-vendor-lock-in philosophy, and it ships natively in Ubuntu 24.04's Universe repository (packages valkey-server + valkey-tools ) — no third-party PPA to trust. Just as importantly, Valkey is a drop-in replacement : it speaks the exact same RESP protocol on the same port ( 127.0.0.1:6379 ), honours the same requirepass / bind directives, and reads the same RDB/AOF data format. Your apps need zero changes — the phpredis extension and your existing REDIS_* .env values keep working exactly as before. How Cipi implements it Install — setup.sh installs and configures Valkey ( /etc/valkey/valkey.conf , service valkey-server ), bound to localhost only and protected with a password. Service management — cipi service … manages valkey-server (the names redis-server , redis , and valkey are still accepted as aliases). It is added to the unattended-upgrades blacklist, so Cipi manages it instead of an automatic upgrade. Credentials — stored under valkey_user / valkey_password in /etc/cipi/server.json (the legacy redis_* keys are still read as a fallback). Host: 127.0.0.1, Port: 6379. Password reset — cipi reset valkey-password regenerates the password and restarts the service ( cipi reset redis-password stays as an alias). Migrating from Redis (4.5.6 / 4.5.7) Existing servers are switched to Valkey automatically on cipi self-update — no app .env editing required. The migration reuses the current Redis password (recovered from server.json or /etc/redis/redis.conf ), forces an RDB SAVE and snapshots dump.rdb /AOF, purges redis-server , installs valkey-server + valkey-tools on the same port with the same requirepass / bind , restores the dataset, and rewrites server.json ( redis_* → valkey_* ) and the unattended-upgrades blacklist — so cache, sessions, and queued jobs survive the switch. v4.5.7 corrects the package name to valkey-server (the Ubuntu 24.04 daemon package; 4.5.6 initially used valkey ) and makes the migration fully self-contained and safe. It auto-enables the universe APT component when the package isn't found, runs a post-start health check ( PING → PONG with the password), and rolls back to redis-server — restoring both the saved password and the dataset — if Valkey can't be installed or doesn't come up healthy. The dataset snapshot is kept until the switch is verified, then cleaned up. The migration is idempotent: servers already on Valkey skip it. Laravel integration Add these variables to your .env via cipi app env myapp . The variable names stay REDIS_* — that is what phpredis and Laravel's redis driver expect, and Valkey answers on the very same socket: env Copy Then set the drivers for each use case: Cache — CACHE_STORE=redis Session — SESSION_DRIVER=redis Queue — QUEUE_CONNECTION=redis (then cipi worker restart myapp ) Broadcasting — BROADCAST_CONNECTION=redis Install the phpredis PHP extension for best performance, or use predis/predis as a pure-PHP fallback. Both talk to Valkey transparently."},{"p":"advanced","c":"Advanced","id":"self-update","h":"Self-Update","t":"Advanced Self-Update Self-Update Cipi can update itself from GitHub without affecting any app, database, or configuration. bash Copy Update process Downloads the latest version from GitHub Backs up the current installation to /opt/cipi.bak.YYYYMMDDHHMMSS/ Replaces CLI and lib scripts Runs any pending migration scripts in order (e.g. new Nginx directives, new packages) Updates the version file Migration scripts live in lib/migrations/ and are named by version (e.g. 4.1.0.sh , 5.0.18.sh ). When updating from v4.0.0 to v4.2.0, Cipi automatically runs 4.1.0.sh and 4.2.0.sh in order. Your apps, databases, and configurations are never touched. Recent migrations improve reliability without changing app data: 5.0.6 — default IP whitelist file and regenerated API sudoers; 5.0.9 — cipi php switch in sudoers for PUT /api/php/default ; 5.0.13 — reclaim API/GUI ownership after update; 5.0.14–5.0.17 — timed GitHub/Packagist panel package updates (no more hung cipi self-update on API/GUI Composer VCS clones); 5.0.18 — repair Panel GUI after symlink/ open_basedir HTTP 500 ( cipi gui fix-permissions ). Run cipi self-update to reach 5.1.0 . Migration 5.1.0 The 5.1.0 migration is the largest of the 5.x line. On an existing server it: installs the automatic-deploy wrapper and repoints each app's webhook cron at it; backfills the CLI 99-cipi.ini and rewrites FPM pools so they inherit the server-wide file — see cipi ini ; converts the hardcoded nightly backup job into a default backup profile , carrying over its existing --weeks retention; takes over the backup schedule with the managed crontab block; claims the :443 default server when nothing else does. Every step degrades to a warning and carries on. That matters: on an early 5.1.0 build a grep that matched nothing could fail the migration under set -o pipefail , and a failed migration makes cipi self-update refuse the release and retry it, failing again, every night. When the update itself fails Up to 5.0.x, cipi self-update reported only “Download failed” — git's stderr was discarded, so the one message that explains what happened (no DNS, no outbound HTTPS, a missing branch, a full disk, an expired CA bundle) never reached anyone. Since v5.1.0 the real error is printed, the likely cause is named, and the command to reproduce it by hand is shown. The clone also runs under a 180-second timeout instead of being able to hang indefinitely, and a missing git is reported as such. An unattended nightly update now also sends the self_update notification, so it no longer lands without a word. For example, the 4.5.5 migration retro-fits existing apps with the new ll='ls -al' shell alias: it appends the alias to each app's ~/.bashrc once (only when missing, preserving ownership), so apps created before 4.5.5 get it on the next cipi self-update . Automatic maintenance crons Cipi schedules several root-level jobs during installation. App-level crontabs (scheduler, deploy trigger) are separate — see User crontab . Schedule Job Daily 02:00 cipi backup run — S3 backups for all apps Daily 03:00 cipi backup prune --weeks=4 Sun 03:30 cipi php upgrade — security patches for all installed PHP versions (wrapped by cipi-cron-notify ) Daily 03:50 cipi self-update (wrapped by cipi-cron-notify ) Sun 04:10 cipi ssl renew Daily 04:15 Panel API maintenance ( cipi-api-maintain — prune jobs/metrics) Daily 04:30 cipi api update — soft-update panel Laravel + cipi/api"},{"p":"advanced","c":"Advanced","id":"wildcard-domains","h":"Wildcard domains","t":"Advanced Wildcard domains Wildcard domains Cipi does not support wildcard domains ( *.myapp.com ) natively. The block is twofold and architectural — not a configuration detail. Why wildcards are not supported 1 — Domain validation rejects * Every domain passed to cipi alias add (and cipi app create ) is validated against a strict regex that requires the string to start with [a-zA-Z0-9] . The asterisk fails immediately, before nginx or certbot are ever touched. 2 — Certbot uses HTTP-01 challenge, which cannot issue wildcard certs cipi ssl install calls certbot --nginx , which relies on the HTTP-01 (or TLS-ALPN-01) challenge — placing a verification file on disk and serving it over port 80. Let's Encrypt only issues wildcard certificates via the DNS-01 challenge , which requires programmatic access to your DNS provider's API. Cipi does not integrate with any DNS provider, so even if the validation were bypassed, certbot would refuse to issue the wildcard cert. Recommended alternative — Multi-SAN certificate If your subdomains are fixed and enumerable (e.g. api , admin , www , staging ), the correct approach is to add each one as an explicit alias and let Cipi issue a single SAN certificate covering all of them: bash Copy Certbot's --expand flag (used internally by Cipi) adds the new SANs to the existing certificate without issuing a new one. The SAN list has no meaningful limit for typical use. Manual wildcard certificate (outside Cipi) If you need dynamic subdomains (e.g. <tenant>.saas.com ), you can obtain a wildcard certificate manually using a DNS plugin for certbot and place it on the server. Cipi will not manage, renew, or track it — you own the lifecycle entirely. bash Copy After obtaining the certificate, edit the nginx vhost for the app directly ( /etc/nginx/sites-available/myapp ) to reference the wildcard cert paths and add server_name *.myapp.com myapp.com; . Then reload nginx: bash Copy Running cipi ssl install myapp after manual wildcard setup will overwrite your custom nginx SSL directives with a Let's Encrypt HTTP-01 certificate. If you manage a wildcard cert manually, avoid running cipi ssl install on that app."},{"p":"advanced","c":"Advanced","id":"nginx-config","h":"Edit Nginx config","t":"Advanced Edit Nginx config Edit Nginx config To customise the Nginx vhost for an app, edit the site config directly. After changes, test and reload Nginx. bash Copy"},{"p":"advanced","c":"Advanced","id":"uninstall","h":"Uninstall Cipi","t":"Advanced Uninstall Cipi Uninstall Cipi Cipi does not provide a built-in uninstall command. If you need to completely remove Cipi from a server, follow the steps below in order . This procedure removes every component that Cipi installs — users, services, packages, configs, and data. This is a destructive and irreversible operation. All apps, databases, SSL certificates, and server configurations managed by Cipi will be permanently deleted. Back up everything you need before proceeding. After uninstalling, the recommended approach is to reprovision the server from scratch. 1 — Stop and remove all apps For each app managed by Cipi, remove its system user, home directory, database, nginx vhost, PHP-FPM pool, and supervisor config. bash Copy 2 — Remove the Cipi user and groups bash Copy 3 — Remove Cipi binaries, libraries, and data bash Copy 4 — Remove Cipi API (if installed) bash Copy 5 — Remove Cipi cron jobs bash Copy 6 — Remove Cipi configuration files bash Copy 7 — Purge installed packages Remove all packages that Cipi installed. Skip any package you want to keep for other purposes. bash Copy 8 — Remove APT repositories bash Copy 9 — Remove Composer and Deployer bash Copy 10 — Remove swap file bash Copy 11 — Restore SSH and PAM defaults Cipi hardens SSH (disables root login and password auth) and adds PAM hooks. If you need to restore defaults: bash Copy 12 — Reset firewall bash Copy After a full uninstall, the server will be stripped of its web stack and security hardening. The recommended approach is to reprovision the server from a clean OS image rather than trying to reconfigure the same machine. Use this guide primarily to clean up before a fresh start, or to selectively remove Cipi components while keeping packages you still need."},{"p":"agent","c":"Cipi Agent","id":"agent-install","h":"Installing the Cipi Agent","t":"Cipi Agent Installing the Cipi Agent Cipi Agent Installing the Cipi Agent Cipi Agent ( cipi/agent on Packagist ) is the official Laravel companion for Cipi . It bridges your application and the server control panel with: Webhook-triggered deploys from GitHub and GitLab Health monitoring (app, database, cache, queue, deploy commit) An in-app MCP server for AI assistants (Cursor, VS Code, Claude Desktop) A GDPR-oriented database anonymizer On a Cipi-managed server, cipi app create injects the required .env variables automatically. Health check and MCP can work on any Laravel host; full deploy and log access expect a Cipi-managed environment. Requirements Requirement Version PHP 8.3+ Laravel 12+ or 13+ (package 1.5.2+ ) Database (anonymizer) MySQL or PostgreSQL CLI tools (anonymizer) mysqldump or pg_dump on the server bash Copy The service provider auto-discovers — no config/app.php change is needed. After install, commit and push; Cipi deploys the update on the next release. Optional — publish the config file (non-Cipi or custom defaults): bash Copy Agent vs panel API: this package runs inside each Laravel app ( /cipi/* on the app domain). The server-level cipi/api package runs on a separate API vhost and manages the whole server — see Agent vs Cipi API . After installing the package, commit and push. Cipi will pick it up on the next deploy automatically. Artisan commands Command Description php artisan cipi:status Show Cipi config values and connectivity status php artisan cipi:deploy-key Print the SSH deploy key for this app php artisan cipi:mcp Show the MCP endpoint URL and setup snippets for Cursor, VS Code, and Claude Desktop php artisan cipi:generate-token {type} Generate a secure token. Type can be mcp , health , or anonymize php artisan cipi:service {type} --enable|--disable Toggle a service on or off. Updates .env in place. Type: mcp , health , or anonymize php artisan cipi:init-anonymize Scaffold the anonymization config at /home/{app_user}/.db/anonymization.json php artisan cipi:anonymize {config} {output} Run an anonymized database dump directly from the CLI (no HTTP)"},{"p":"agent","c":"Cipi Agent","id":"agent-webhook","h":"Webhook — Automatic Deploys","t":"Cipi Agent Webhook — Automatic Deploys Webhook — Automatic Deploys The agent exposes a POST endpoint at /cipi/webhook . When your Git provider sends a push event, the agent verifies the signature and writes a .deploy-trigger flag file. A cron job running every minute as the app user detects this file, removes it, and runs Deployer in the background. This design means the webhook response is instant (no HTTP timeout waiting for deployment to complete) and Deployer runs with the correct user permissions — no sudo required. Configure your Git provider Provider Webhook URL Authentication GitHub https://yourdomain.com/cipi/webhook X-Hub-Signature-256 HMAC — use CIPI_WEBHOOK_TOKEN as Secret GitLab https://yourdomain.com/cipi/webhook X-Gitlab-Token header — same token value The token to use is stored in .env as CIPI_WEBHOOK_TOKEN . You can also retrieve it any time with: bash Copy Branch filtering By default every push triggers a deploy. To restrict deploys to a specific branch, add this to your .env : env Pushes to any other branch will receive a skipped response and no deploy will be triggered."},{"p":"agent","c":"Cipi Agent","id":"agent-health","h":"Health Check","t":"Cipi Agent Health Check Health Check The agent also exposes a GET endpoint at /cipi/health that returns a JSON payload with the status of the app, database, cache, queue, and the currently deployed Git commit hash. Useful for external monitoring services such as UptimeRobot. Protected by the CIPI_HEALTH_TOKEN Bearer token — generate one with php artisan cipi:generate-token health . bash Copy json The deploy commit is resolved from the first available source: /home/{app_user}/.cipi/deploy.json (Cipi deploy metadata) /home/{app_user}/.cipi/last_commit /home/{app_user}/logs/deploy.log .git/HEAD or git rev-parse HEAD Authentication The Bearer token is resolved in order: CIPI_HEALTH_TOKEN (dedicated), then CIPI_WEBHOOK_TOKEN (fallback). Disable the endpoint entirely with php artisan cipi:service health --disable or CIPI_HEALTH_CHECK=false . Monitoring integrations The health endpoint works with any HTTP checker that supports Bearer tokens — e.g. UptimeRobot , Better Stack , Grafana , or custom cron + curl . Poll checks.queue.pending_jobs for queue backlog alerts."},{"p":"agent","c":"Cipi Agent","id":"agent-mcp","h":"MCP Server","t":"Cipi Agent MCP Server MCP Server cipi-agent includes a built-in MCP server (Model Context Protocol) that exposes your application to AI assistants such as Cursor , VS Code (with GitHub Copilot), and Claude Desktop . The endpoint implements MCP 2024-11-05 over HTTP using JSON-RPC 2.0 and is protected by the CIPI_MCP_TOKEN Bearer token. The MCP endpoint is available at POST /cipi/mcp and is disabled by default. To enable it: bash Copy Available tools The MCP server exposes six tools that an AI assistant can invoke by name: Tool Description health App, database, cache, and queue status — same data as the /cipi/health endpoint app_info Full application configuration: app user, PHP version, Laravel version, environment, queue/cache/session drivers, deploy branch, and all Cipi URLs deploy Trigger a new zero-downtime deployment — writes the .deploy-trigger file; Deployer picks it up within 1 minute logs Read the last N lines (default 50, max 500) from application logs. Supports type ( laravel , nginx , php , worker , deploy ), level for Laravel severity filtering (e.g. error ), and search for keyword filtering. Laravel daily rotation ( laravel-YYYY-MM-DD.log ) is auto-detected. db_query Execute SQL queries against the application database — equivalent to cipi app tinker . Supports SELECT, SHOW, DESCRIBE, EXPLAIN (read) and INSERT, UPDATE, DELETE (write). Results formatted as ASCII table, capped at 100 rows. Destructive DDL (DROP TABLE/DATABASE, TRUNCATE, GRANT/REVOKE, file I/O) is blocked. artisan Run any Artisan command (e.g. migrate:status , queue:size , cache:clear ). Long-running and interactive commands like serve , queue:work , and tinker are blocked logs tool parameters Parameter Values Description type laravel , nginx , php , worker , deploy Log file to read (Laravel daily rotation auto-detected) level debug … emergency Minimum severity — Laravel logs only search any string Case-insensitive keyword filter; stack traces stay intact lines 1–500 (default 50) Number of lines to return Blocked operations (MCP safety) Artisan: serve , tinker , queue:work , queue:listen , schedule:work , horizon , octane:start , reverb:start SQL: DROP , TRUNCATE , GRANT , REVOKE , file I/O — read/write capped at 100 rows Setup instructions Run the cipi:mcp Artisan command to get the endpoint URL and ready-to-paste configuration snippets for your AI client: bash Copy The command prints the available tools and the JSON configuration for Cursor, VS Code, and Claude Desktop. Cursor Add the following to ~/.cursor/mcp.json (or go to Cursor → Settings → MCP): json Copy Replace cipi-myapp with your app user name, yourdomain.com with your actual domain, and YOUR_CIPI_MCP_TOKEN with the token from your .env . Cursor connects natively over HTTP — no bridge needed. VS Code VS Code (with GitHub Copilot) supports MCP natively since version 1.102. Add the following to .vscode/mcp.json in your project (or run MCP: Open User Configuration for a global setup): json Copy Replace cipi-myapp , yourdomain.com , and YOUR_CIPI_MCP_TOKEN as above. Use MCP: Add Server from the Command Palette for a guided setup. VS Code connects over HTTP — no bridge needed. Claude Desktop Claude Desktop requires the mcp-remote bridge to convert stdio to HTTP. Add the following to ~/Library/Application Support/Claude/claude_desktop_config.json : json Copy Install mcp-remote globally once with: bash Copy What you can do with it Once connected, your AI assistant can interact with the Cipi-managed application in natural language. For example: conversation The MCP server does not require SSH access to the VPS. It works entirely over HTTPS using the same Bearer token used by the webhook and health check endpoints. This makes it ideal for teams where developers should be able to monitor and deploy without root access. Keep your CIPI_MCP_TOKEN secret. Anyone with the token can trigger deploys, read logs, run database queries, and run Artisan commands through the MCP endpoint. If you suspect a leak,"},{"p":"agent","c":"Cipi Agent","id":"agent-anonymizer","h":"Database Anonymizer","t":"Cipi Agent Database Anonymizer Database Anonymizer cipi-agent includes a built-in database anonymizer that creates sanitized copies of your production database — ideal for sharing with developers, QA teams, or staging environments without exposing real user data. It supports both MySQL and PostgreSQL , uses Faker -based transformations configured via a JSON file, and runs as a background job so large databases do not block HTTP requests. This feature lives inside your Laravel application (the cipi/agent package). It is separate from the server-level database backup API exposed by cipi api — see Agent vs Cipi API below. Use cases Local development — give every developer a realistic dataset without copying production emails, addresses, or payment notes Staging / preview environments — refresh a non-production database from production structure and volume, with PII replaced QA and demos — reproduce bugs that depend on relational data without GDPR risk Vendor or contractor access — share a SQL dump when a full VPN + production access is not acceptable CI pipelines — run cipi:anonymize on the server or trigger POST /cipi/db from a secure automation step Prerequisites Requirement Why composer require cipi/agent The anonymizer is part of the agent package, not the Cipi server CLI Queue worker running POST /cipi/db dispatches AnonymizeDatabaseJob — without a worker the job never runs. As root: cipi worker list myapp ; as the app user: sudo cipi-worker status myapp mysqldump or pg_dump The command shells out to the native dump tool for your DB driver Laravel mail ( MAIL_* in app .env ) Success and failure notifications are sent via Laravel's mailer — if SMTP is missing or misconfigured, the anonymization job can still finish but no email is delivered and the download link is only in that message anonymization.json on the server Must exist at /home/{app_user}/.db/ or /home/{app_user}/.cipi/ before triggering a job Agent vs Cipi API Both Cipi components touch databases, but they solve different problems: Agent anonymizer ( cipi/agent ) Cipi API backups ( cipi/api ) Scope One Laravel app's database (from the app's .env ) Any database on the Cipi server (MariaDB vault) Runs on Inside the app (PHP + queue worker) On the Cipi host via sudo cipi db … jobs Output SQL dump with Faker-transformed sensitive columns Full compressed backup — real data, unchanged Auth CIPI_ANONYMIZER_TOKEN (per app) Sanctum token with dbs-manage (per server) Typical endpoint POST https://myapp.com/cipi/db POST https://api.example.com/api/dbs/{name}/backup GDPR / PII Designed for safe sharing — only configured columns are transformed Disaster recovery and cloning — treat backups as production-secret Use Cipi API DbBackup when you need a faithful snapshot for restore. Use the agent anonymizer when people need data that looks real but must not contain real identities. You can use both on the same project: backup for ops, anonymize for humans. How it works An authenticated POST /cipi/db request (with a recipient email ) queues AnonymizeDatabaseJob The job runs php artisan cipi:anonymize , which: dumps the database with mysqldump or pg_dump streams through INSERT statements and rewrites only the columns listed in anonymization.json writes the result to storage/cipi/anonymized_{jobId}.sql On success, Laravel sends an email with a time-limited download URL (15 minutes) GET /cipi/db/{token} serves the file — no Bearer token; the URL itself is the credential On failure, a plain-text error email is sent to the same address Email notifications The HTTP API does not return the download URL in the JSON response — email is the only delivery channel for POST /cipi/db . Understanding who receives it and what must be configured avoids silent failures. Who receives the email? Exactly the address you pass in the JSON body — nothing else: json Success → HTML email with the signed download link (15 minutes) Failure → plain-text email with the error and job ID No CC, BCC, or fallback to "},{"p":"agent","c":"Cipi Agent","id":"agent-security","h":"Security","t":"Cipi Agent Security Security Cipi Agent uses defense in depth : each feature has its own Bearer token and can be toggled off independently. When disabled, routes return 404 (hidden, not 403). Token isolation Feature Token variable Endpoint Webhook deploy CIPI_WEBHOOK_TOKEN POST /cipi/webhook Health check CIPI_HEALTH_TOKEN (fallback: webhook token) GET /cipi/health MCP server CIPI_MCP_TOKEN POST /cipi/mcp DB anonymizer CIPI_ANONYMIZER_TOKEN POST /cipi/db , POST /cipi/db/user Webhook verification GitHub — X-Hub-Signature-256 HMAC-SHA256 GitLab — X-Gitlab-Token header comparison Source code and releases: github.com/cipi-sh/agent (MIT)."},{"p":"agent","c":"Cipi Agent","id":"agent-env","h":"ENV Variables","t":"Cipi Agent ENV Variables ENV Variables These variables are automatically injected by Cipi into the app's .env during cipi app create . Toggle optional features with php artisan cipi:service {type} --enable|--disable or set them manually. Variable Description Default CIPI_WEBHOOK_TOKEN Secret for webhook authentication (GitHub HMAC / GitLab token) auto-generated CIPI_APP_USER Linux username for this app (paths, deploy script) auto-set CIPI_PHP_VERSION PHP version reported in health check system PHP CIPI_DEPLOY_SCRIPT Path to Deployer config ~/.deployer/deploy.php CIPI_DEPLOY_BRANCH Branch that triggers a deploy ( empty = any branch) empty CIPI_ROUTE_PREFIX URL prefix for all agent routes cipi CIPI_LOG_CHANNEL Laravel log channel for deploy events null CIPI_HEALTH_CHECK Enable /cipi/health true CIPI_HEALTH_TOKEN Bearer token for health (falls back to webhook token) none CIPI_MCP Enable /cipi/mcp false CIPI_MCP_TOKEN Bearer token for MCP none CIPI_ANONYMIZER Enable anonymizer at /cipi/db false CIPI_ANONYMIZER_TOKEN Bearer token for anonymizer none Use php artisan cipi:generate-token {type} to generate tokens for mcp , health , or anonymize . Use php artisan cipi:service {type} --enable|--disable to toggle services — the command updates your .env in place."},{"p":"apps","c":"Apps","id":"cli-app-create","h":"cipi app create","t":"Apps cipi app create Apps cipi app create cipi app create supports two app types: Laravel (default) and Custom ( --custom ). Laravel apps get a fully isolated environment: Linux user, PHP-FPM pool or Laravel Octane (FrankenPHP) since v5.0 , Nginx vhost, database (MariaDB by default; optional PostgreSQL since v4.8.0 ), Supervisor worker, crontab entry, Deployer zero-downtime releases, SSH deploy key, and auto-compiled .env . Custom apps are simpler — see custom apps for details. Laravel app (default — PHP-FPM) bash Copy Laravel Octane (FrankenPHP) bash Copy See Laravel Octane for requirements, convert, and how the vhost differs from FPM. Non-interactive (flags) bash Copy --user Linux username for the app. Must be unique, lowercase, alphanumeric. --domain Primary domain (e.g. myapp.com ). Used for Nginx vhost and APP_URL . --repository SSH Git URL (e.g. git@github.com:you/repo.git ). Must be SSH, not HTTPS. --branch Branch to deploy. Defaults to main . --php PHP version for this app (e.g. 8.5 ). Defaults to PHP 8.5. Must be an installed version — only 8.3 , 8.4 and 8.5 are accepted since v4.5.4 (Deployer 8 requires PHP ≥ 8.3). --octane / --octane=frankenphp Serve HTTP via Laravel Octane (FrankenPHP) instead of PHP-FPM (since v5.0 ). Laravel apps only — rejected with --custom . See Laravel Octane . --engine Database engine: mariadb (default) or pgsql (since v4.8.0; requires cipi db install pgsql ). Interactive create prompts when PostgreSQL is installed. Writes matching .env / connection URL. If a GitHub or GitLab token is configured, Cipi automatically adds the SSH deploy key and creates the webhook on the repository — no manual steps needed. See Git auto-setup for setup instructions and fallback options when auto-configuration is not available."},{"p":"apps","c":"Apps","id":"cli-app-octane","h":"Laravel Octane (FrankenPHP)","t":"Apps Laravel Octane (FrankenPHP) Apps Laravel Octane (FrankenPHP) Since v5.0 , Laravel apps can serve HTTP via Laravel Octane with the FrankenPHP server instead of a per-app PHP-FPM pool. Octane and classic FPM apps run side by side on the same Cipi server. What Cipi configures Allocates a localhost port in 8100–8999 and stores octane / octane_port in apps.json Nginx vhost uses proxy_pass to Octane and serves static files from current/public — no per-app FPM pool Supervisor program ${app}-octane alongside queue workers Deployer template laravel-octane.php restarts / reloads Octane on deploy .env : OCTANE_SERVER=frankenphp , OCTANE_HTTPS=true App requirements Your Laravel repository must include laravel/octane and run php artisan octane:install --server=frankenphp . Octane starts after the first successful deploy. bash Copy cipi app convert --to=octane|fpm rewrites the pool / vhost / Supervisor / Deployer template / .env , and re-applies SSL when a certificate is already present. Octane tip: keep long-running queue work on cipi worker or Horizon — Octane handles HTTP only. Pair with Reverb for WebSockets."},{"p":"apps","c":"Apps","id":"cli-app-custom","h":"cipi app create --custom","t":"Apps cipi app create --custom cipi app create --custom Creates a custom app with classic deploy (no zero-downtime): code is deployed into htdocs — no current / shared symlinks. Ideal for static sites, SPAs (Vue, React, Svelte), WordPress, other CMS, or any non-Laravel PHP project or framework. During creation you only choose the document root (default / , or e.g. www , dist , public ). Nginx is pre-configured with index index.html index.php , try_files $uri $uri/ /index.php?$args , and error_page 404 /404.html — no prompts for try_files or entry point. Git optional (SFTP-only) The Git repository is optional for custom apps. Laravel apps still require a repository. If you skip the repository for a custom app, Cipi creates /home/<app>/htdocs with a placeholder index.html and does not configure a deploy key or webhook — you upload files with SFTP (or SCP/rsync) to ~/htdocs as the app user. The onboarding output explains this “no repo — SFTP only” workflow. If you do provide a repository, behavior is unchanged: use cipi deploy <app> to pull code into htdocs . What’s included and what you add Custom apps have no database , no .env , no cron, and no queue workers. When a repository is configured, a deploy key and (with Git auto-setup) webhook are shown; for SFTP-only apps, those are omitted. The post-creation summary lists SSH access and next steps accordingly. If your custom app needs a database (e.g. WordPress, Drupal), create one with cipi db create --name=<app> after deployment. See cipi db for backup, restore, and password management. With Git — non-interactive example: bash Copy SFTP-only — omit --repository and --branch : bash Copy With a repository, use cipi deploy <app> to deploy; code is cloned into /home/<app>/htdocs ."},{"p":"apps","c":"Apps","id":"cli-app-manage","h":"app list / app show / app edit / app delete","t":"Apps app list / app show / app edit / app delete app list / app show / app edit / app delete Command Description cipi app list List all apps with domain, PHP version, and status ( (suspended) when offline) cipi app show <app> Full details: domain, PHP, deploy key, workers, webhook, suspend state. For custom apps: type \"Custom\", docroot; webhook (and deploy key) omitted when SFTP-only without a repository. cipi app edit <app> --php=8.5 Hot-swap PHP version. Updates FPM pool, Nginx socket, Supervisor, crontab, Deployer config, and .env — zero downtime cipi app edit <app> --branch=develop Change the deploy branch cipi app edit <app> --domain=new.example.com Rename the primary domain (since v4.6.2 ). Validates format and uniqueness, moves the old primary to aliases, regenerates the Nginx vhost, updates APP_URL , refreshes Git webhooks when auto-configured, and re-issues Let's Encrypt when a certificate already existed cipi app edit <app> --repository=<SSH-URL> Attach or change the Git repository (e.g. enable deploy on a custom SFTP-only app created without --repository ). Composable with --branch , --php , and --domain cipi app edit <app> --node-build='npm ci && npm run build' Run a Node build after Composer vendors on every deploy (since v5.0 ). Deployer executes .deployer/node-build.sh (fail-closed; command is validated). Clear with --no-node-build cipi app edit <app> --predeploy-snapshot Enable a database dump before every deploy (since v5.0 ). Same behaviour as passing --snapshot on each cipi deploy . See Pre-deploy DB snapshots cipi app convert <app> --to=octane|fpm Convert between PHP-FPM and Laravel Octane (since v5.0 ). See Laravel Octane cipi app env <app> [--show|--get|--set|--unset] Open the app's .env in nano, or manage keys non-interactively since v5.0.3 ( --show / --get / --set / --unset , optional --json ). Exits with error for custom apps (no .env). cipi app run <app> <cmd> [args…] Whitelisted non-interactive command as the app user (since v5.0.3 ). See app run cipi app deploy-config <app> Structured Deployer recipe options (since v5.0.3 ). See deploy-config cipi app webhook recreate <app> [--rotate-secret] Recreate the GitHub/GitLab deploy webhook; optional secret rotation updates CIPI_WEBHOOK_TOKEN (since v5.0.6 ). REST: POST /api/apps/{name}/webhook/recreate (API 1.15.0+ ). Since 5.0.6 , app edit --repository= only recreates webhook/deploy key when the repository URL actually changes. cipi app reset-password <app> Regenerate the app's Linux user SSH password. The new password is displayed on screen — save it immediately cipi app reset-db-password <app> Regenerate the app's database password (MariaDB or PostgreSQL per app engine ) and automatically update DB_PASSWORD in the app's .env . Exits with error for custom apps (no database). cipi app delete <app> Permanently remove the app, user, database (if Laravel), Nginx vhost, FPM pool, and Supervisor workers. For custom apps, skips database drop (none was created). Asks for confirmation. cipi app delete <app> --force Same as delete but skips the confirmation prompt — for scripts, the panel API, and cipi-cli"},{"p":"apps","c":"Apps","id":"cli-app-reverb","h":"cipi app reverb","t":"Apps cipi app reverb cipi app reverb Laravel Reverb is Laravel's first-party WebSocket server. Cipi has run it since v5.0 ; v5.1.2 is the release that made it work in production — generated credentials, a scheme derived from the certificate, raised connection limits, and a proxy rule that no longer swallows your own routes. Enabling Reverb on a Laravel app wires four things: 127.0.0.1:9000–9099 A free localhost port, allocated per app and recorded in apps.json . Reverb never listens on a public interface. ${app}-reverb A Supervisor program running artisan reverb:start as the app user, logging to /home/<app>/logs/reverb.log . nginx A proxy block on the app's own domain for /app/{key} (the WebSocket handshake) and /apps/{id}/… (the HTTP API the application broadcasts through). .env REVERB_* and VITE_REVERB_* , generated on first enable and never rotated afterwards. bash Copy --restart-supervisor Restart Supervisor as part of enable , so the raised file-descriptor limit applies immediately. This restarts every queue worker on the server , which is why Cipi never does it on its own. Reverb is Laravel only — enable refuses a --custom app. Running enable again on an app that already has Reverb is safe and useful: it does not reallocate the port or touch the credentials, it re-derives the public host and scheme and fills in any .env key that went missing. Enabling it end to end bash Copy Order matters only in that the Node build has to run after reverb enable : Vite bakes VITE_REVERB_* into the bundle at build time. Cipi's node_build step runs after Deployer has linked the shared .env , so a deploy that follows enable produces a frontend that can find the socket. Change the domain or install a certificate later and you need another deploy for the bundle to catch up. What Cipi writes in .env env Before 5.1.2 the three credentials were left blank for you to invent — and an empty REVERB_APP_KEY is exactly what stops reverb:start from booting. They are now generated on the first enable and an existing non-empty value always wins, so an app that arrived with its own credentials keeps them. Cipi never rotates them: a new key would disconnect every live client and invalidate any frontend bundle already built against the old one. Connecting from the frontend The stock resources/js/echo.js from install:broadcasting already reads exactly the variables Cipi writes — nothing to change: js Copy Because nginx fronts Reverb on the app's own domain and port, there is no extra port to open in the firewall and no second certificate to manage. cipi app reverb status prints the exact URL a client ends up dialling. /app and /apps belong to the protocol. Up to 5.1.1 the proxy was written as location /app , which in nginx is a prefix match, not a path — so on every app with Reverb enabled, /appointments , /apple and /application/… were proxied into Reverb instead of reaching Laravel. Since 5.1.2 the block is the regex ~ ^/apps?(/|$) , which matches only /app/{key} and /apps/{id}/… . An app that has its own /app or /apps route still cannot use Reverb behind the same domain — put it on a dedicated subdomain instead. ws:// or wss:// Cipi derives the public host, port and scheme from what the server actually serves rather than assuming HTTPS: no certificate http on port 80 — the app is served over HTTP, so the socket is ws:// . certificate present https on port 443 — wss:// . wildcard domain *.example.com resolves to a concrete host ( www.example.com ), because no browser can open a socket to a wildcard. A multi-tenant frontend has to pick its own host at runtime. Installing a certificate later — HTTP-01 or DNS-01 — moves the app's Reverb settings to wss:// on the spot, and a domain change re-derives them too. Before 5.1.2 these values were hardcoded to https /443 at enable time, which on an app without a certificate meant a browser silently refusing the connection as mixed content, with nothing in the Reverb log to explain it. reverb status now warns when the"},{"p":"apps","c":"Apps","id":"cli-app-clone","h":"cipi app clone","t":"Apps cipi app clone cipi app clone Since v5.0 , clone an existing Laravel app into a new staging (or review) app with its own domain. Sets cloned_from in apps.json ; does not copy webhook or git IDs. If the source has Reverb, the clone gets its own port, credentials and public host (since v5.1.2 ) — it no longer inherits the production socket. See cipi app reverb . bash Copy --domain Required. Primary domain for the new app. --name Optional Linux username for the clone. --branch Deploy branch for the clone (defaults to the source app's branch). --with-db Create a fresh database for the clone. --no-db Skip database provisioning for the clone."},{"p":"apps","c":"Apps","id":"cli-app-limits","h":"cipi app limits","t":"Apps cipi app limits cipi app limits Since v5.0 , set per-app resource limits with hard caps enforced by Cipi. bash Copy --fpm-max-children PHP-FPM pm.max_children for FPM apps. --memory-limit PHP memory_limit . --octane-workers Octane worker count for FrankenPHP apps. --worker-procs Supervisor queue worker process count."},{"p":"apps","c":"Apps","id":"cli-app-suspend","h":"app suspend / app unsuspend","t":"Apps app suspend / app unsuspend app suspend / app unsuspend Available since v4.5.8 . Take an app offline without deleting it — useful for billing holds, maintenance windows, or staging sites you want fully dark. Suspending swaps the Nginx vhost for a static HTTP 503 page served from /var/www/cipi-suspended/ . bash Copy Command Description cipi app suspend <app> Sets suspended: true in apps.json , rebuilds the vhost to return 503 for all requests. Idempotent if already suspended. cipi app unsuspend <app> Clears the flag, restores the normal Laravel/custom vhost, and reapplies SSL blocks. Idempotent if already online. Behaviour HTTPS included — certbot clones the suspension vhost into the :443 block, so HTTPS also shows the offline page Let's Encrypt still works — the /.well-known/acme-challenge/ path stays public so certificates can be issued or renewed while suspended Survives vhost regeneration — alias changes, PHP edits, and SSL installs respect the suspended flag Visible in listings — cipi app list marks suspended apps; cipi domains appends ⏸ suspended on each row Suspend vs basic auth: cipi basicauth shows a login prompt but still runs your app. app suspend stops PHP entirely and serves a static offline page — visitors never reach Laravel. Use suspend for “site closed”; basic auth for “invite-only preview”. Also available via the REST API ( POST /api/apps/{name}/suspend ), cipi-cli ( apps suspend ), and the WHMCS module (Suspend / Unsuspend buttons). Requires token ability apps-suspend for API access."},{"p":"apps","c":"Apps","id":"cli-basicauth","h":"cipi basicauth","t":"Apps cipi basicauth cipi basicauth Available since v4.5.2 . Protect any app — Laravel or custom — behind an Nginx username/password prompt. Useful for staging sites, internal tools, or apps that aren't ready for public traffic yet. Command Description cipi basicauth enable <app> [--user=NAME] [--password=PASS] Turn on HTTP basic auth. Credentials are generated when omitted and shown once on screen — save them immediately cipi basicauth disable <app> Remove the prompt and clear stored credentials cipi basicauth status <app> Show whether basic auth is enabled and the configured user Credentials are hashed with openssl passwd -apr1 (no apache2-utils needed) and stored in /etc/nginx/cipi-basicauth/<app>.htpasswd ; the enabled state lives in apps.json . The auth_basic directives are injected per location block, so protection survives vhost regeneration (alias changes, PHP edits) and is cloned into the :443 block by certbot — HTTPS is covered too . ACME challenges stay public, so certificate issuance and renewal are never blocked. Basic auth is removed automatically on cipi app delete . This is distinct from cipi auth , which manages the Composer auth.json for private package repositories."},{"p":"apps","c":"Apps","id":"cli-app-env","h":"Managing ENV variables","t":"Apps Managing ENV variables Managing ENV variables Every Laravel app has a single .env file living at /home/<app>/shared/.env . Custom apps have no .env . It is created and pre-populated by Cipi during app create with the database credentials, APP_KEY , APP_URL , cache/session/queue settings, and the webhook token. The shared/ directory is symlinked into every release, so the same .env is always active regardless of which release is current. Edit interactively via CLI The safest way to change ENV values is through Cipi itself — it opens the file in nano as the app user, with the correct permissions: bash Copy Save with Ctrl+O then exit with Ctrl+X . Changes take effect immediately for new requests — no restart needed for most values. If you change queue connection or cache driver, restart the workers: bash Copy Non-interactive flags (v5.0.3+) Since v5.0.3 , scripts, the panel API, and the web GUI can manage .env without opening an editor: bash Copy REST: GET|PUT /api/apps/{name}/env (ability apps-env , API 1.14+ ). MCP: AppEnvShow , AppEnvUpdate . Edit directly via SSH You can also edit the file directly over SSH as root or as the app user: bash Copy Key ENV variables set by Cipi Variable Description Set by APP_KEY Laravel encryption key — generated once at app creation Cipi APP_URL Updated automatically by cipi ssl install Cipi DB_CONNECTION mysql for MariaDB (drop-in compatible), or pgsql when the app engine is PostgreSQL (v4.8.0+) Cipi DB_DATABASE / DB_USERNAME / DB_PASSWORD Auto-generated credentials for the app's isolated database Cipi CACHE_STORE database — uses the app's database Cipi SESSION_DRIVER database Cipi QUEUE_CONNECTION database Cipi BROADCAST_CONNECTION Set to reverb on enable; restored to the previous driver (or log ) on disable (v5.1.2+) Cipi REVERB_APP_ID / KEY / SECRET Generated once on the first cipi app reverb enable , then never rotated. An existing non-empty value always wins. Cipi (v5.1.2+) REVERB_HOST / PORT / SCHEME Public socket: derived from the certificate ( https /443 or http /80). A wildcard domain resolves to a concrete host. Cipi (v5.1.2+) VITE_REVERB_APP_KEY / HOST / PORT / SCHEME Copies of the public Reverb values, baked into the frontend by Vite at build time. Redeploy after enable, a domain change or SSL. Cipi (v5.1.2+) REVERB_SERVER_HOST / PORT Localhost bind for the Supervisor program. Removed on disable; the credentials stay. Cipi CIPI_WEBHOOK_TOKEN HMAC secret for cipi-agent webhook validation Cipi CIPI_APP_USER Linux username owning this app Cipi CIPI_MCP Enable or disable the built-in MCP server at /cipi/mcp User ( true by default) Do not change DB credentials manually. If you need to regenerate the database password use cipi db password myapp (or cipi app reset-db-password myapp ) — it updates both the engine and the .env atomically. Editing them by hand risks leaving the two out of sync. Adding your own variables Add any custom variable at the bottom of the file as you normally would in a Laravel project. They are preserved across deploys because the .env lives in shared/ and is never overwritten by Deployer. env"},{"p":"apps","c":"Apps","id":"cli-app-run","h":"cipi app run","t":"Apps cipi app run cipi app run Since v5.0.3 , run a whitelisted non-interactive command as the app user — useful from the CLI, panel API, and web GUI “App commands”. Editors, pagers, shells, and REPLs are blocked ( nano , vim , less , bash , tinker , …). Interactive flags (e.g. tail -f , php -a ) are rejected. bash Copy Allowed binaries include composer , npm / npx / yarn / pnpm , ls / ll , cat / head / tail , filesystem helpers, archives, git , php , node , and find . REST: POST /api/apps/{name}/run (async job, ability apps-run ) and GET /api/run-commands (API 1.14+ ). MCP: AppRun , AppRunCommands . v5.0.4+ fixed /usr/bin/env: '--': No such file or directory — every whitelisted command (including GUI App commands) failed before that patch. Run cipi self-update ."},{"p":"apps","c":"Apps","id":"cli-app-logs","h":"cipi app logs","t":"Apps cipi app logs cipi app logs Tail application logs in real-time. Logs are rotated daily and kept for 14 days. By default, all logs are shown including Laravel daily logs ( laravel-YYYY-MM-DD.log ) from shared/storage/logs/ . bash Copy --type=nginx Nginx access and error logs --type=php PHP-FPM error log --type=worker Supervisor / queue worker output --type=deploy Deployer output — full deploy history with timestamps --type=laravel Laravel application logs from shared/storage/logs/ (daily rotating laravel-YYYY-MM-DD.log )"},{"p":"apps","c":"Apps","id":"cli-app-artisan","h":"app artisan & app tinker","t":"Apps app artisan & app tinker app artisan & app tinker Run Artisan commands and Tinker as the app user with the correct PHP version and open_basedir context — exactly as they would run during a deploy. bash Copy"},{"p":"apps","c":"Apps","id":"cli-app-ssh","h":"SSH as the app user","t":"Apps SSH as the app user SSH as the app user Each app runs under its own isolated Linux user. Sometimes you need to work directly inside that user's environment — inspect files, run one-off scripts, or debug something that only reproduces as the correct user. Direct SSH as app user (recommended) App users can SSH directly to the server with the password generated at app creation: bash Copy The password is shown when the app is created (or use cipi app reset-password myapp to regenerate it). This works for SFTP clients, IDE remote sessions, and terminal access. Via cipi (admin path) If you are already connected as cipi , you can switch directly to any app user: bash Copy Reset the app user password If you need to regenerate an app user's password (e.g. for direct SSH or SFTP), use: bash Copy The new password is displayed on screen — save it immediately. Useful commands once logged in as the app user bash Copy Every app user's .bashrc defines a handy ll='ls -al' alias (since v4.5.5 ) for quicker directory listings over SSH, alongside the deploy and composer shortcuts. Apps created before 4.5.5 receive the ll alias automatically on the next cipi self-update via the 4.5.5 migration . The app user's open_basedir restriction limits PHP to /home/myapp . This is enforced at the PHP-FPM level, not at the shell level — you can access any file your shell user can read when working in the terminal."},{"p":"cli-client","c":"CLI Client","id":"cli-install","h":"Installing cipi-cli","t":"CLI Client Installing cipi-cli CLI Client Installing cipi-cli cipi-cli is a standalone Go binary that talks to the Cipi REST API from your local machine. No SSH required — manage apps, databases, SSL certificates, and deployments from any terminal. Binaries are available for Linux and macOS (amd64 and arm64). The Cipi server must have the API package installed and configured before using cipi-cli . See cipi api for setup instructions. Download binary Download the latest release for your platform from the Releases page , then: bash Copy Build from source bash Copy Verify installation bash Copy"},{"p":"cli-client","c":"CLI Client","id":"cli-configure","h":"Configuration","t":"CLI Client Configuration CLI Client Configuration A profile is a named connection to one Cipi server (API endpoint + token). Use as many profiles as you have servers — for example prod , staging , or client-a . You need the API endpoint URL and a Sanctum token created with cipi api token create on each server. Credentials are stored per profile in ~/.cipi/config.json with permissions 0600 . Always pass a profile name — if you omit it, the CLI prompts for one instead of silently writing to default . Interactive setup Recommended: store the token with api token add . Equivalent helpers are configure --profile and profiles add : bash Copy Non-interactive setup bash Copy Show configuration bash Copy"},{"p":"cli-client","c":"CLI Client","id":"cli-profiles","h":"profiles","t":"CLI Client profiles CLI Client profiles Manage multiple server connections from a single cipi-cli installation. One profile equals one server. Prefix any command with a profile name to target that server, or set a default with profiles use to omit the prefix. The aliases servers / server work the same as profiles . Targeting a server bash Copy With a default server set, you can run commands without the prefix: bash Copy Command Description cipi-cli api token add [profile] Add or update API endpoint + token for a named server profile cipi-cli profiles List configured servers (alias: cipi-cli servers ) cipi-cli profiles add [name] [flags] Add or update a server profile (same as configure --profile ) cipi-cli profiles list List configured servers cipi-cli profiles show [profile] Show one server profile, or all if omitted cipi-cli profiles use <profile> Set the default server (alias: profiles default ) cipi-cli profiles delete <profile> [-y] Delete a local server profile (credentials only — nothing changes on the remote server) api token add , profiles add , and configure accept the same flags: --endpoint and --token (plus --profile when the name is not positional). Aliases: servers / server → profiles ; profiles use → profiles default . The same management commands are also available under cipi-cli configure list , configure show , configure default , and configure delete — prefer profiles or api token add for storing credentials."},{"p":"cli-client","c":"CLI Client","id":"cli-apps","h":"apps","t":"CLI Client apps CLI Client apps Manage applications on your Cipi server. Command Description cipi-cli apps list List all applications cipi-cli apps show <name> Show application details cipi-cli apps create [flags] Create a new application cipi-cli apps edit <name> [flags] Edit an application cipi-cli apps delete <name> [-y] Delete an application cipi-cli apps suspend <name> Take an app offline (HTTP 503 maintenance page) without deleting it cipi-cli apps unsuspend <name> Restore a suspended app to normal service cipi-cli apps logs <name> [flags] Read paginated application logs (nginx, PHP-FPM, Laravel, worker, deploy) Create flags --user Application name (used as Linux user and database name) --domain Primary domain for the application --php PHP version (e.g. 8.5 ) --repository Git repository URL --branch Git branch to deploy from --custom Create a custom (non-Laravel) app with htdocs deploy --docroot Document root relative to htdocs (custom apps only) Edit flags --php Change PHP version --repository Update Git repository URL --branch Change deploy branch --domain Rename the primary domain (requires Cipi 4.6.2+ and API 1.9.0+ ) Logs flags --type Log type filter: all (default), nginx , php , worker , deploy , or laravel --page Page number, starting at 1 for the most recent lines (default 1 ) --per-page Lines per log file per page (default 50 , max 1000 ; requires API 1.11.9+ ) Examples bash Copy"},{"p":"cli-client","c":"CLI Client","id":"cli-domains","h":"domains","t":"CLI Client domains CLI Client domains List every primary domain and alias across all apps on the server in a single table — the remote counterpart to cipi domains on the server. Useful for auditing DNS coverage or spotting domains missing SSL before a certificate renewal. Command Description cipi-cli domains List every domain and alias across all apps bash Copy The global domain map is built from GET /api/apps on the server. It requires Cipi 4.5.5+ on the server (for the underlying cipi domains data) but no minimum API package version."},{"p":"cli-client","c":"CLI Client","id":"cli-deploy","h":"deploy","t":"CLI Client deploy CLI Client deploy Trigger deployments, rollback to the previous release, or unlock a stuck deployment. Command Description cipi-cli deploy <app> Trigger a zero-downtime deployment cipi-cli deploy rollback <app> [-y] Rollback to previous release cipi-cli deploy unlock <app> Unlock a stuck deployment bash Copy"},{"p":"cli-client","c":"CLI Client","id":"cli-ssl","h":"ssl","t":"CLI Client ssl CLI Client ssl Install Let's Encrypt certificates for your applications. Command Description cipi-cli ssl install <app> Install Let's Encrypt certificate (covers primary domain and all aliases) bash Copy"},{"p":"cli-client","c":"CLI Client","id":"cli-aliases","h":"aliases","t":"CLI Client aliases CLI Client aliases Manage domain aliases for an application. Command Description cipi-cli aliases list <app> List all aliases for an app cipi-cli aliases add <app> <domain> Add a domain alias cipi-cli aliases remove <app> <domain> [-y] Remove a domain alias bash Copy"},{"p":"cli-client","c":"CLI Client","id":"cli-db","h":"db","t":"CLI Client db CLI Client db Manage MariaDB databases. Command Description cipi-cli db list List all databases cipi-cli db create <name> Create a database cipi-cli db delete <name> [-y] Delete a database cipi-cli db backup <name> Create a database backup cipi-cli db restore <name> [-y] Restore database from backup cipi-cli db password <name> [-y] Regenerate password and update .env bash Copy"},{"p":"cli-client","c":"CLI Client","id":"cli-status","h":"status","t":"CLI Client status CLI Client status Read the same host snapshot as cipi status on the server — via GET /api/status . Bare status shows a global overview (one row per configured server). Pass a profile name (or use a profile prefix) for full details. Command Description cipi-cli status Global overview — one row per server (alias: status all ) cipi-cli status <profile> Full details for one server cipi-cli <profile> status Same detail view, via profile prefix Global columns: NAME , IP , CPU , RAM , HDD , APPS , SVC , CIPI . The default profile is marked with * . bash Copy Requires the API token ability status-view and GET /api/status on the server (API 1.11.6+ ). Create or rotate the token with cipi api token create and include status-view ."},{"p":"cli-client","c":"CLI Client","id":"cli-jobs","h":"jobs","t":"CLI Client jobs CLI Client jobs Write operations (create, edit, delete, deploy, SSL, etc.) are asynchronous on the Cipi API. The CLI automatically polls for job completion and displays a spinner while waiting. If you prefer to handle polling manually, use the jobs commands. Command Description cipi-cli jobs show <id> Show job status cipi-cli jobs wait <id> Wait for a job to complete (blocking) bash Copy"},{"p":"cli-client","c":"CLI Client","id":"cli-update","h":"update","t":"CLI Client update CLI Client update Update cipi-cli to the newest release from GitHub. The command picks the highest semver among published releases (not GitHub’s “latest” flag, which can point at an older tag), downloads the matching binary for your platform, verifies its SHA-256 checksum, and replaces the running binary in place. Aliases: self-update , upgrade . Command Description cipi-cli update Update to the highest published semver cipi-cli update --force Reinstall even if already up to date (allows downgrade) bash Copy"},{"p":"cli-client","c":"CLI Client","id":"cli-completion","h":"completion","t":"CLI Client completion CLI Client completion Install shell autocompletion for cipi-cli . The recommended path is completion install , which detects your shell, writes the completion script, and hooks it into your rc file (idempotent). Command Description cipi-cli completion install Auto-detect shell and install completion cipi-cli completion install --shell zsh Install for a specific shell ( zsh , bash , or fish ) cipi-cli completion zsh Print the zsh completion script to stdout cipi-cli completion bash Print the bash completion script to stdout cipi-cli completion fish Print the fish completion script to stdout bash Copy For zsh and bash, the script is written under ~/.cipi/completions/ and a source line is appended to your shell rc file. For fish, the script goes to ~/.config/fish/completions/ (auto-loaded). Reload the shell afterwards, then try cipi-cli <TAB> ."},{"p":"cli-client","c":"CLI Client","id":"cli-global-flags","h":"Global Flags","t":"CLI Client Global Flags CLI Client Global Flags These flags are available on all commands. --json Output in JSON format — useful for scripting and CI/CD pipelines --no-color Disable colored output --help Show help for any command bash Copy Releases Releases are automated via GitHub Actions. Pre-built binaries for Linux (amd64/arm64) and macOS (amd64/arm64) are published with SHA-256 checksums on the GitHub Releases page . Source code cipi-cli is open source, MIT licensed, and written in Go. Contributions welcome on GitHub ."},{"p":"cli-client","c":"CLI Client","id":"cli-requirements","h":"Requirements","t":"CLI Client Requirements CLI Client Requirements The Cipi server must have the API package installed and configured before using cipi-cli : bash Copy New app PHP versions must be 8.3 , 8.4 , or 8.5 (Cipi 4.5.4+). Some CLI features depend on specific server and API versions: Feature Minimum Cipi Minimum API Suspend / unsuspend ( apps suspend ) 4.5.8 1.8.1 Rename primary domain ( apps edit --domain ) 4.6.2 1.9.0 App logs ( apps logs ) — 1.11.9 Server status ( status ) — GET /api/status + status-view (1.11.6+) Global domain map ( domains ) 4.5.5 — (built from /api/apps )"},{"p":"deploy","c":"Deploy & CI/CD","id":"cli-deploy","h":"cipi deploy","t":"Deploy & CI/CD cipi deploy Deploy & CI/CD cipi deploy Cipi uses Deployer for all deployments. Every deploy is atomic: a new release directory is prepared fully before the current symlink is swapped, so traffic is never interrupted. Since v4.5.4 Cipi bundles Deployer 8 , which requires PHP ≥ 8.3 . cipi deploy and cipi deploy --rollback abort with a clear upgrade message before invoking Deployer when an app is still pinned to an older PHP version — switch it with cipi app edit <app> --php=8.3 (or higher) first. Deploy pipeline Deployer and Composer run with the app's configured PHP version (e.g. /usr/bin/php8.5 ), not the system default. This applies to cipi deploy , cipi deploy --rollback , crontab deploy triggers, cipi sync import deploys, and the deploy / composer aliases in the app user's .bashrc . Stop queue workers ( cipi worker stop ) Clone repo into releases/N/ Run composer install --no-dev (with app's PHP) Link shared/.env and shared/storage/ Run artisan migrate --force Run artisan optimize Run artisan storage:link Swap current symlink atomically Restart queue workers Prune old releases (keep last 5) bash Copy Knowing whether a deploy worked (v5.1.0+) Up to 5.0.x a failing deploy could pass in silence: the failure branch, its warnings, the rollback hint and the deploy_fail email were unreachable code, because a non-zero exit from Deployer killed the cipi process on the spot. Since v5.1.0 : Both paths email on success and on failure — the CLI and the automatic Git webhook alike. Subjects are visibly different ( Cipi deploy succeeded: myapp release 55 … / Cipi deploy FAILED: myapp … ), and the body names the branch, the release number, the commit hash and subject, its author and date, the duration, the previous release, the deploy log path and the post-deploy healthcheck verdict. cipi deploy exits non-zero when the deploy failed , so CI and webhooks can see it. The same applies to cipi deploy --rollback . The success email is sent after post-deploy verification , so it can never announce a successful deploy while the site is returning 500. Reading the deploy log /home/<app>/logs/deploy.log used to be raw Deployer output appended forever, which made a deploy that failed overnight unreadable afterwards. Since v5.1.0 every line is timestamped and each run is bracketed by a banner naming the trigger (CLI or webhook), the branch, the release and the duration. bash Copy Release directories stay numeric — rollback depends on that ordering — so --releases adds the human detail on top rather than renaming anything. Since v5.0 , opt-in pre-deploy DB snapshots are available via --snapshot / --snapshot-required or a permanent app setting — see Pre-deploy DB snapshots . Octane apps use the laravel-octane.php Deployer template (reload/restart Octane on deploy); enable Node builds with cipi app edit <app> --node-build='…' . If a deploy is interrupted (e.g. by a network error), Deployer may leave a lock file behind. Use cipi deploy myapp --unlock to remove it before re-deploying."},{"p":"deploy","c":"Deploy & CI/CD","id":"predeploy-snapshot","h":"Pre-deploy DB snapshots","t":"Deploy & CI/CD Pre-deploy DB snapshots Pre-deploy DB snapshots Since v5.0 , cipi deploy <app> can dump the database before the release pipeline runs. Snapshots land under /var/log/cipi/backups/ — the same path used by cipi db backup . bash Copy What happens Before Deployer starts, Cipi takes a DB dump into /var/log/cipi/backups/ . With --snapshot-required , a failed dump (or a missing DB engine) blocks the deploy. With --snapshot alone, a failed dump prints a warning and the deploy continues . --snapshot Opt-in dump before deploy; warn and continue if the snapshot cannot be taken. --snapshot-required Same dump, but fail the deploy when the snapshot (or engine) is unavailable. Enable on every deploy Turn it on permanently for an app so every deploy (CLI, webhook, or pipeline) takes a snapshot first: bash Copy cipi deploy --rollback restores the previous code release only. It does not restore the database. If you need the pre-deploy dump back, use cipi db restore . For pipeline workflows that also archive shared/ to S3 before release, see Safe deploy — backup before release ."},{"p":"deploy","c":"Deploy & CI/CD","id":"cli-deploy-config","h":"cipi app deploy-config","t":"Deploy & CI/CD cipi app deploy-config cipi app deploy-config Since v5.0.3 , manage durable Deployer recipe options stored in apps.json and applied by regenerating deploy.php from the template — a safe alternative to editing free-form PHP. bash Copy REST: GET|PUT /api/apps/{name}/deploy-config (ability apps-deploy-config , API 1.14+ / Cipi 5.0.3+ ). MCP: AppDeployConfigShow , AppDeployConfigUpdate ."},{"p":"deploy","c":"Deploy & CI/CD","id":"cli-yml","h":"cipi.yml — configuration that travels with the code","t":"Deploy & CI/CD cipi.yml — configuration that travels with the code cipi.yml — configuration that travels with the code Available since v5.1.0 . An app can carry a cipi.yml file in its repository describing the state it expects: domain aliases , PHP version and settings , extra databases , queue workers or Horizon , Reverb , the scheduler , its healthcheck and its backup strategy . The file lives next to the code, so server configuration is reviewed, versioned and shipped like everything else. Commands bash Copy The file is looked up in current/cipi.yml , then current/cipi.yaml , then shared/cipi.yml — override with --file=<path> . Start from what the server already has You do not have to write it by hand. cipi yml generate <app> prints the app's configuration as it stands on the server — aliases, PHP version and per-app settings, its extra databases, its queue workers (read back out of Supervisor), Horizon, Reverb (since v5.1.2 ), the scheduler and the backup profiles it owns — as a ready-to-commit file. Declaring horizon: false or reverb: false is now a real plan (also 5.1.2): previously false was treated like a missing key. bash Copy Cron expressions come back as the friendlier every: 30m form where they map cleanly, values are quoted wherever a plain scalar would be misread, and the result is fed through the validator before printing. Server-wide backup profiles and the app's own database are deliberately left out — those stay yours. The file yaml Copy cipi yml example takes an optional app name — cipi yml example myapp — so the placeholder databases and profiles land inside that app's namespace and the template validates as-is. Deploys ignore the file until you opt in Nothing happens on deploy until you run cipi yml auto <app> on . With that opt-in given, every successful deploy reconciles — from both cipi deploy and the Git webhook, the latter through one narrowly scoped sudoers rule. A release that carries no cipi.yml is a quiet no-op, and a file that fails validation is reported by email ( yml_fail ) and never partially applied . A successful reconcile fires yml_apply . Why it is safe to accept over Git The file arrives from a repository, so anyone who can commit controls its contents. It is therefore fail-closed throughout: It can only configure an app that already exists — never create, rename or delete one. Its databases must be named <app> or <app>_* , and its backup profiles <app> or <app>-* . Its healthcheck URL must resolve to one of the app's own domains — otherwise a commit could aim the server's five-minute prober at an internal address and read the answer back out of the alert emails. Unknown keys are errors, and no field carries a shell command or a path to include. The parser implements a deliberately small YAML subset and refuses anchors, aliases, tags, merge keys, block scalars and flow mappings outright. Once yml auto is on, anyone who can push to that repository can change the app's aliases, PHP settings, workers, healthcheck and backup profiles. That is the point of configuration-as-code — treat write access to the repo accordingly."},{"p":"deploy","c":"Deploy & CI/CD","id":"cli-auth","h":"auth.json","t":"Deploy & CI/CD auth.json auth.json Manage the auth.json file for an app. This file lives at /home/<app>/shared/auth.json and is automatically symlinked into every release by Deployer — exactly like .env . Use it to store structured credential data (e.g. API keys, feature flags, or any JSON payload) that your Laravel app can read at runtime. bash Copy REST: GET|POST|PUT|DELETE /api/apps/{name}/auth (ability apps-auth , API 1.14+ ) — Composer/structured JSON, distinct from HTTP Basic Auth. MCP: AppAuthJsonShow , AppAuthJsonCreate , AppAuthJsonUpdate , AppAuthJsonDelete . Command details Command Description cipi auth create <app> Creates shared/auth.json with the initial structure {\"users\":[]} , sets permissions to 640 (owner app:app ), and adds auth.json to shared_files in the app's Deployer config so it is symlinked on every deploy. cipi auth edit <app> Opens shared/auth.json in $EDITOR (falls back to nano ). After the editor closes, validates the JSON with jq and warns if the file is malformed. cipi auth show <app> Prints the contents of shared/auth.json formatted with jq . cipi auth delete <app> Asks for confirmation, then deletes shared/auth.json and removes the auth.json entry from shared_files in the app's Deployer config. Deployer integration cipi auth create automatically appends auth.json to the shared_files list in /home/<app>/.deployer/deploy.php , and cipi auth delete removes it. This means the file is treated exactly like .env : it persists across releases and is never overwritten by a deploy. Every cipi auth operation is logged via log_action for auditability. The AUTH section is also listed in the output of cipi help ."},{"p":"deploy","c":"Deploy & CI/CD","id":"git-providers","h":"Git providers","t":"Deploy & CI/CD Git providers Git providers Cipi is ready to work with GitHub and GitLab but it supports any other Git provider that supports SSH deploy keys — no vendor lock-in. For self-hosted or custom Git servers, you need to trust the server's host fingerprint before Deployer can clone over SSH. Use the --trust-host flag to add the fingerprint to the app user's ~/.ssh/known_hosts automatically: bash Copy When a non-standard port is specified, Cipi also writes the Host / Port entry to the app user's ~/.ssh/config so that Deployer can reach the server without any extra configuration."},{"p":"deploy","c":"Deploy & CI/CD","id":"git-auto-setup","h":"Git auto-setup","t":"Deploy & CI/CD Git auto-setup Git auto-setup If you save a GitHub or GitLab Personal Access Token, Cipi automatically adds the SSH deploy key and creates the webhook on the repository every time you run cipi app create . No manual steps required. Save a token bash Copy GitHub token permissions Fine-grained tokens (recommended) need Administration and Webhooks set to Read and write on the target repositories. Classic tokens need the repo scope. GitLab token permissions The api scope is the minimum required — GitLab does not offer a more granular scope that covers both deploy keys and webhooks. Automatic lifecycle Event What Cipi does automatically app create Adds deploy key + creates webhook on the repository via API. The summary shows \"auto-configured ✓\" instead of manual instructions. app edit --repository=... Removes deploy key + webhook from the old repository, then adds them to the new one. app delete Removes deploy key + webhook from the repository before deleting the app. cipi git commands Command Description cipi git status Show provider connection status and per-app integration details (deploy key ID, webhook ID) cipi git github-token <token> Save a GitHub Personal Access Token cipi git gitlab-token <token> Save a GitLab Personal Access Token cipi git gitlab-url <url> Set the base URL for a self-hosted GitLab instance cipi git remove-github Remove the stored GitHub token cipi git remove-gitlab Remove the stored GitLab token and URL Manual setup (fallback) Auto-setup is skipped when no token is configured, when the API call fails (wrong permissions, repository not found, rate limit), or when the repository is hosted on a provider other than GitHub or GitLab (e.g. Gitea, Forgejo, Bitbucket). In all these cases Cipi falls back to the manual workflow and the app creation proceeds normally. To configure deploy key and webhook manually: bash Copy Then add them in your provider's repository settings: Deploy key — GitHub: Settings → Deploy keys → Add deploy key ; GitLab: Settings → Repository → Deploy keys Webhook — GitHub: Settings → Webhooks → Add webhook ; GitLab: Settings → Webhooks → Add new webhook . Set the payload URL and secret to the values shown by cipi deploy myapp --webhook If you remove a provider token after apps have been created with auto-setup, Cipi will not be able to clean up deploy keys and webhooks when you delete or edit those apps. A warning is shown and you will need to remove them manually from the provider's repository settings."},{"p":"deploy","c":"Deploy & CI/CD","id":"cli-deploy-script","h":"Customising the deploy script","t":"Deploy & CI/CD Customising the deploy script Customising the deploy script The deploy configuration for each app is stored at: /home/ myapp /.deployer/deploy.php This file is auto-generated by Cipi during app create and updated automatically when you change the PHP version or deploy branch via cipi app edit . You can edit it to customise the deploy pipeline, but you should understand the implications before doing so. Default deploy pipeline The auto-generated deploy.php runs these tasks in order: php Copy Adding custom tasks You can add tasks before or after any step. For a complete frontend build example ( npm install && npm run build ), see Building frontend assets below. Other common examples: php Copy Building frontend assets (npm / Vite) There is no dedicated cipi CLI flag for frontend builds (e.g. npm install && npm run build ). Customising deploy.php is the supported and expected approach — define a Deployer task() and hook it with after() or before() . You do not need to avoid this; extending the pipeline is exactly what the file is for. Cipi installs Node.js and npm on the server during setup. Verify they are available as the app user: bash Copy Commit package.json and package-lock.json to your repository. Hook the build after deploy:shared so .env is linked (Vite reads VITE_* variables from there) and before deploy:symlink so compiled assets exist in the release before it goes live. Append the block below at the bottom of /home/myapp/.deployer/deploy.php , below Cipi's auto-generated task definitions: php Copy This runs the equivalent of npm install && npm run build on every deploy. Prefer npm ci in production when package-lock.json is committed — it is faster and reproducible. Use npm install instead only if you do not lock dependencies. If you need separate install and build steps (e.g. to cache node_modules across releases), split them into two tasks: php Copy To speed up subsequent deploys, you can persist dependencies across releases by adding node_modules to Deployer's shared directories (optional — only if your project supports it): php Copy Edit the file on the server as the app user, then test with cipi deploy myapp : bash Copy Alternative: run npm ci && npm run build in GitHub Actions or GitLab CI before the SSH deploy step, so the server only receives pre-built assets. See CI/CD pipelines — SSH deploy . Running additional artisan commands php Copy Cipi may overwrite deploy.php when you run cipi app edit myapp --php=X or cipi app edit myapp --branch=X . Back up your customisations or keep them in a section clearly separated from the Cipi-managed blocks. A safe pattern is to put all custom tasks at the bottom of the file after the default task definition. Disabling a default step To skip a task — for example if you handle migrations manually — comment it out or remove it from the deploy task definition: php Copy Testing your changes After editing deploy.php , always do a test deploy before pushing to production: bash Copy The deploy log is always available at ~/logs/deploy.log or via cipi app logs myapp --type=deploy . Check it first when troubleshooting a failed deploy."},{"p":"deploy","c":"Deploy & CI/CD","id":"ci-overview","h":"Deploy & CI/CD — Overview","t":"Deploy & CI/CD Deploy & CI/CD — Overview Deploy & CI/CD — Overview With Cipi, CI (build and test) and CD (release to production) can be split or combined. Every deploy ultimately runs the same Deployer pipeline on the server — clone, composer install , migrations, symlink swap, worker restart. What changes is what triggers that pipeline. Cipi supports two trigger models. For most Laravel apps, start with the webhook + Cipi Agent path. Move to a full CI/CD pipeline when you need gates, backups, or infrastructure orchestration that a simple push hook cannot express. Two ways to trigger a deploy Webhook + Cipi Agent (recommended) CI/CD pipeline via SSH Trigger Git provider POSTs to /cipi/webhook on push GitHub Actions / GitLab CI job runs cipi deploy over SSH Server access from CI None — only HTTPS to your app domain Dedicated SSH key stored as a CI secret Pre-deploy tests Run locally or in a separate CI job; deploy still fires on push unless you disable the webhook Native — deploy step runs only after needs: test (or equivalent) passes Backup before release Manual or cron on the server Pipeline job — see safe deploy Preview / review apps Not supported out of the box Pipeline creates per-branch Cipi apps — see preview environments Setup complexity Low — composer require cipi/agent + one webhook Medium — SSH key, secrets, workflow YAML Which approach should I use? Use case Recommended approach Where to read more Single Laravel app, push-to-deploy on main Webhook + Agent Webhook setup Deploy only if CI tests pass Pipeline SSH (disable production webhook) Pipeline SSH deploy DB + file backup before every production release Pipeline SSH Safe deploy w/ backup Slack / Telegram alerts on deploy outcome Pipeline SSH Deploy notifications Ephemeral URL per feature branch (review apps) Pipeline SSH Preview environments Deploy multiple apps on one server from one repo Either — webhook per app, or one pipeline with parallel cipi deploy Multi-app deploy Pick one trigger per app. Do not leave a production webhook active while also running pipeline deploys on push — two concurrent Deployer runs conflict on the lock file. Use cipi deploy myapp --unlock if a stuck lock is left behind."},{"p":"deploy","c":"Deploy & CI/CD","id":"ci-webhook","h":"Automatic deploys — Cipi Agent & webhook","t":"Deploy & CI/CD Automatic deploys — Cipi Agent & webhook Automatic deploys — Cipi Agent & webhook cipi-agent ( cipi/agent ) is a Laravel package that exposes POST /cipi/webhook inside your running application. When GitHub or GitLab sends a push event, the agent validates the payload signature, acknowledges immediately, and queues a deploy on the server — no SSH from the CI runner, no sudo , no open inbound ports beyond HTTPS. How the webhook flow works The design separates fast HTTP acknowledgement from slow Deployer work . A deploy can take several minutes; Git providers time out webhook HTTP calls after ~10 seconds. Cipi solves this with a flag file and the app user's crontab. flow Deployer always runs as the app Linux user (e.g. myapp ), with the correct PHP binary and file permissions — the same context as a manual cipi deploy myapp . The webhook never shells out to Deployer directly; it only drops the trigger file that Cipi's crontab already watches. Prerequisites Requirement Why Cipi app created with Git repository Deploy key must clone the repo — see Git auto-setup At least one successful manual deploy The agent package must be present in the current release before the webhook route exists composer require cipi/agent in the project Registers the /cipi/webhook route and signature validation Webhook URL reachable over HTTPS Git providers require a public URL; use cipi ssl install first CIPI_WEBHOOK_TOKEN in shared/.env Auto-generated at cipi app create ; shared across all releases Step-by-step setup 1. Create the app and deploy once manually so the server can clone your repository: bash Copy 2. Install Cipi Agent in your Laravel project locally, commit, and push: bash Copy 3. Configure the webhook. If you saved a GitHub or GitLab token, Cipi may have already created the webhook during app create — check with cipi git status . Otherwise, retrieve the URL and secret: bash Copy Add the webhook in your Git provider: Provider Payload URL Secret field Events GitHub https://myapp.com/cipi/webhook Secret → value from --webhook Just the push event GitLab https://myapp.com/cipi/webhook Secret token → same value Push events 4. Restrict to your deploy branch (recommended for production): env Set this in shared/.env via cipi app env myapp . Pushes to other branches receive a skipped response and no deploy runs. 5. Verify. Push a small commit to main and watch the deploy log: bash Copy Within about one minute of the webhook delivery, a new Deployer release should appear. Confirm the live commit with php artisan cipi:status or the health check endpoint. Git auto-setup When a GitHub or GitLab token is configured on the server, Cipi registers the deploy key and creates the webhook automatically on every cipi app create . The app summary shows \"auto-configured ✓\" instead of manual instructions. Lifecycle events ( app edit --repository , app delete ) keep keys and webhooks in sync. Troubleshooting Symptom Likely cause Fix Webhook returns 404 Agent not deployed yet Run cipi deploy myapp after adding cipi/agent to composer.json Webhook returns 403 / invalid signature Secret mismatch Re-copy token from cipi deploy myapp --webhook into the provider settings 200 OK but no deploy Branch filtered out Check CIPI_DEPLOY_BRANCH matches the pushed branch Deploy stuck / lock error Previous deploy interrupted cipi deploy myapp --unlock then retry Deploy runs twice on one push Webhook + pipeline both active Disable one trigger — see overview The agent also supports manual and AI-triggered deploys via the MCP deploy tool — it uses the same .deploy-trigger mechanism. See Cipi Agent for health checks, MCP, and anonymizer features."},{"p":"deploy","c":"Deploy & CI/CD","id":"ci-pipeline-ssh","h":"CI/CD pipelines — SSH deploy","t":"Deploy & CI/CD CI/CD pipelines — SSH deploy CI/CD pipelines — SSH deploy When the webhook model is not enough, run GitHub Actions or GitLab CI/CD jobs that SSH into the server and invoke cipi deploy . This is the right choice whenever deploy must be conditional — gated on tests, preceded by backups, followed by notifications, or orchestrating new preview apps. When you need a pipeline instead of a webhook Quality gate — run php artisan test , static analysis, or frontend builds before any code reaches production Safe release — snapshot the database and shared/ to S3 before swapping the symlink ( safe deploy ) Team visibility — post success/failure to Slack or Telegram with rollback on failure ( deploy notifications ) Review apps — create or update a full Cipi app per feature branch ( preview environments ) Multi-app monorepo — deploy frontend and api in parallel after a single test job For these workflows, disable the production webhook (or never create one) so only the pipeline triggers deploys. You can still use Cipi Agent in the app for health checks and MCP. SSH access for CI Generate a dedicated ed25519 key pair for the CI runner. Add the public key to /root/.ssh/authorized_keys on the server (or the cipi user if you prefer sudo cipi deploy ) and store the private key as a CI secret. Never reuse Git deploy keys or personal SSH keys. bash Copy Store SERVER_HOST (server IP or hostname) alongside SERVER_SSH_KEY in your repository secrets (GitHub) or CI/CD variables (GitLab). GitHub Actions — test then deploy Add the private key as a repository secret named SERVER_SSH_KEY and the server IP as SERVER_HOST . yaml Copy For rollback on failure, extend the script step: yaml Copy GitLab CI / CD Add the private key as a CI/CD variable named SERVER_SSH_KEY (type: File) and the server IP as SERVER_HOST . yaml Copy With rollback on failure: yaml Copy Multi-app deploy If the same pipeline manages multiple apps on the same server: yaml Copy Advanced pipeline patterns Once SSH deploy works, compose these sections into a single production workflow: Pattern What the pipeline adds Guide Notifications Slack or Telegram message on success, failure, and auto-rollback Deploy notifications Safe deploy cipi db backup + cipi backup run before cipi deploy ; rollback on failure Safe deploy w/ backup Preview environments Create/update/delete per-branch Cipi apps with wildcard DNS + SSL Preview environments A typical mature setup uses the webhook for a staging app (instant feedback on every push) and a pipeline for production (tests → backup → deploy → notify). Each app has its own trigger — they never conflict because they target different Cipi app users."},{"p":"deploy","c":"Deploy & CI/CD","id":"deploy-notifications","h":"Deploy notifications","t":"Deploy & CI/CD Deploy notifications Deploy notifications Pipeline use case: the webhook path deploys silently — Git returns 200 and the team finds out only if they watch the logs. With an SSH pipeline , add notification steps after cipi deploy to broadcast success, failure, and automatic rollbacks to Slack or Telegram. Both examples below work with GitHub Actions and GitLab CI using only standard HTTP calls — no extra platform dependencies. Slack Add a final step that posts to a Slack webhook regardless of deploy outcome. Use if: always() in GitHub Actions so the notification fires on both success and failure. Create an Incoming Webhook in your Slack workspace and store the URL as SLACK_WEBHOOK_URL in your CI secrets. yaml Copy For GitLab CI, use curl directly — no plugin needed: yaml Copy Telegram Create a Telegram bot via @BotFather , get the bot token, and find your chat/group ID. Store them as TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID in CI secrets. yaml Copy GitLab CI equivalent (pure curl , no extra dependencies): yaml Copy To find your Telegram chat ID, add the bot to the target group/channel, then call https://api.telegram.org/bot<TOKEN>/getUpdates and look for the chat.id field in the response. For private chats, just message the bot first."},{"p":"deploy","c":"Deploy & CI/CD","id":"safe-deploy","h":"Safe deploy — backup before release","t":"Deploy & CI/CD Safe deploy — backup before release Safe deploy — backup before release For a built-in dump right before Deployer runs (no pipeline stage required), use --snapshot / --snapshot-required or enable cipi app edit <app> --predeploy-snapshot . Pipeline use case: a webhook deploy cannot run a backup step before releasing code — the push event fires deploy immediately. In a CI/CD pipeline , add a dedicated backup stage that must succeed before deploy starts. A production-grade workflow should always create a restore point before the new code goes live. Cipi provides two complementary backup commands that map to two different safety levels: bash Used together in a pipeline, they give you both a fast local restore point and an off-server copy of the database and all uploaded files. The deploy only starts if both backups succeed. Pre-requisite: run cipi backup configure once on the server to link your S3 credentials before cipi backup run can be used. cipi db backup works without any configuration — it is always available. What each command does internally cipi db backup <app> calls mysqldump --single-transaction --routines --triggers and gzips the output to /var/log/cipi/backups/<app>_<timestamp>.sql.gz . The file stays on the server and is never deleted automatically — add a cleanup step or a cron if disk space matters. cipi backup run <app> does two things: dumps the database with mariadb-dump --single-transaction into a temp dir, and archives the entire /home/<app>/shared/ folder (which contains .env , storage/ , and any user-uploaded files). Both archives are then uploaded to S3 under the path cipi/<app>/<timestamp>/ . The temp files are deleted after a successful upload. GitHub Actions — safe deploy workflow yaml Copy The job graph enforces the order: test → backup → deploy . If any job fails, the subsequent ones are skipped. If the deploy step itself fails, the rollback step fires automatically and restores the previous Deployer release. GitLab CI/CD — safe deploy pipeline yaml Copy backup-local and backup-s3 are in the same stage so they run in parallel if you have multiple runners, cutting overall pipeline time. Both must succeed before the deploy stage starts. Restore from local backup If you need to roll back the database to the snapshot taken just before the deploy: bash Copy Restore from S3 backup bash Copy Local backups are never deleted automatically. Each deploy adds a new .sql.gz file to /var/log/cipi/backups/ . On a busy deployment schedule, add a cleanup cron or keep only the last N files: ls -t /var/log/cipi/backups/myapp_*.sql.gz | tail -n +6 | xargs rm -f This example keeps the 5 most recent snapshots and deletes older ones."},{"p":"deploy","c":"Deploy & CI/CD","id":"preview-environments","h":"Preview environments (per-branch deploy)","t":"Deploy & CI/CD Preview environments (per-branch deploy) Preview environments (per-branch deploy) Pipeline use case: webhooks point at a single production URL — they cannot spin up a new Cipi app per branch. Preview environments require a CI/CD pipeline that SSHes into the server, computes a deterministic app name from the branch, and runs cipi app create or cipi deploy accordingly. Every non-production branch can get its own live URL — a fully deployed Laravel app with its own database, workers, and HTTPS. This pattern is sometimes called \"review apps\" or \"ephemeral environments\". The URL format uses three slugs separated by hyphens, so each environment is human-readable and globally unique: example URLs How the identifiers are generated Three values are derived at pipeline runtime: bash Copy Pre-requisites (one-time server setup) 1. DNS wildcard — add an A record *.preview.domain.ltd → <server-ip> in your DNS provider. All subdomains resolve automatically; no per-branch DNS changes needed. 2. Wildcard SSL certificate — obtain a wildcard cert via DNS-01 challenge once and install it on the server. See the Wildcard domains section for instructions. The cert path used by the pipeline examples below is /etc/letsencrypt/live/preview.domain.ltd/ . 3. Repository access — the pipeline examples use an HTTPS URL with a personal access token (PAT) embedded, so no per-app SSH deploy key setup is needed. The token only needs read access to the repository. GitHub Actions Add these secrets to the repository: SERVER_HOST , SERVER_SSH_KEY , DEPLOY_WILDCARD_DOMAIN (e.g. preview.domain.ltd ), GH_PAT (a fine-grained PAT with read access to the repo). yaml Copy GitLab CI/CD Add these CI/CD variables: SERVER_HOST , SERVER_SSH_KEY (File type), DEPLOY_WILDCARD_DOMAIN , GL_TOKEN (a project/group access token with read_repository scope). yaml Copy In GitLab, you can trigger cleanup-preview automatically when a merge request is merged by adding a rules: block that checks $CI_MERGE_REQUEST_EVENT_TYPE == \"merge_train\" or using a dedicated workflow: with if: $CI_PIPELINE_SOURCE == \"merge_request_event\" . Notes and limits Each preview app is a full Cipi app — it gets its own Linux user, database, FPM pool, Supervisor worker, and crontab. On a small VPS this accumulates quickly. Run cipi app list periodically and delete stale previews. The nginx SSL patch is not idempotent — if the pipeline runs cipi app create twice (e.g. due to a retry), the awk patch will be applied again. The hash ensures APP_NAME is deterministic, so the if cipi app show guard prevents double-creation under normal conditions. Avoid running cipi ssl install on a preview app — it will overwrite the wildcard cert config with a per-domain Let's Encrypt cert that will fail (the domain has no dedicated DNS record, only the wildcard)."},{"p":"getting-started","c":"Getting Started","id":"installation","h":"Installing Cipi","t":"Getting Started Installing Cipi Getting Started Installing Cipi Cipi installs a complete Laravel-ready production stack on Ubuntu 24.04 or 26.04 with a single command. The installer takes roughly 10 minutes and sets up Nginx, MariaDB, PHP, Valkey (optional PostgreSQL via cipi db install pgsql after install), Supervisor, Fail2ban, UFW, Certbot, Deployer, and the cipi CLI itself. From zero to production in three steps bash Copy That is the whole path. First create an SSH key on your computer (below), then run the installer on the server — it will ask you to paste that public key. Everything after that is explained in the rest of this page. Run the installer on the server, not on your own machine. SSH into the fresh VPS first and run it there, as a user with sudo . Cipi provisions the whole box — it is not something you point at a remote host from your laptop. Create an SSH key on your computer Do this on your laptop or workstation , not on the VPS. The installer needs the public half of the key so it can lock down the server: after setup you SSH in as the cipi user with this key. Root SSH login is disabled. Never paste the private key. See if you already have one: bash Copy If id_ed25519.pub or id_rsa.pub is listed, you already have a key — skip generation and copy the public file with the command below. If the folder is empty or the command errors, create a new Ed25519 key: bash Copy Press Enter to accept the default path ( ~/.ssh/id_ed25519 ). Set a passphrase if you want (recommended). On macOS and Linux run this in Terminal; on Windows use PowerShell or WSL. Show the public key — this is the single line you will paste when the installer asks: bash Copy It looks like ssh-ed25519 AAAA… comment . Copy the whole line: macOS: pbcopy < ~/.ssh/id_ed25519.pub Linux: xclip -sel clip < ~/.ssh/id_ed25519.pub (or copy from the terminal) Windows (PowerShell): Get-Content $env:USERPROFILE\\.ssh\\id_ed25519.pub | Set-Clipboard Only the .pub file is public. The file named id_ed25519 (no extension) is the private key — keep it on your computer and never paste it into the installer or send it to the server. Run the installer on the server SSH into the fresh Ubuntu VPS as a user with sudo , then: bash Copy The wizard asks for the public key before any package is installed . Paste the line you copied above (accepted formats: ssh-ed25519 , ssh-rsa , ecdsa ). Cipi sanitizes comments, carriage returns, and extra whitespace, then creates a dedicated cipi Linux user for admin SSH access and hardens SSH: root login disabled, cipi public-key only (group cipi-ssh ), app users (group cipi-apps ) can connect with password. Login attempts are limited to 3 with a 20-second grace period; X11 forwarding is disabled. The installer also generates a random 40-character root password , stores it in /etc/cipi/server.json , and displays it in the installation summary. It is not an SSH password: root SSH login is disabled. It is what you type when, already logged in as the cipi user with your key, you run su root . Keep it in a password manager. Non-interactive installation For automated setups, pass your SSH public key via the SSH_PUBKEY environment variable to skip the interactive prompt: bash Copy AWS (root login disabled by default) bash Copy At the end of the installation you will see a summary screen with the SSH access details, the auto-generated root password, and the MariaDB root password. Save them immediately — they are shown only once. Credentials are stored in /etc/cipi/server.json , which — like all Cipi configuration files — is encrypted at rest using AES-256-CBC via the built-in Vault system. Credentials, SSH keys, .env files, and database dumps are protected both on disk and during transfer (via Sync encrypted archives). Cipi also enforces GDPR-compliant log retention with automatic rotation policies for application, security, and HTTP logs — see Log retention for details. Save the SSH access details, root password, and MariaDB root password. Shown o"},{"p":"getting-started","c":"Getting Started","id":"requirements","h":"Requirements","t":"Getting Started Requirements Requirements Ubuntu 24.04 or 26.04 LTS (fresh installs only; interim releases are not supported) Root access (or sudo -s on AWS) Ports 22, 80, and 443 open A clean server — do not install Cipi on a server with an existing web stack A public internet-facing server with a routable public IPv4 address — NAT-only VPS, home networks behind a router, and hosts not reachable from the internet are not supported Full KVM or bare-metal virtualization — not OpenVZ, LXC containers, or shared-kernel environments (including local Docker, Docker Desktop, OrbStack, Colima, WSL, and similar desktop container runtimes) Cipi does not run locally. It is not compatible with local dockerized environments. The installer may appear to succeed on a laptop VM or container, but SSL provisioning, domain routing, and production workflows will not work without a real public server. See Why not local? for the full explanation. Cipi is tested and works on: DigitalOcean, AWS EC2, Vultr, Linode / Akamai, Hetzner, Google Cloud, OVH, Scaleway, and any KVM or bare-metal host running Ubuntu with a public IP."},{"p":"getting-started","c":"Getting Started","id":"quickstart","h":"Quick Start","t":"Getting Started Quick Start Quick Start 1. Create your first app The interactive wizard asks for a username, primary domain, Git repository URL (SSH format) and branch when applicable, and PHP version. Laravel apps always require a repository; for custom apps you can leave the repository empty to host the site via SFTP only — see custom apps . Since v5.0 , Laravel apps can use classic PHP-FPM or Laravel Octane (FrankenPHP) with --octane — see Laravel Octane . bash Copy Or pass all flags directly to skip interactive mode: bash Copy At the end, Cipi prints a credentials summary — save it, shown only once — including the server's public IP address (for DNS configuration), the SSH deploy key, database credentials, a ready-to-use mariadb+ssh:// connection URL for GUI clients (TablePlus, DBeaver, Sequel Pro), webhook URL, and webhook token. 2. Add the deploy key to your Git provider If you have configured a GitHub or GitLab token via cipi git , this step is automatic — Cipi adds the deploy key and creates the webhook for you. See Git auto-setup for details. Otherwise, copy the ssh-ed25519 ... key shown after app creation and add it as a Deploy Key in your repository: GitHub: Repository → Settings → Deploy keys → Add deploy key GitLab: Repository → Settings → Repository → Deploy keys 3. Prepare your Laravel project Cipi uses the database driver for cache, sessions, and queues. Run these once inside your Laravel project, commit, and push the generated migrations: bash Copy Cipi automatically runs artisan migrate --force on every deploy. The cache, session, and queue tables will be created on first deploy if you commit the migrations. 4. Deploy bash Copy Deployer clones your repo into a new releases/N/ directory, runs composer install --no-dev , links .env and storage/ , runs migrations, runs artisan optimize , creates storage:link , swaps the current symlink atomically, and restarts queue workers. Zero downtime. 5. Install SSL bash Copy Certbot provisions a Let's Encrypt certificate, configures Nginx for HTTPS, and updates APP_URL in .env . Your Laravel app is now live on https://myapp.com ."},{"p":"getting-started","c":"Getting Started","id":"tech-stack","h":"Tech Stack","t":"Getting Started Tech Stack Tech Stack Cipi brings a complete, production-ready stack to your Ubuntu server. Here is everything that gets installed and configured: Component Role Ubuntu Base OS (24.04 or 26.04 LTS) Nginx Web server, reverse proxy, SSL termination. Since v4.5.9 , mainline nginx from nginx.org (1.29.8+) with max_headers 1000 for HTTP/2 hardening — not the Ubuntu archive package PHP-FPM Default PHP runtime per app (8.3–8.5; ondrej/php, packages.sury.org, or Ubuntu main per release) Laravel Octane Optional FrankenPHP HTTP server per Laravel app (since v5.0 ) — cipi app create --octane ; FPM and Octane apps coexist on the same server MariaDB Default relational database (drop-in MySQL replacement, port 3306). Optional PostgreSQL via cipi db install pgsql (port 5432) since v4.8.0 Valkey In-memory store for cache, sessions, queues, broadcast (BSD-licensed Redis fork) Supervisor Process manager for Laravel queue workers, Octane, Horizon, and Reverb Deployer Zero-downtime deployment tool Certbot Let's Encrypt SSL certificates UFW Firewall (ports 22, 80, 443) Fail2ban Progressive brute-force protection with recidive jail unattended-upgrades Automatic security patches Composer PHP dependency manager cipi CLI Server management and orchestration What every app gets Each app runs in a fully isolated environment. Laravel is the default — zero-downtime deploys, its own database, workers, scheduler and webhook — optionally served by Octane (FrankenPHP) instead of PHP-FPM. --custom is for simple sites (WordPress, static + PHP): a classic deploy into htdocs , a configurable docroot, no database, no .env , no cron, no workers. Area What Cipi gives you Web server Nginx with a per-app virtual host — PHP-FPM or Octane via proxy_pass , tuned for Laravel PHP & Composer Selectable per app, hot-swappable; settings managed with cipi ini across FPM and CLI Database MariaDB by default, optional PostgreSQL — a dedicated database and user per Laravel app Queue workers Supervisor pools per app — queue:work or Horizon; optional Reverb for WebSockets Deployments Deployer — atomic symlink, 5 releases, rollback, optional Node build; classic clone into htdocs for custom apps SSL Let's Encrypt via Certbot — HTTP-01 by default, optional DNS-01 (Cloudflare) with wildcards Security Fail2ban + UFW, a dedicated Linux user, its own FPM/Octane process and SSH key per app Healthchecks HTTP probes every 5 minutes, plus a post-deploy check with optional automatic rollback of a broken release Backups Backup profiles — what, how often, where, how long — S3 / S3-compatible / local, with client-side encryption Configuration Optional per-project cipi.yml for aliases, databases, workers, healthcheck and backups"},{"p":"getting-started","c":"Getting Started","id":"app-structure","h":"App Structure","t":"Getting Started App Structure App Structure When you run cipi app create , Cipi creates a fully isolated environment for the app. Here is everything that gets set up: In addition to the home directory, Cipi creates these system files: The .env is auto-compiled with all credentials — database name, password, webhook token, and APP_KEY . You never have to touch it manually, but you can always edit it with cipi app env myapp ."},{"p":"gui","c":"Control Panel (GUI)","id":"gui-overview","h":"The optional web control panel","t":"Control Panel (GUI) The optional web control panel Control Panel (GUI) The optional web control panel Cipi is, and always will be, CLI-first . Everything a server needs — apps, databases, SSL, deploys, backups, firewall — is a single cipi command away over SSH. But from v4.7.0 Cipi also ships an optional web control panel: a self-hosted Laravel dashboard that manages one or more Cipi servers from the browser, over the same REST API the CLI client and WHMCS module use. 100% optional. The panel is a separate package you install only if you want it. Skip it and Cipi runs exactly as before — a lean CLI with no daemon, no web surface, and nothing extra listening. Install it and your team or clients get a friendly UI without giving up the terminal. Your server, your choice. The panel is a thin visual layer on top of the API — it does not replace the CLI or add a proprietary control plane. Anything you do in the browser, you can still do from the terminal, and vice versa. A single panel can drive many servers, so it doubles as a lightweight multi-server cockpit. What you can do from the panel Manage apps — create, edit, deploy, suspend/unsuspend, and delete Laravel and custom apps; manage aliases, WWW/apex redirects, SSL (install + force HTTPS), and HTTP basic auth. Create Laravel apps with optional Laravel Octane (FrankenPHP) (API 1.13+ / Cipi 5.0+ ); list and detail views show FPM vs Octane. Edit app .env , manage Composer auth.json , run Artisan , and execute whitelisted App commands ( cipi app run ) — requires API 1.14+ / Cipi 5.0.3+ (terminal output supports Markdown copy). Pick a database engine at app create ( mariadb or pgsql ) and manage multi-engine databases (API 1.12+ / Cipi 4.8+ ). Browse application and server logs with type filter, pagination, and auto-refresh. Watch async jobs in an interactive overlay (spinner + terminal output while polling GET /api/jobs/{id} ). Live dashboard — CPU, memory, disk, services, and app count via GET /api/status . Server cockpit (API 1.15+ / Cipi 5.0.6+ ) — manage PHP versions and system default, SSH keys, services, SMTP notifications, HTTP healthchecks, and panel API IP whitelist from the browser. Register and switch between multiple servers from one login."},{"p":"gui","c":"Control Panel (GUI)","id":"gui-requirements","h":"Requirements","t":"Control Panel (GUI) Requirements Control Panel (GUI) Requirements The panel talks to your servers exclusively through the Cipi REST API. Before installing it, make sure each server you want to manage has the API enabled with a token that covers the full ability set. Each managed server needs cipi api installed and a Bearer token with all abilities. See the API setup guide to generate one. A running Cipi server (Ubuntu 24.04 / 26.04) to host the panel — this can be one of the servers you manage or a small dedicated box. A domain (or subdomain) pointed at that server, e.g. panel.example.com . cipi api enabled on every server you want to control, each with a full-ability token (include www-manage , status-view , and for app tooling apps-env , apps-auth , apps-artisan , apps-run , apps-deploy-config ). For server cockpit (PHP, SSH, services, SMTP, health, IP whitelist), managed servers need API 1.15+ and Cipi 5.0.6+ . For .env / auth.json / Artisan / App commands, managed servers need API 1.14+ and Cipi 5.0.3+ . For Octane create and FPM/Octane indicators, API 1.13+ and Cipi 5.0+ . For multi-engine MariaDB/PostgreSQL and www redirects, API 1.12+ and Cipi 4.8+ . PHP, the FPM pool, the Nginx vhost and the scheduler are all provisioned for you by the installer — you do not set up a Laravel environment by hand."},{"p":"gui","c":"Control Panel (GUI)","id":"gui-install","h":"Installation — cipi gui <domain>","t":"Control Panel (GUI) Installation — cipi gui <domain> Control Panel (GUI) Installation — cipi gui <domain> One command provisions the whole panel on a Cipi server: a dedicated PHP-FPM pool, an Nginx vhost for your domain, and a scheduler cron job. The Laravel app is bundled at /opt/cipi/gui , the same model used by the cipi-api package. bash Copy The installer prompts interactively for the first administrator account — an email and a password. The password must satisfy the panel's policy: Minimum 12 characters . At least one uppercase, one lowercase, one digit, and one special character. No more than 4 identical consecutive characters. Configuration is written to /etc/cipi/gui.json . Once the command finishes, open your domain in a browser and log in with the admin account you just created. The panel is served over plain HTTP until you issue a certificate. Run cipi gui ssl right after installation to secure it."},{"p":"gui","c":"Control Panel (GUI)","id":"gui-ssl","h":"SSL — cipi gui ssl","t":"Control Panel (GUI) SSL — cipi gui ssl Control Panel (GUI) SSL — cipi gui ssl Issue a free Let's Encrypt certificate for the panel's domain and switch it to HTTPS. The panel is HTTPS-aware and sets the correct forwarded headers behind the Cipi Nginx vhost. bash Copy The certificate renews automatically through the same ACME renewal that Cipi uses for app domains."},{"p":"gui","c":"Control Panel (GUI)","id":"gui-2fa","h":"Two-factor authentication (TOTP)","t":"Control Panel (GUI) Two-factor authentication (TOTP) Control Panel (GUI) Two-factor authentication (TOTP) The panel uses classic session login and supports optional TOTP two-factor authentication . Each administrator can enrol an authenticator app (Google Authenticator, 1Password, Aegis, …) from their profile. 2FA is opt-in per user, not forced. If an administrator loses access to their second factor, an operator with shell access can clear it from the server with cipi gui reset-user , which rewrites the account and removes the 2FA enrolment."},{"p":"gui","c":"Control Panel (GUI)","id":"gui-update","h":"Updating — gui update vs gui upgrade","t":"Control Panel (GUI) Updating — gui update vs gui upgrade Control Panel (GUI) Updating — gui update vs gui upgrade The panel ships as the cipi/gui Composer package, so updates are ordinary Composer updates plus migrations. Two commands cover the common cases: cipi gui update — soft update Runs composer update cipi/gui , applies database migrations, and refreshes the compiled theme. This is the day-to-day update path. bash Copy cipi gui upgrade — full rebuild Rebuilds the panel from scratch. Use it after a major release, or if an environment has drifted and a clean reinstall of the app code is the fastest way back to a known-good state. bash Copy cipi gui refresh-theme — theme only Recompiles and reloads the panel theme without touching code or the database — handy after a theme tweak. bash Copy"},{"p":"gui","c":"Control Panel (GUI)","id":"gui-operations","h":"Operations","t":"Control Panel (GUI) Operations Control Panel (GUI) Operations cipi gui status Report the panel's current status — whether it is installed, its domain, and the health of its services. bash Copy cipi gui fix-permissions Repair the panel's PHP-FPM open_basedir restriction and all runtime file permissions. Run it if the panel starts throwing permission or autoload errors after an update — including the HTTP 500 after cipi gui upgrade / soft update fixed in 5.0.18 (Composer symlink for cipi/gui outside open_basedir ). bash Copy cipi gui reset-user Rewrite an administrator's email, name and password, and clear their 2FA enrolment — the recovery path when someone is locked out. reset-password is an alias for the same command. bash Copy"},{"p":"gui","c":"Control Panel (GUI)","id":"gui-remove","h":"Removing the panel — cipi gui remove","t":"Control Panel (GUI) Removing the panel — cipi gui remove Control Panel (GUI) Removing the panel — cipi gui remove Uninstall the panel and its Nginx vhost, PHP-FPM pool and scheduler. Your servers and apps are untouched — the panel is just a client of the API. uninstall is an alias, and --force skips the confirmation prompt. bash Copy Removing the panel is the ultimate expression of its optionality: uninstall it any time and Cipi is back to a pure CLI, exactly as it was before."},{"p":"gui","c":"Control Panel (GUI)","id":"gui-architecture","h":"Architecture","t":"Control Panel (GUI) Architecture Control Panel (GUI) Architecture The panel is a standard Laravel 12 application distributed as the cipi/gui package from its own repository ( cipi-sh/gui ) and installed via a Composer VCS entry tracking dev-main . It lives at /opt/cipi/gui , mirroring how the cipi-api package is bundled. Aspect Detail Framework Laravel 12 Package cipi/gui (GitHub cipi-sh/gui , Composer VCS dev-main ) Install path /opt/cipi/gui Config /etc/cipi/gui.json Isolation PHP-FPM open_basedir restricted to /opt/cipi/gui/ and /opt/cipi/cipi-gui/ (since 5.0.18 ) Auth Session login + optional TOTP 2FA Jobs No local queue worker — remote async jobs are polled through each managed server's cipi api Because it holds no local queue worker and stores no server state of its own, the panel stays stateless with respect to your infrastructure: it authenticates to each server's API, submits jobs, and polls their status. Tear the panel down and rebuild it elsewhere without touching a single managed server. API coverage The GUI consumes the full Cipi API OpenAPI surface. Keep the panel and each managed server's cipi/api package current ( cipi gui update / cipi api update ) so create forms and list fields match the server. Area Coverage Server GET /api/status Apps CRUD, basic auth, logs, .env , auth.json , Artisan, App commands ( app run ); create accepts engine (mariadb/pgsql) and octane (FrankenPHP, API 1.13+ ); env/auth/run need API 1.14+ ; list/show expose octane / octane_port Deploy config Structured Deployer knobs via /api/apps/{name}/deploy-config (API 1.14+ ) Aliases List, add, remove WWW Status, add counterpart, force-to-root / force-from-root, clear ( www-manage ; API 1.12+ ) Deploy Deploy, rollback, unlock SSL Install Let's Encrypt, force HTTPS redirect Databases Engines + list (sync); create/delete/backup/restore/password with optional engine (async; API 1.12+ ) Jobs Poll status and CLI output Prefer the terminal? See the CLI client for the same API-driven management from your shell, or the cipi api and WHMCS module for automation. Package source: cipi-sh/gui ."},{"p":"index","c":"Documentation","id":"docs-index","h":"Cipi Documentation","t":"Documentation Cipi Documentation Documentation Cipi Documentation Full technical documentation for Cipi — the Laravel-first open-source deploy CLI. Choose a chapter below to get started. Getting Started Installation, requirements, quick start, tech stack, and app structure for Cipi. Cipi Agent Laravel cipi/agent package — webhooks, health, in-app MCP, anonymizer, security model. Apps Create, manage, suspend, and configure Laravel apps — including Octane, Reverb, and clones — plus custom applications. Deploy & CI/CD Zero-downtime deployments, pre-deploy DB snapshots, Git integration, CI/CD pipelines, notifications, and preview environments. Infrastructure PHP, databases, SSL (incl. DNS-01), Horizon, healthchecks, backups, workers, firewall, and server management. CLI Client Manage one or more Cipi servers remotely from your local terminal. Apps, databases, SSL, deploys, multi-server profiles, global status overview, and shell completion — all via the REST API. Advanced cipi/api REST + MCP + Swagger, server sync, vault, SMTP, log retention, and advanced config. About Cipi History, security model, design decisions, and comparison with alternatives."},{"p":"infrastructure","c":"Infrastructure","id":"cli-php","h":"cipi php","t":"Infrastructure cipi php Infrastructure cipi php PHP 8.5 is pre-installed during setup. Additional versions can be added at any time; since v4.5.12 Cipi probes APT sources per Ubuntu codename — typically the ondrej/php PPA on 24.04, packages.sury.org on 26.04 when Launchpad has no suite, or Ubuntu main as a last resort (co-installable 8.3–8.5 where the chosen repo supports it). Since v4.5.4 Cipi bundles Deployer 8 , which requires PHP ≥ 8.3 . cipi php install , cipi php switch , cipi app create and cipi app edit therefore accept only 8.3, 8.4 and 8.5 . Legacy versions (7.4–8.2) remain detectable and removable, so you can still clean up a pre-4.5.4 server with cipi php remove <old-version> . bash Copy Security patch upgrades Since v4.7.13 , PHP packages are excluded from unattended-upgrades and managed by Cipi instead. cipi php upgrade runs apt-get update and --only-upgrade on every installed php* and libphp* package, then restarts the affected PHP-FPM pools. When SMTP is configured and packages were upgraded, Cipi sends an email ( php_upgrade trigger; disable with cipi notifications disable php_upgrade ). A weekly check runs automatically on Sunday at 03:30 via root crontab (wrapped by cipi-cron-notify ). Log: /var/log/cipi/php-upgrade.log . Existing servers receive the cron on cipi self-update (migration 4.7.13). System default vs app-specific PHP The system default is the PHP version used by the root and cipi users, the Cipi API FPM pool, and the API queue worker. Use cipi php switch <ver> to change it. The command migrates the API pool (and the GUI FPM pool when installed), recreates panel sockets, and restarts the API worker. Since 5.0.9+ , PUT /api/php/default wraps this via the panel API ( php-manage ability). Since 5.0.10–5.0.12 , switch tolerates partial update-alternatives success, remounts read-only roots when needed, and keeps API/GUI FPM pools on the target version to avoid nginx 502. REST: GET /api/php , POST /api/php/install , DELETE /api/php/{version} , PUT /api/php/default (API 1.15.0+ / Cipi 5.0.6+ ). CLI JSON: cipi php list --json (since 5.0.6 ). Each app has its own PHP version (set at app create or via cipi app edit --php= ). Deployer, Composer, crontab deploy triggers, and cipi sync import always run with the app's configured PHP — never the system default. To switch an existing app to a different version: bash Copy This hot-swaps the PHP version with zero downtime: updates the FPM pool, Nginx socket, Supervisor workers, crontab, Deployer config, and .env in one atomic operation."},{"p":"infrastructure","c":"Infrastructure","id":"cli-ini","h":"cipi ini","t":"Infrastructure cipi ini cipi ini Available since v5.1.0 . A guided way to change PHP settings without hunting for the right php.ini . Every server-wide change reaches both SAPIs — PHP-FPM and CLI — so what your web requests see is also what queue workers, artisan and cron see. bash Copy What it does for you It raises the settings that would silently cap yours. Setting upload_max_filesize also raises post_max_size and memory_limit when they would otherwise limit it, and Nginx's client_max_body_size is flagged when it would. It refuses what should not be set this way. Settable keys are an explicit whitelist — see cipi ini keys . open_basedir , auto_prepend_file , extension and friends are refused by name, with the reason , because Cipi manages them as part of app isolation. Per-app overrides win. --app=<app> writes into that app's FPM pool, which outranks the server-wide file — for that app only. Why this needed fixing Before 5.1.0 every FPM pool hardcoded upload_max_filesize , post_max_size and max_execution_time , and pool values outrank conf.d — so a server-wide php.ini change could not reach a single app. On top of that, Cipi wrote 99-cipi.ini for FPM only, leaving the CLI SAPI (queue workers, artisan , cron) on the PHP package defaults. In 5.1.0 pools carry only what is genuinely per-app ( open_basedir , the error log, explicit overrides) and inherit everything else; both SAPIs get their file; and migration 5.1.0 backfills the CLI 99-cipi.ini and rewrites existing pools on update. Each change fires the ini_set notification trigger, so a PHP setting never changes on a shared server without a trace. An app can also carry its PHP version and per-app settings in its repository — see cipi.yml ."},{"p":"infrastructure","c":"Infrastructure","id":"cli-db","h":"cipi db","t":"Infrastructure cipi db cipi db Cipi creates a dedicated database for each Laravel app automatically during app create . MariaDB (port 3306 ) is the native default; since v4.8.0 you can also install optional PostgreSQL (port 5432 ) and choose an engine per database or app. Custom apps do not get a database — use cipi db create --name=<app> if you need one (e.g. for WordPress or another CMS). After setup, a ready-to-use connection URL is displayed (MariaDB: mariadb+ssh:// ), combining SSH credentials, server IP, and database information for GUI clients like TablePlus, DBeaver, or Sequel Pro. Engines (v4.8.0+) bash Copy Database lifecycle bash Copy Laravel apps store the chosen engine in app metadata; backup, sync, and cipi app reset-db-password follow it. Use cipi app create --engine=pgsql (or the interactive prompt) to provision a Laravel app on PostgreSQL — .env and the connection URL match the engine. Root password reset: cipi reset db-password [--engine=] ."},{"p":"infrastructure","c":"Infrastructure","id":"cli-aliases","h":"cipi alias","t":"Infrastructure cipi alias cipi alias Add multiple domains or subdomains to any app. After adding aliases, run cipi ssl install to provision or renew the certificate with SAN coverage for all domains. Since v4.8.0 , cipi alias add|remove regenerates the vhost and re-applies SSL with certbot install --redirect so HTTPS is not dropped. bash Copy Since v5.1.0 , wildcard aliases such as *.example.com are accepted — Nginx matches a wildcard server_name natively, and multi-tenant apps need it. Quote the pattern so the shell does not expand it, and issue the certificate with cipi ssl install myapp --dns=cloudflare --wildcard : HTTP-01 cannot validate a wildcard. For www ↔ apex canonical redirects, prefer cipi www instead of managing the counterpart host by hand."},{"p":"infrastructure","c":"Infrastructure","id":"cli-www","h":"cipi www","t":"Infrastructure cipi www cipi www Available since v4.8.0 . Manage www/apex aliases and canonical 301 redirects for an app. State lives in apps.json ( www_redirect ); Nginx emits a dedicated redirect server block (ACME path kept public) that survives vhost regeneration. SSL is re-applied via certbot install --redirect . bash Copy force-to-root / force-from-root auto-add the missing alias when needed."},{"p":"infrastructure","c":"Infrastructure","id":"cli-domains","h":"cipi domains","t":"Infrastructure cipi domains cipi domains Available since v4.5.5 . A top-level command that lists every domain and alias across all apps in a single table — the global counterpart to the per-app cipi alias list <app> . Ideal for auditing the whole domain-to-app mapping or spotting a domain that is still missing a certificate. bash Copy Each row shows the following columns: Column Description DOMAIN The domain or alias name. Rows are sorted alphabetically by domain. APP The app that owns the domain. KIND primary or alias . TYPE Laravel or Custom . PHP The app's PHP version. DOCROOT public for Laravel apps, /<docroot> for custom apps. BRANCH The Git branch deployed for the app. LAST DEPLOY Human-relative age ( just now , 10m ago , 2h ago , 3d ago , 2w ago , 2mo ago , 2y ago ) — derived from the mtime of the app's current symlink, which Deployer atomically re-points on every successful deploy. Shows - when the app was never deployed. SSL Per-name certificate status ( ✓ / ✗ ), detected from /etc/letsencrypt/live/<domain> . REPOSITORY The Git repository, or (SFTP only) for custom apps with no repo. (suffix) When the owning app is suspended, each row ends with ⏸ suspended (yellow). The footer also reports how many apps are suspended. A footer summarises the totals — number of domains, apps, certificates, and suspended apps — making it easy to audit the whole mapping at a glance."},{"p":"infrastructure","c":"Infrastructure","id":"cli-ssl","h":"cipi ssl","t":"Infrastructure cipi ssl cipi ssl Certbot manages Let's Encrypt certificates. Certificates auto-renew via a weekly cron. cipi ssl status shows expiry dates with color-coded warnings: green (>30 days), yellow (14–30 days), red (<14 days). bash Copy Since v4.8.0 , cipi ssl force <app> re-applies the HTTP → HTTPS redirect for an app that already has a Let's Encrypt certificate — without issuing a new one. cipi ssl install also sets force_https in apps.json automatically. Since v5.0 , optional DNS-01 challenges via Cloudflare replace the default HTTP-01 flow when you need wildcard certificates or cannot expose port 80. Configure once with cipi ssl dns set , then pass --dns=cloudflare (and optionally --wildcard ) to cipi ssl install . After adding domain aliases with cipi alias add , always run cipi ssl install again to provision a new SAN certificate covering all domains. If HTTPS redirect was lost after a vhost change, use cipi ssl force instead of re-issuing."},{"p":"infrastructure","c":"Infrastructure","id":"cli-nginx","h":"cipi nginx default-server","t":"Infrastructure cipi nginx default-server cipi nginx default-server Available since v5.1.0 . Claims whichever of :80 / :443 has no default server and closes unmatched requests with an empty reply ( 444 ). bash Copy Cipi always had a default server on :80 , but never on :443 . An HTTPS request carrying an unknown Host therefore fell through to whichever vhost Nginx had loaded first — for a wildcard multi-tenant app, straight into a tenant resolver that cannot parse it. It is enabled on fresh installs, and migration 5.1.0 claims it on existing servers where nothing else already does. If another vhost legitimately owns the default server, the command says so and changes nothing."},{"p":"infrastructure","c":"Infrastructure","id":"cli-backup","h":"cipi backup","t":"Infrastructure cipi backup cipi backup Back up application files and databases to local disk, to Amazon S3, or to any S3-compatible provider (Cloudflare R2, Hetzner Object Storage, DigitalOcean Spaces, Backblaze B2, Scaleway, MinIO, …). Since v5.1.0 backups are driven by profiles instead of one hardcoded nightly job. A profile answers four questions — what it takes, how often it runs, where it goes and how long it is kept — and profiles are independent, so a 30-minute database-only copy kept on disk sits happily next to an encrypted nightly full copy on S3. Upgrading from 5.0.x? Migration 5.1.0 converts the old nightly job into a profile named default , carrying over its existing --weeks retention, and takes over the schedule with a managed crontab block. Nothing outside that block is touched, and cipi backup prune <app> --weeks=N still prunes the pre-5.1 layout. 1 — Destinations cipi backup configure stores the S3 credentials shared by every profile. Since v5.1.0 you may leave the bucket empty for local-only backups, which land under /var/backups/cipi . bash Copy S3-compatible endpoints Provider Endpoint URL AWS S3 leave empty Cloudflare R2 https://<account-id>.r2.cloudflarestorage.com Hetzner https://<datacenter>.your-objectstorage.com DigitalOcean Spaces https://<region>.digitaloceanspaces.com Backblaze B2 https://s3.<region>.backblazeb2.com Scaleway https://s3.<region>.scw.cloud MinIO https://your-minio-host 2 — Backup profiles Create a profile once; it then runs on its own schedule. Two typical ones: bash Copy bash Copy Flags for profile add / profile edit --scope=all|files|db What the profile takes: application files, databases, or both. --apps='shop,blog' Which apps its file archives cover. Glob patterns allowed. --databases='main,tenant_*' Which databases it covers. They are discovered from the engine itself, so tenant databases an app creates at runtime are matched too. --exclude-databases='<glob,…>' Databases to leave out of an otherwise broad selection. --exclude-tables='*.jobs,*.telescope_*' Tables to skip — expanded against the live table list on MariaDB, passed straight to pg_dump on PostgreSQL. --every=30m|6h|1d or --cron='0 2 * * *' How often it runs. --dest=local|s3|local,s3 Where the run goes. --keep=N / --keep-days=N / --keep-weeks=N Retention — at least one is mandatory . A profile that would grow without bound is refused. --encrypt / --no-encrypt Client-side AES-256 encryption before anything leaves the server. Multi-tenant apps are covered now. Before 5.1.0 a run dumped exactly one database per app — the one named after the app — so tenant_1 , tenant_2 , … were never saved at all. Databases now come from the engine (minus system schemas) and are selected with glob patterns. 3 — Encryption With --encrypt the archive is encrypted with AES-256 on the server , before it is uploaded, so the bucket operator never holds readable data. The manifest stays readable so a run can still be identified. bash Copy Without the key an encrypted backup cannot be restored — not by you, not by Cipi. Save it in a password manager the moment you enable --encrypt , and remember that runs taken before a key rotate still need the old key. 4 — Running, checking and restoring bash Copy Since v5.1.0 these commands tell you the truth rather than reassuring you: Every archive is integrity-checked before it ships . A dump cut short by a full disk is still a non-empty file, so the old size check passed it; now a corrupt archive is discarded and reported instead of quietly replacing a good backup. cipi backup fetch no longer reports success on a prefix that holds no objects, so a mistyped timestamp is an error rather than an empty directory. cipi backup verify says plainly that there is nothing to verify when no run has happened yet, instead of “passed”. tar warnings — “file changed as we read it”, constant on a live app writing its logs — no longer fail a perfectly good archive. Only exit code 2 and above counts. A disabled profile actua"},{"p":"infrastructure","c":"Infrastructure","id":"cli-app-cron","h":"User crontab","t":"Infrastructure User crontab User crontab Cipi automatically adds a crontab entry for the Laravel scheduler when an app is created: bash Copy This entry runs as the myapp Linux user every minute, using the PHP version selected for the app. It is updated automatically when you change PHP version via cipi app edit myapp --php=X . Viewing the current crontab bash Copy Adding custom cron jobs You can add extra cron jobs to the app user's crontab. Switch to the app user first to ensure jobs run with the correct user context and file permissions: bash Copy Example entries you might add: bash Do not remove the Laravel scheduler entry. Cipi does not re-add it automatically if deleted — you would need to run cipi app edit myapp --php=<current-version> to restore it. Always keep the schedule:run line as the first entry so it is easy to identify. Cron jobs run as the app user. They respect the same filesystem restrictions as the app itself. If a cron job needs to write files, make sure the target path is inside /home/myapp/ . Jobs that require root access should be added to the root crontab instead, with crontab -e as root. Checking if cron is working bash Copy"},{"p":"infrastructure","c":"Infrastructure","id":"cli-schedule","h":"cipi schedule","t":"Infrastructure cipi schedule cipi schedule Since v5.0 , manage the Laravel scheduler crontab entry that runs schedule:run (the crontab itself already existed; it is now toggleable and tracked in apps.json ). bash Copy"},{"p":"infrastructure","c":"Infrastructure","id":"cli-workers","h":"cipi worker & Laravel Horizon","t":"Infrastructure cipi worker & Laravel Horizon cipi worker & Laravel Horizon Every app gets a default Supervisor worker for the default queue. You can add additional queues with custom process counts and timeouts. bash Copy With Horizon enabled, deploy runs horizon:terminate and restarts workers via cipi-worker . You cannot run classic queue:work workers and Horizon on the same app at once. On 5.0.x, cipi worker horizon enable could leave Horizon half enabled : the command printed nothing after “Enabling Horizon…” and status still said disabled . v5.1.0 fixes both causes, always writes the state, reports what Supervisor actually did, and makes status surface any drift between the two. If you hit this, run cipi self-update and enable it again. Queue workers can also be declared in the app's repository — see cipi.yml . Flag Description --queue=<name> Queue name to consume (e.g. default , emails , exports ) --processes=<n> Number of parallel worker processes --timeout=<seconds> Job timeout in seconds. Default is 60. Workers are stopped before the symlink swap and restarted after every deploy, preventing Supervisor from picking up stale artisan paths. Supervisor is configured with autorestart=unexpected so workers only restart on unexpected exits, not on graceful stops. App-user helper: cipi-worker Each app user can restart, stop, or check workers without root — via a restricted sudo helper installed at /usr/local/bin/cipi-worker : bash Copy As root, use cipi worker list|restart|stop <app> instead. There is no cipi worker status admin command — use cipi worker list or the app-user helper above."},{"p":"infrastructure","c":"Infrastructure","id":"cli-health","h":"cipi health","t":"Infrastructure cipi health cipi health Since v5.0 , configure HTTP healthchecks per app. Cipi then watches the app in two different ways, because “is the site up?” and “did the push I just made break production?” are not the same question. Check When it runs When it alerts Periodic probe Every 5 minutes After 3 consecutive failures → health_fail Post-deploy check Right after every release goes live Immediately on the first failure → deploy_health_fail bash Copy Post-deploy verification (v5.1.0+) Once an app has a healthcheck URL, the release that just went live is verified right after every deploy — from cipi deploy and from the Git webhook alike. The probe waits a short grace period (8 seconds for Octane apps, 3 otherwise, or whatever --grace=N says) and retries five times, so an app that needs a moment to come up is not reported as broken. The verdict never changes the deploy's exit code — the release is live either way — but the alert says so and hands you the rollback command. The success email is sent after verification, so it can never announce a successful deploy while the site is returning 500. bash Copy Auto-rollback of an unhealthy release (opt-in) Cipi can undo a release that fails its post-deploy healthcheck: the current symlink moves back to the previous release, the app is probed again, and one email describes the whole sequence — what was published, what it answered, what it was rolled back to and whether that fixed it. bash Copy Four outcomes are reported distinctly: recovered ; rolled back but still unhealthy (so the cause is probably not the code); the rollback itself failed (the bad release is still live); and there is no earlier release to return to. Auto-rollback is off by default and deliberately so: database migrations are not undone. A release that migrated the schema and then failed can be worse off after the code is rolled back. Enable it only for apps whose deploys do not migrate, or where you know the migrations are backwards-compatible. REST: GET /api/health , GET|PUT|DELETE /api/apps/{name}/health , POST /api/apps/{name}/health/check (API 1.15.0+ / Cipi 5.0.6+ ). An app can also declare its healthcheck in its repository — see cipi.yml ."},{"p":"infrastructure","c":"Infrastructure","id":"cli-firewall","h":"cipi firewall","t":"Infrastructure cipi firewall cipi firewall Cipi installs UFW with ports 22, 80, and 443 open by default. Use the firewall commands to manage additional rules without touching UFW directly. bash Copy"},{"p":"infrastructure","c":"Infrastructure","id":"cli-ban","h":"cipi ban","t":"Infrastructure cipi ban cipi ban Inspect and manage Fail2ban bans directly from the CLI. Cipi configures Fail2ban with progressive banning: a 24-hour base ban that doubles on each repeat offence up to a 7-day cap, with max retries reduced to 3. A dedicated recidive jail bans repeat offenders for 7 days after 3 bans within 24 hours. bash Copy cipi ban list Lists every IP currently banned by Fail2ban, grouped by jail (e.g. sshd , recidive ). Useful for a quick security check or before running an unban. cipi ban unban <IP> Removes the given IP from all Fail2ban jails at once. Handy when a legitimate user or CI runner gets locked out by mistake. Existing installations are upgraded automatically by the 4.3.0 migration script when you run cipi self-update . No manual configuration is needed."},{"p":"infrastructure","c":"Infrastructure","id":"cli-services","h":"cipi service","t":"Infrastructure cipi service cipi service Check and control the system services that power Cipi directly from the CLI. Nginx uses a graceful reload (zero downtime) instead of a full restart. bash Copy REST: GET /api/services , POST /api/services/{name}/restart (API 1.15.0+ / Cipi 5.0.6+ ; abilities services-view / services-manage ). Supported service names: nginx , mariadb , postgresql (aliases: pgsql , postgres — when installed via cipi db install pgsql ), valkey-server , supervisor , fail2ban , php<ver>-fpm (e.g. php8.5-fpm ). The keyword php targets all installed PHP-FPM versions at once. For the cache backend, redis-server , redis , and valkey are accepted as aliases of valkey-server . Valkey — the BSD-licensed, Redis-compatible fork — is included in the default stack and replaces redis-server since Cipi 4.5.6. It is installed with a password, bound to localhost only, and its credentials (user, password) are saved in /etc/cipi/server.json and shown at the end of installation. valkey-server is added to the unattended-upgrades blacklist — Cipi manages it, so it is not auto-upgraded automatically. See the Valkey section for details and the automatic Redis → Valkey migration."},{"p":"infrastructure","c":"Infrastructure","id":"cli-ssh","h":"cipi ssh — SSH Key Management","t":"Infrastructure cipi ssh — SSH Key Management cipi ssh — SSH Key Management Manage the authorized SSH keys for the cipi user — the admin SSH entry point. The cipi user (group cipi-ssh ) uses public-key only; root login is disabled. App users (group cipi-apps ) connect with password — see SSH as the app user . Commands bash Copy REST: GET|POST /api/ssh/keys , DELETE /api/ssh/keys/{n} (API 1.15.0+ / Cipi 5.0.6+ ). Safety mechanisms cipi ssh remove includes two safeguards to prevent lockout: Current-session protection — you cannot remove the key used by your active SSH session. Last-key protection — you cannot remove the last remaining authorized key. Key comments SSH keys are stored with their original comments intact, making it easy to identify who each key belongs to. Use cipi ssh rename to change the display name of any key: bash Copy Email notifications When SMTP is configured, Cipi sends an email alert every time a key is added, removed, or renamed. The notification includes the server hostname, IP address, key fingerprint, key comment, timestamp, and remaining key count. Rename notifications also include the old and new key name."},{"p":"infrastructure","c":"Infrastructure","id":"cli-cipi","h":"cipi — Server & Self-Update","t":"Infrastructure cipi — Server & Self-Update cipi — Server & Self-Update Top-level commands for server status and Cipi self-management. bash Copy Password & credential reset Cipi provides commands to regenerate server-level passwords. New passwords are stored in /etc/cipi/server.json (encrypted via Vault) and displayed on screen. Save them immediately — they are shown only once. bash Copy cipi reset valkey-password (alias cipi reset redis-password ) restarts the Valkey service. Connected clients will be temporarily disconnected. If your apps use Valkey for cache or sessions, expect a brief interruption."}];
